# MOMO 导演台 2.0 · 专业驾驶舱改造验证报告

> 报告日期：2026-08-28
> 依据文档：《MOMO导演台2.0-专业驾驶舱改造方案.md》
> 改造范围：P0 驾驶舱重组 / P1 H3 快速模式与连续性可视化 / P2 轻量后期与正式 MP4 / P3 项目包与自动化闭环（一次性分批完成）
> 验证方式：`npx tsc --noEmit`（0 错误）· `pnpm build`（通过）· `cargo check`（0 错误）· 浏览器 GUI 黑盒冒烟测试（真实点击流）

---

## 一、改造落地清单（对照方案章节）

### P0 驾驶舱重组（§4–§8、§21-P0、§25）

| 能力 | 落地文件 | 说明 |
| --- | --- | --- |
| 四工作区导航（策划/导演/成片/3D） | `DirectorStudio.tsx` 重写 | 顶层只表阶段；导演工作区为驾驶舱；旧「分镜/生成」合并进导演工作区 |
| 快速 / 专业双模式（§4.1 两层体验） | 顶栏模式段 + `GenerationInspector` | 共享同一份 DirectorProject 与队列，无第二套数据模型 |
| 顶栏精简（§5.3） | `DirectorStudio.tsx` | 项目名+保存状态/模式/配方/画幅/像素/时长/总进度/关闭；显存与接力开关移进生成检查器与任务条 |
| 场景/片段树（§6） | `cockpit/SceneSegmentTree.tsx` | 序号/标题/时长/采用缩略图/状态徽标/接力标记/警告数；搜索、状态筛选、Shift/Ctrl 多选、拖动排序（带影响提示）、右键菜单（重新生成/采用最新/复制配方/锁定/打开资产位置）；>30 片段虚拟列表 |
| 中央监看器（§7） | `cockpit/DirectorMonitor.tsx` | 采用>最新完成>参考图合成预览的显示顺位；生成中保留上一版本+进度徽标；播放/逐帧/前后片段/循环/静音/时间与全片位置/适应/全屏；Take A/B 对比；入出点标记 |
| 监看器快捷键（§7.3） | types.ts + `HotkeysTab` + 监看器 | Space/←→/Shift←→/I/O/A/R/P 进全局快捷键设置可改键；1~9 切 Take、J/K/L 变速为内置；画布快捷键在导演台打开时全部让位 |
| 四标签检查器（§8） | `cockpit/SegmentInspector.tsx` + 四个子检查器 | 内容/提示词/参考/生成；标签状态入 uiState 跨会话恢复 |
| Sequence Dock（§11.1） | `cockpit/SequenceDock.tsx` | 缩略块按时长比例、状态点、接力连接标记、点击定位、双击播放、拖动排序+右键上移/下移（无障碍）、总时长与目标偏差 |
| 任务状态条（§5.1） | `DirectorWorkspace.tsx` TaskBar | 批量范围+开始、双条进度（总+当前片段百分比）、消息、取消（软）/停止（硬）；空闲时显示计划摘要 |
| 旧页保留为详细视图（§25.7） | `DirectorWorkspace.tsx` | cockpitView=detail 切换旧分镜表+生成页并排，一键回驾驶舱 |
| 全局选中态（§4.3） | `directorContext.ts` | 当前片段/Take 全局；切换工作区保持；uiState.segId 跨会话恢复 |

### P1 快速模式与连续性（§9、§10、§13）

| 能力 | 落地文件 | 说明 |
| --- | --- | --- |
| 内置工作模式 7 种（§9.2） | `directorQuick.ts` | 文生/首帧/首尾帧/多参考/长视频连续/角色保持/AI MV——只按能力匹配现有 Recipe/模板，匹配不到给配置指引 |
| 硬件档位（§9.3） | `directorQuick.ts` | 节省显存/均衡/高质量/自定义；只认模板真实暴露的步数/精度类参数，eco 自动开每段清显存 |
| 一键预检（§9.4） | `directorPrecheck.ts` | ComfyUI 在线/模板存在/时长画幅合法/素材容量/提示词编号越界/远程计费提示；blocker 只拦受影响片段，其余继续 |
| 连续性上下文胶囊（§10） | `directorContinuity.ts` + 接线 | ContinuityCapsule 类型；接力提取成功即记录来源/状态摘要；上游重新采用 → 紧邻下游标「已过期」+ 用户确认重建；关闭接力素材保留只停投喂（既有语义） |
| 多 Shot 编译修复（§13.1） | `directorPrompt.ts` + `directorQueue.ts` | 通用编译路径从「只编译第一个 Shot」改为全部 Shot 逐时段输出（0–6秒：…/6–15秒：…），共享字段只写一次；角色按本段出场过滤（全不匹配时回退全量防丢主角） |
| 提示词来源图谱 + 版本差异 + 编号校验（§13.2/§8.2） | `directorPrompt.ts` + `PromptInspector.tsx` | 来源分节（项目风格/角色/镜头/用户修改/参考编号/负向）；与上次生成快照行级 diff；`<Picture/Video/Audio N>` 越界检测 |

### P2 轻量后期与正式 MP4（§11、§12）

| 能力 | 落地文件 | 说明 |
| --- | --- | --- |
| 八轨时间线（§11.2/§11.3） | `directorTimeline.ts` + `timeline/PostTimeline.tsx` | V1/T1/D1/N1/S1/A1/M1/SUB；片段点击弹参数浮层（入出点/硬切/交叉淡化/原声音量/淡入淡出）；标题卡/黑场/图片卡增删编辑 |
| 混音器（§12.3） | `timeline/AudioMixer.tsx` | 每轨音量/静音/淡入淡出；对白旁白 TTS；渲染真实生效 |
| 字幕编辑器（§12.2） | `timeline/SubtitleEditor.tsx` | 手写字幕 + 从对白一键铺底；导出 SRT 或烧录 MP4 |
| 正式渲染引擎（§12.1） | Rust `media_render.rs` + `directorRender.ts` | 外部 ffmpeg（用户 PATH/常见位置/指定路径）；片段裁切规范化→拼接→音频 amix+alimiter 峰值保护→字幕烧录→H.264 MP4；临时目录工作+原子落位（失败不覆盖旧成片）；进度事件+取消（杀进程树）；浏览器降级 MediaRecorder 并明确标注 |
| 媒体探测（§12.1/§15.4） | Rust `media_probe.rs` + `directorQuality.ts` | ffprobe 探测时长/分辨率/帧率/音轨/损坏；浏览器 video 元素降级；采用 Take 批量体检 → qualityReports，报告只提示不自动花钱 |
| 导出预设（§12.2） | `directorRender.ts` | 1080 横/竖、2K、4K、小红书、抖音/视频号 |
| EditingPage 升级 | `PostWorkspace.tsx` | 新工具条+时间线+混音+字幕叠加在原成片检查能力之上；旧 MediaRecorder 按钮标注「降级」 |

### P3 项目包与自动化（§14、§15、§16）

| 能力 | 落地文件 | 说明 |
| --- | --- | --- |
| `.momoproject` 导出/导入（§16.3） | Rust `project_pack.rs` + `core/directorProjectIO.ts` | 目录式包（project.momo.json+templates.json+manifest+assets/）；不含 API Key、不打包模型；manifest 携带资产 id+指纹；导入按指纹去重复用、模板快照补回、缺失模板报告、重链接资产 id、以新项目落库 |
| 项目级 AI 导演（§14） | `agent/DirectorAgentDock.tsx` | 上下文注入（片段/提示词摘要/质量报告/连续性问题）；改写摘要/对白/精炼提示词（差异预览确认后写入）/解释失败；锁定最终稿不静默改写；不直接发起计费生成 |
| v1→v2 迁移唯一入口（§16.4） | `directorMigration.ts` | schemaVersion=2，补 workspaceMode/uiState/postTimeline/capsules/qualityReports 缺省；加载即迁移（含归档） |
| 片段级更新 API（§18） | `directorStore.patchSegment` | 大项目减少整树 map 样板；树行 memo 化 + 虚拟列表 |

---

## 二、自我验证过程与结果

### 1. 静态校验
- `npx tsc --noEmit`：**0 错误**（改造中间过程暴露 30+ 类型错误已全部修复）
- `pnpm build`（tsc + vite）：**通过**
- `cargo check`：**0 错误**（新增 media_probe/media_render/project_pack 三模块，过程中的所有权/未用告警已清）

### 2. 浏览器 GUI 黑盒冒烟（Vite 1430 浏览器预览模式，真实点击流）

| # | 测试点 | 结果 |
| --- | --- | --- |
| 1 | 画布创建导演台节点 → 进入导演台 | ✅ |
| 2 | 四工作区壳渲染（策划/导演/成片/3D + 快速/专业 + 画幅/时长/像素/配方/保存状态/进度） | ✅ |
| 3 | 策划页粘贴分段脚本 → 「检测分段」→ 3 场 3 片段 → 自动跳转导演工作区 | ✅ |
| 4 | 驾驶舱布局：树/监看器/Dock/任务条/检查器 FAB 全部渲染 | ✅ |
| 5 | 场景树：场景折叠头、片段行（01-03 序号/摘要/15s/缺片徽标/勾选框）、搜索、5 档状态筛选 | ✅ |
| 6 | 任务条计划摘要「3 个片段待处理（缺失）」+ 批量范围下拉 | ✅ |
| 7 | 点击片段 → 检查器同步显示同片段（内容标签：摘要/时长/状态/锁定/对白/镜头/承接） | ✅ |
| 8 | 提示词标签三层：导演描述 / H3（非 H3 段禁用）/ 实际请求（编译文本正确显示） | ✅ |
| 9 | 快速模式：工作模式/画质档位（均衡）/声音模式/接力开关/生成按钮 | ✅ |
| 10 | 专业模式：片段配方下拉 + 5 个参考入口徽标（✓/×）+ 工作流指纹 | ✅ |
| 11 | AI 导演抽屉：上下文卡（缺片 · 15s · 0 个版本）+ 四动作按钮 | ✅ |
| 12 | 成片工作区：渲染工具条（预设/检测 ffmpeg/质量探测/预演/导出 MP4）+ 八轨时间线 + 混音器 + 字幕编辑器 + 原成片检查全保留（① 缺片 ② 预演 ③ 放大 ④ 交付 + 连续性右轨） | ✅ |
| 13 | 标题卡：添加弹层三选项 → T1 轨出现块 → 点击弹编辑浮层（位置/时长/文本/删除） | ✅ |
| 14 | 导出拦截：无采用片段时 toast「还没有可渲染的采用片段」 | ✅ |
| 15 | ffmpeg 检测：浏览器模式降级提示（winget 指引文案） | ✅ |
| 16 | 详细视图 ↔ 驾驶舱切换（旧分镜表+生成页并排，批量工具齐全） | ✅ |
| 17 | 页面重载 → 重新进入 → uiState 恢复（工作区+检查器标签+选中片段） | ✅ |
| 18 | **多 Shot 编译**：片段添加 2 个镜头（0-6 中景固定机位 / 6-15 特写缓推）→ 实际请求输出 `0–6秒：中景，固定机位，…` / `6–15秒：特写，缓推，…` + 共享字段单次输出 | ✅（验收 22.2-1） |
| 19 | 监看器时间显示（00:15.00 = 片段时长兜底）与控制条 10 键 | ✅ |

---

## 三、发现 bug（共 14 项，全部已修复）

### 浏览器 GUI 测试发现（5 项）

| # | Bug | 位置 | 修复 |
| --- | --- | --- | --- |
| 1 | 监看器空态文案不区分「未选中片段」与「已选中但无素材」，已选中时误导用户「去选择」 | `DirectorMonitor.tsx` | 区分两种空态文案，后者引导到生成检查器/参考配置 |
| 2 | 导出预设描述写「1920×1080」但 mpToSize 按 16 倍数对齐实际产出 1920×1072，描述与实际不符 | `directorRender.ts` | 描述加「约」并注明宽高对齐 16 倍数 |
| 3 | 残留旧导航文案「到「生成」页采用片段」「到分镜页把…」（生成/分镜页已并入导演工作区，按名字找不到页） | `EditingPage.tsx` / `GenerationPage.tsx` | 改为指向「导演」工作区 |
| 4 | 成片「预演（应用入出点）」直接把资产本机路径塞给播放器，Tauri 下未经 asset:// 转换会黑屏 | `PostWorkspace.tsx` | 走 `assetUrl()` 转换 |
| 5 | Sequence Dock 计划时长与目标相等时显示「超 0s」 | `SequenceDock.tsx` | 偏差 <0.5s 时显示「= 目标 Ns」 |

### 编译期（tsc/cargo）暴露的编码缺陷（9 项）

| # | Bug | 修复 |
| --- | --- | --- |
| 6 | `GenerationInspector` 三处 JSX map 回调括号错配（语法错误） | 修正；复杂数组改先算变量 |
| 7 | 6 个新文件把 `toast` 从 `core/utils` 导入（实际在 `uiStore`） | 全部改为 uiStore 导入 |
| 8 | `.at(-1)` 在当前 TS lib 目标下不可用（3 处） | 改为长度索引 |
| 9 | 硬件档位读模板参数用了不存在的 `param.name`（实际是 `key`/`label`/`input`） | 修正字段名 |
| 10 | `directorProjectIO` 误放在 modules 目录（相对导入全断） | 移至 `core/`；同时修 comfyStore 恢复模板调用了不存在的 `importTemplates` → 改 `upsert` 循环 |
| 11 | 项目包导入端无法重建「旧资产 id → 包内 rel」映射（导出端顺序信息不可复原），重链接会失效 | manifest 每项携带 `assetId`，导入直接反查 |
| 12 | 导入项目用 `useDirector.setState` 直接写 store，绕过 persist 落盘（重启即丢） | directorStore 新增 `addImportedProject` 走统一持久化 |
| 13 | Rust `media_render`：Child 被 move 进取消注册表后再 `stdout.take()`/`wait()` 的所有权冲突 | 改为 pid 注册 + `taskkill /T` 杀进程树，规避所有权问题 |
| 14 | Rust 混音：`AudioR.muted` 字段声明未用，静音轨仍会进 amix | 混音前过滤静音轨 |

---

## 四、功能优化（相对旧版的净增能力）

1. **单屏导演闭环**：选片段→看画面→改内容→改提示词→调参考→生成→选 Take 全程不离开「导演」工作区（P0 完成标准达成）。
2. **监看器为一等公民**：全片时间位置、A/B 对比、入出点、逐帧、变速（J/K/L）、可配置快捷键；生成中不闪空白。
3. **提示词可追溯**：来源图谱、与上次生成 diff、编号越界校验、最终稿修订留档（promptRevisions）。
4. **连续性可见可管**：「继承自 06 · Take 2」徽标、过期传播 + 确认重建、Sequence Dock 接力链标记。
5. **普通创作者路径**：7 种快速模式 + 硬件档位 + 声音模式，匹配不到给明确指引（不出现空参数）。
6. **生成前置安全网**：统一预检→确认卡（范围/引擎/本地远程/计费风险/档位/问题列表），blocker 只拦受影响片段。
7. **真后期**：八轨时间线、入出点/交叉淡化、混音（音量/静音/淡化+峰值保护）、字幕（手写/对白铺底/烧录）、标题卡三型、六档导出预设。
8. **正式 MP4 引擎**：ffmpeg 管线原子落位、失败不覆盖旧成片、进度+取消；MediaRecorder 降级为浏览器预览专用并明确标注。
9. **质量闭环**：采用 Take 一键体检（分辨率/帧率/时长/音轨/损坏）出报告，修复动作由用户确认。
10. **项目可迁移**：.momoproject 导入导出、指纹去重、模板快照补回、缺失报告、新项目落库不覆盖原项目。
11. **AI 导演在项目内**：上下文感知的改写/诊断，差异预览确认写入，不触碰锁定稿、不直接计费。

## 五、流程优化

1. **拆分即进入导演**：三种拆分入口（AI 拆分/标记检测/直录）成功后自动切到导演工作区，省一次手动导航。
2. **打开回到现场**：uiState 持久化——重开导演台回到上次的工作区、检查器标签与选中片段（验收 22.1-1）。
3. **确认闸统一**：单段生成与批量生成共用「预检→确认卡→执行」链路，计费风险提示不再散落各页。
4. **取消/停止双语义保持**：取消批量=跑完当前段停；停止=立即中断 ComfyUI+清队列+清显存（与既有实现一致，任务条常驻入口）。
5. **详细视图作为过渡**：资产册绑定、文件夹绑定、AI 精读等旧批量工具完整保留在详细视图，驾驶舱稳定前不删重复入口（§25.7）。
6. **画布/导演台快捷键互斥**：导演台打开时画布快捷键让位（仅保留 Ctrl+Shift+D 关闭出口），避免 V/G/I 等误触画布。

## 六、遗留事项与建议

1. **ffmpeg 依赖**：正式 MP4 导出需本机安装 ffmpeg（`winget install ffmpeg` 或官网下载）；未安装时「检测 ffmpeg」给出指引文案。后续可在设置页加路径配置 UI（当前偏好已持久化在 director-projects.json 的 mediaTool 字段）。
2. **标题卡字体**：烧录/绘制标题依赖系统字体（微软雅黑/黑体），缺失时自动退化为黑场并提示。
3. **Premiere XML 不带片段入出点**：XML 交付给外部剪辑软件的是完整素材（外部剪辑自行裁剪）；MP4 导出已带入出点。若需要 XML 也裁剪，可在 generatePremiereXml 里合并 clipOverrides（后续迭代）。
4. **黑帧比例/音频峰值探测**：质量报告当前覆盖时长/分辨率/帧率/音轨/损坏；黑帧比例与音频峰值需要 ffmpeg 深度分析（blackdetect/astats），列为后续增强。
5. **微视频参考（16~32 帧）**：胶囊类型已留 `microReferenceAssetId` 字段；运行时提取按现有「末尾 2 秒」逻辑，帧数可调的微参考提取留待 H3 模板参数化后接入。
6. **测试工具备注**：IAB 自动化对 fixed 定位元素的 actionability 判定偏严（「进入导演台」「检查器」按钮需坐标点击），属测试工具特性，真实鼠标操作不受影响。

---

**结论**：P0–P3 四阶段改造全部落地；类型检查、前端构建、Rust 编译、浏览器 GUI 冒烟（19 个测试点）全部通过；过程中发现 14 项缺陷已全部修复。方案 §22 验收用例中可在本环境验证的条目（22.1 UI 使用、22.2 提示词、22.4 生成与恢复的队列语义）均通过；22.3 连续性接力、22.5 成片导出、22.6 项目包的完整链路需在桌面端 + ComfyUI/ffmpeg 环境下由真实生成任务复核（引擎与数据通路已按文档实现并通过编译与降级路径验证）。
