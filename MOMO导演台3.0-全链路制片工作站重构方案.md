# MOMO 导演台 3.0：全链路制片工作站重构方案

> ⚠️ **架构纠偏通知（2026-08-29）**  
> 本文原版将“工位界面独立”误扩展为“底层能力也独立”，会与 MOMO 画布的角色卡、生图、创作助手、资产库和任务执行形成职责双轨。七工位产品概念保留，但其智能职能与共享底层必须按 3.2 重新划分。  
> 后续以《[MOMO 导演台 3.2：智能职能深化与通用能力复用方案](./MOMO导演台3.2-智能职能深化与通用能力复用方案.md)》为准；本文仅保留为参考视频功能调研和历史决策记录。

> 文档状态：重构基线（取代《MOMO导演台2.0-专业驾驶舱改造方案》）  
> 规划日期：2026-08-29  
> 覆盖口径：100% 覆盖参考视频中可观察、可验证的产品功能；不臆测视频未展示的私有实现  
> 重构原则：保留 MOMO 已有的数据、工作流与生成能力；重做产品信息架构、桌面交互和缺失链路

---

## 0. 结论先行

2.0 的问题不是“还不够像参考软件”，而是产品模型仍然错了。

2.0 把原来的五页压成“策划 / 导演 / 成片 / 3D”四个阶段，再把新能力塞进驾驶舱、抽屉、详细视图和卡片区。功能数量增加了，但用户仍然在一套阶段表单里工作，结果是：

- 导航按流程阶段组织，剧本、角色、图片、MV 等内容工具没有独立工作空间；
- 一个功能存在驾驶舱、详细视图、旧页面等多个入口，用户不知道哪一个是真入口；
- 顶栏同时承载模式、画幅、时长、像素、配方和进度，像配置表而不是创作工具；
- 浅色主题中，黑色监看器、黑色时间线和黑色 3D 视口成为三块孤岛；
- AI 导演只是一个抽屉，剧本仍是大文本框，AI 制图和 AI MV 也没有形成产品级工位；
- 22 帧微视频参考在数据类型里只有占位字段，没有完成“提取—绑定—失效—重建—生成”的闭环。

3.0 不再修补旧四页，而是把导演台重构为一个独立的 **AI 制片工作站**：

```text
AI 导演 → 剧本库 → 角色与场景资产 → AI 制图 → H3 导演台 → 成片交付
                           ↘ 3D 预演 ↗        ↘ AI MV ↗
```

左侧导航不再表示“第 1、2、3、4 步”，而表示可以随时切换、共享同一项目数据的专业工位。H3 导演台是主工位，其他模块为它生产或消费结构化数据。

这次允许 UI 大面积重做，但不推倒 MOMO 的核心能力。现有 ComfyUI 配方、远程模型、素材槽、Skill、提示词编译、生成队列、Take、空间接力、3D、FFmpeg、项目包继续使用；需要被替换的是它们面向用户的组织方式。

---

## 1. 调研口径与视频功能复盘

### 1.1 信息来源

本方案基于三类证据：

1. 用户提供的小红书视频《MiniMax H3 导演台巨大优化升级！》，完整时长约 8 分 33 秒；前约 2 分 35 秒是功能演示，之后主要是完成样片播放。
2. 用户提供的四张 MOMO 2.0 当前界面截图。
3. 公开的 [ComfyUI MiniMax H3 Director 工作流](https://github.com/huangserva/ComfyUI_MiniMaxH3_Director)，用于校验 H3 执行模式边界：T2V、FL2V/I2V、R2V、V2V、RV2V、逐段运行与缓存、音频策略、尾帧交接。

小红书页面当前返回“笔记暂时无法浏览”，所以所有竞品功能结论以已经留存的完整视频画面为准，不把搜索摘要当作事实来源。

视频画面还能确认一个关键事实：AI 制图页面顶部明确出现 `Z-IMAGE STUDIO`，左侧长期存在 AI 导演、剧本库、角色库、AI 制图、AI MV 等模块入口。也就是说，参考对象并不是“一个做得更漂亮的导演页面”，而是一套模块化 Studio 产品。2.0 只复刻了其中的监看器布局，却没有复刻它的产品架构，这是本次偏差的根源。

### 1.2 视频中可见的功能清单

| 约略时间 | 可观察功能 | 3.0 必须覆盖的验收口径 |
| --- | --- | --- |
| 00:00–00:20 | H3 主导演台：左侧分镜列表、中间监看器、下方镜头/帧条与音频条、右侧生成参数 | 单屏完成选段、监看、改提示词、配置参考、生成、选版本 |
| 00:15 | 所有分镜均有预览 | 未生成视频时显示分镜图；有 Take 后显示采用版；生成中不闪空 |
| 00:25–00:35 | 提示词与角色页面可独立同步、可独立编辑 | 提示词修订与角色修订各自版本化；可选择同步范围并预览影响 |
| 00:40–00:50 | 分镜的 22 帧微视频参考，用于连贯长视频生成 | 真正提取上一段末 22 帧，绑定到下一段视频参考并处理过期重建 |
| 00:50–01:05 | 分镜自定义音频位置、基础镜像/变换、视频与音频叠加、整条分镜视频链 | 预演与正式导出使用同一时间线数据；调整必须真实进入 MP4 |
| 01:10–01:25 | AI 导演接入 LLM API；既可内置使用，也可由 Codex Harness 等 Agent 协作 | 内置 AI 导演 + 外部 Agent 网关；写项目必须经过差异确认 |
| 01:25–01:45 | 对话形成草稿，确认后保存到剧本库；提供导入/归并选择 | AI 方案一键存为新剧本或新版本，不直接破坏当前项目 |
| 01:45–02:05 | 从剧本库二次修改；图片集中保存；AI 制图结果可进入生产库 | 剧本、角色、图片都成为可复用实体，不藏在单个页面表单中 |
| 02:00–02:15 | AI 制图支持本地与火山 Seedream API，量化档位及生成参数 | 文生图/图生图/编辑图；本地 ComfyUI 与远程 Provider 为同一适配层 |
| 02:15–02:35 | AI MV：音乐轨与多张图关联，形成 MV；包含口型/歌曲方向 | 音乐分析、图片映射、分段生成、口型分支、合成导出形成闭环 |
| 02:35–08:33 | 完整长片播放 | 已采用片段、声音与转场可连续预演并正式导出 |

### 1.3 “100%”的准确含义

本方案承诺的是：

- 视频里能看到的功能，在功能覆盖矩阵里都有明确模块、数据和验收用例；
- 不以“有按钮”视为完成，必须走通数据写入、任务执行、结果回流和正式导出；
- 对视频没有展示的账户、云后台、模型训练、计费等内部实现不做虚假复刻；
- MOMO 已经强于视频的软件能力继续保留，但不得破坏主链路的清晰度。

视频侧栏还短暂出现若干数字人/音频类入口，但没有进入页面、没有展示操作和结果。它们只能证明 Studio 预留了更多模块，不能证明具体功能，因此不纳入“100% 可见功能”承诺；3.0 的导航保留扩展位，待取得实际演示后再立项，避免只复刻空按钮。

---

## 2. 当前 2.0 为什么失败

### 2.1 截图 1：成片页是两个产品叠在一起

上方是黑色轨道区，下方又是一组浅色统计卡、缺片卡、连续性卡和预演卡。时间线是编辑工具，卡片区是管理后台，两套视觉层级互相争抢。

改造要求：成片页必须成为全屏后期工位。播放器、时间线、混音、字幕、质量问题和导出围绕同一播放头组织；统计只作为窄状态条或检查器分组，不再铺成纵向仪表盘。

### 2.2 截图 2：导演页有监看器，却没有“导演动作”

中央监看器占据绝大多数空间，但空态时只是巨大黑块；左侧片段树信息太弱，右侧检查器与中央内容没有视觉连接，下方也没有形成稳定的分镜/音频链。

改造要求：监看器、22 帧参考条、音频波形和 Take 切换构成一块连续的暗色制作舞台；左侧片段、中央播放头、右侧参数必须同步选中同一 `segmentId`。

### 2.3 截图 3：策划页仍然是“大文本框 + 卡片”

剧本编辑几乎等于填写一张巨大表单，AI 拆解和 Skill 被放在右侧卡片里，用户看不到对话、方案、结构大纲与版本之间的关系。

改造要求：取消“策划页”。用独立的 AI 导演工位和剧本库承接：AI 导演负责共创、拆解和方案；剧本库负责正式文本、版本、对比、归并和送入项目。

### 2.4 截图 4：3D 本体正确，外壳错误

3D 视口本身已经接近专业工具，但白色总导航、白色属性面板和暗色视口没有共同的层级规则，看起来像网页外壳中嵌入了另一个应用。

改造要求：3D 视口与 H3 监看器共用 `stage` 语义表面；工具架和属性检查器使用同一套停靠面板，不再套圆角白卡。

### 2.5 结构性问题

| 2.0 设计 | 造成的问题 | 3.0 决策 |
| --- | --- | --- |
| 策划 / 导演 / 成片 / 3D 四阶段导航 | 内容库和工具被塞进阶段页 | 改为专业工位导航 |
| 全局快速 / 专业模式 | 同一能力出现两套表达和隐藏状态 | 删除；参数在检查器内渐进展开 |
| 驾驶舱 / 详细视图双入口 | 两个“真相页面”，维护和认知都翻倍 | 删除详细视图入口；必要能力迁入唯一工位 |
| 顶栏放画幅、时长、像素、配方 | 项目栏变成横向参数表 | 顶栏只保项目、状态、命令、导出、关闭 |
| 大量圆角卡片 | 像 SaaS 仪表盘，不像制片工具 | 改为平面停靠面板 + 1px 分隔线 |
| AI 导演抽屉 | 不能承担完整创作与剧本沉淀 | 升级为一级工位 |
| 旧页面整体保留 | 页面逻辑和文案持续割裂 | 只兼容数据，不兼容旧 UI |

---

## 3. 新产品定义

### 3.1 产品名称与定位

建议导演台全屏层内部使用工作名称：**MOMO AI 制片工作站**。

它不是一个“导演节点的参数面板”，也不是一个缩小版 Premiere，而是连接以下对象的项目操作系统：

- 故事与剧本；
- 角色、场景、道具和声音资产；
- 分镜、镜头、提示词与参考素材；
- H3 / ComfyUI / 远程模型生成任务；
- Take 版本、连续性与后期时间线；
- 3D 预演、AI MV 和最终交付。

### 3.2 核心产品原则

1. **单一项目真相**：所有工位读写同一个 `StudioProject`，不复制剧本和素材。
2. **单一功能入口**：同一任务只在一个主工位完成，其他地方只能跳转或显示摘要。
3. **对象驱动而非页面驱动**：选择一个角色、片段、Take 或音轨后，所有关联区域同步。
4. **生成与后期共用时间线事实**：预演和导出不得各算一套。
5. **AI 写入可审计**：任何结构性修改先展示差异、影响范围和费用风险。
6. **主题改变颜色，不改变产品语法**：三主题共享布局、尺寸、层级、边界和交互。

---

## 4. 3.0 信息架构：一壳七工位

### 4.1 一级导航

左侧一级导航固定为以下工位，不编号：

1. **AI 导演**：故事共创、方案拆解、外部 Agent 协作。
2. **剧本库**：草稿、正式剧本、版本、导入和项目化。
3. **角色库**：角色档案、外观、声音、参考素材和受影响片段。
4. **AI 制图**：文生图、图生图、图片编辑、结果入库与绑定。
5. **H3 导演台**：分镜、22 帧微参考、提示词、参考、生成与选 Take。
6. **AI MV**：音乐、歌词、节拍、图像映射、口型和 MV 生成。
7. **成片交付**：视频轨、音频轨、字幕、质检、预演和导出。

左侧底部放置不参与主流程排序的共享工具：

- **3D 预演**；
- **资产库**；
- **任务中心**；
- **引擎状态 / 设置**。

### 4.2 为什么不再使用“步骤”导航

真实制作会来回修改：成片质检发现服装漂移，要回角色库；H3 生成发现构图有问题，要去 AI 制图或 3D；AI MV 可能直接消费角色图与音乐，不经过普通剧本。

所以流程只在数据状态中存在，不应绑死在导航里。顶部可显示项目进度，但导航本身必须允许自由往返。

### 4.3 全局外壳

```text
┌──────────────────────────────────────────────────────────────────────┐
│ 项目名 / 当前对象   保存状态   引擎状态   ⌘命令   导出   关闭       │ 44
├──────┬───────────────────────────────────────────────────────────────┤
│ 一级 │ 当前工位：可选的上下文栏 │ 主制作区              │ 检查器 │
│ 导航 │ 220–260                 │ 自适应                │ 300–360│
│ 56–64│                         │                       │        │
└──────┴───────────────────────────────────────────────────────────────┘
```

规则：

- 顶栏不再出现画幅、时长、像素、配方和快速/专业开关；
- 这些参数进入“项目设置”或当前对象检查器，首次生成前统一预检；
- 工作区不使用页面级纵向滚动，只有列表、编辑器和检查器各自滚动；
- 所有面板可拖动调整宽度，支持隐藏与恢复默认布局；
- 当前对象地址可表达为 `项目 / 场景 / 片段 / Take`，方便返回与定位。

---

## 5. 统一视觉系统：三主题，一套制片语法

### 5.1 关键策略：Chrome 与 Stage 分层

软件需要两类表面：

- **Chrome**：导航、列表、检查器、弹窗，跟随云白 / 深空蓝 / 深邃黑主题；
- **Stage**：视频监看器、时间线、波形、3D 视口，始终是中性暗色制作舞台。

Stage 在浅色主题中也保持暗色，但必须是从监看器贯穿到底部时间线的连续区域，不能再作为白卡片里的孤立黑块。这种做法和专业影像软件一致：颜色主题可以变，影像判断环境必须稳定。

### 5.2 语义令牌

新增导演台专用语义层，组件禁止直接写主题色：

| 令牌 | 用途 | 云白 | 深空蓝 | 深邃黑 |
| --- | --- | --- | --- | --- |
| `--studio-chrome` | 应用外壳 | 浅灰白 | 深蓝黑 | 中性黑 |
| `--studio-panel` | 列表/检查器 | 白色 | 蓝灰面板 | 深灰面板 |
| `--studio-panel-2` | 二级分组 | 微灰 | 稍亮蓝灰 | 稍亮黑灰 |
| `--studio-stage` | 监看/时间线/3D | 近黑 | 近黑 | 近黑 |
| `--studio-stage-panel` | 舞台内控件 | 暗灰 | 暗蓝灰 | 暗灰 |
| `--studio-border` | 所有停靠分隔 | 主题低对比边界 | 主题低对比边界 | 主题低对比边界 |
| `--studio-text-*` | 三级文字 | 映射全局文字令牌 | 映射全局文字令牌 | 映射全局文字令牌 |
| `--studio-accent` | 当前选择与主动作 | `var(--accent)` | `var(--accent)` | `var(--accent)` |

深邃黑主题继续使用项目已有的暖色强调色。禁止在导演台写死蓝色光晕；所有强调外发光使用 `color-mix(in srgb, var(--accent) …)`。

### 5.3 形态规则

- 核心工作区采用平面停靠布局，不使用 Bento 卡片墙；
- 面板圆角 0–6px，只有弹窗、浮层和媒体缩略图允许 8px；
- Pill 只用于状态、筛选和片段标签，不用于普通按钮；
- 阴影只用于浮层、菜单、模态框；停靠面板靠 1px 分隔线建立层级；
- 一级导航图标统一手绘 SVG、统一描边粗细；激活态为窄色条 + 文字权重，不画巨大圆角边框；
- 数字、时码、帧号、百分比使用等宽数字；
- 动效只表达面板展开、选择传播、任务状态和媒体载入，150–240ms，并支持减少动态效果。

### 5.4 密度与尺寸

| 元素 | 标准 |
| --- | --- |
| 顶栏 | 44px |
| 一级导航 | 60px，图标 + 短标签 |
| 列表行 | 紧凑 48px；带双行摘要时 64px |
| 主按钮 | 32–36px 高；一屏只允许一个高强调主动作 |
| 图标点击区 | 最小 32×32px，关键动作 36×36px |
| 检查器字段 | 标签常显，复杂参数使用折叠分组 |
| 面板间距 | 4 / 8 / 12 / 16 的统一节奏 |

这是桌面生产工具，不机械套用移动端 44px 密度；但所有可点击图标都必须有清晰热区、焦点态和键盘替代。

---

## 6. 各工位完整设计

## 6.1 AI 导演

### 布局

```text
对话/方案列表 │ 对话与视觉草稿画布                    │ 剧本大纲与项目事实
              │ 消息、图片、候选方案、差异确认        │ 角色/场景/时长/风险
```

### 功能

- 新建创作会话、保存会话、从现有项目继续；
- 支持文字、图片、剧本片段和当前项目上下文；
- 生成故事梗概、角色设定、场景表、分段方案和提示词草案；
- AI 输出中的角色、场景、片段可单独采用，不要求整份接受；
- “保存到剧本库”提供：新建剧本、另存新版本、合并到当前剧本三种明确动作；
- 合并前显示新增、修改、删除及受影响片段；
- 内置 Agent 不直接发起计费生成，生成动作仍走统一预检和确认闸。

### 外部 Agent 网关

视频里展示了 Codex Harness 等 Agent 协作。MOMO 需要提供受控的项目动作协议，而不是让外部 Agent 直接修改存储文件：

```text
read_project_context
propose_script_patch
propose_character_patch
propose_shot_patch
propose_prompt_patch
explain_generation_error
```

所有 `propose_*` 只返回变更草案；用户在 MOMO 内确认后才写入。第一版可以使用本机 localhost IPC / Tauri command，后续再包装成 MCP Server。

## 6.2 剧本库

### 不是文件列表，而是正式内容库

- 草稿 / 正式 / 归档状态；
- 标题、类型、预计时长、场景数、片段数、更新时间；
- 文本、Markdown、结构化分段提示词包三态导入继续保留；
- 版本时间线、双栏差异、回退和分支；
- 从 AI 导演存入、从文件导入、从项目另存；
- “送入项目”时可以新建项目、替换项目剧本或合并场景；
- 任何替换都先提示将失效的分镜、提示词、22 帧参考和 Take。

### 编辑布局

左侧版本与场景目录，中间正文/结构编辑器，右侧角色、场景、时长和结构检查。取消整页巨大空白文本框。

## 6.3 角色库

### 角色实体

角色不再只是 `name + continuity + assetIds`，升级为：

- 基础身份、年龄段、体型、外貌锚点、服装组；
- 正面 / 侧面 / 全身 / 表情 / 动作参考；
- 声音参考与默认 TTS 音色；
- 可选 3D 模型或人偶预设；
- 出场剧本、场景和片段反向索引；
- 修订版本和“锁定身份”状态。

### 独立同步

角色修改后不静默重写所有提示词，而是把受影响片段标为“角色来源已更新”。用户可以：

- 只更新未手改的片段；
- 更新所选片段；
- 查看差异后逐段应用；
- 保留旧角色快照继续生成。

这才是视频中“提示词与角色页面可以独立同步”的可靠实现。

## 6.4 AI 制图

### 页面结构

```text
生成历史/预设 │ 参数与提示词主区                     │ 结果与版本
```

### 必备能力

- 文生图、图生图、图片编辑三个一级标签；
- 引擎可选本地 ComfyUI 或远程 Provider；
- 火山 Seedream 只是 Provider 之一，不写成专用逻辑；
- 画幅、分辨率、步数、种子、量化/显存档位、批量数量；
- 结果多版本对比、收藏、重跑、参数复用；
- 一键存入资产库、设为角色参考、设为场景图、绑定到所选片段；
- 本地生成显示模型加载阶段与显存状态，远程生成显示计费提示；
- 保留 MOMO 已有模型家族参数推断和 ComfyUI 配方能力。

“INT8 约 6GB”只能作为某个已验证模型预设的说明，不得把硬件数字写成平台通用承诺。

## 6.5 H3 导演台（核心工位）

### 桌面宽屏布局

```text
┌──────────────┬─────────────────────────────────────┬────────────────┐
│ 场景/片段树  │              暗色制作舞台           │ 当前片段检查器 │
│ 缩略图       │  ┌───────────────────────────────┐  │ 提示词         │
│ 状态/时长    │  │           监看器              │  │ 角色/参考      │
│ 搜索/筛选    │  └───────────────────────────────┘  │ 模型/参数      │
│              │  Take 条 / 22 帧微参考 / 音频波形 │ 生成/采用       │
└──────────────┴─────────────────────────────────────┴────────────────┘
```

### 左侧片段树

- 场景分组、片段序号、标题、时长、采用缩略图；
- 状态：缺分镜、缺参考、可生成、排队、生成中、失败、待采用、已采用、来源已过期；
- 搜索、状态筛选、多选和拖动排序；
- 单击选择，双击从该段播放；
- 不再显示大段提示词和技术参数。

### 中央暗色制作舞台

1. **监看器**：采用 Take > 最新成功 Take > 分镜图 > 明确空态。
2. **Take 条**：缩略图、版本号、耗时、配方、采用、A/B 对比。
3. **镜头/帧条**：显示片段内部镜头区间和 22 帧微参考。
4. **音频波形**：本段原声、参考音频、对白/旁白位置。
5. **播放头**：监看、波形、镜头与后期时间线共用时码语义。

### 右侧检查器

采用四个稳定分组，但不做页面级标签跳转：

- **导演描述**：摘要、镜头、动作、对白、连续性；
- **提示词**：结构化来源、实际 H3 请求、修订差异、锁定；
- **角色与参考**：图片 / 视频 / 音频槽，显示真实 `<Picture/Video/Audio N>` 编号；
- **生成**：工作模式、配方、时长、质量、种子、声音策略、生成动作。

常用项常显，高级参数折叠。删除全局“快速/专业”开关。

### 22 帧微视频参考闭环

```text
上一段采用 Take
  → 媒体探测
  → 从稳定结尾提取连续 22 帧
  → 生成 micro-reference 资产
  → 写入 ContinuityBridge
  → 显示来源“段 06 / Take 2 / 末 22 帧”
  → 绑定下一段可用的 Video 槽
  → 编译提示词中的空间/动作承接说明
```

规则：

- 默认只取故事顺序紧邻上一段，禁止跨缺失片段乱接；
- 上游重新采用后，下游微参考立即标记过期；
- 用户可预览 22 帧、重取稳定区间、关闭投喂但保留资产；
- 配方无视频槽时退化为稳定尾帧 + 文字状态，不伪装为已使用微视频；
- 提取工作由 Rust/FFmpeg 完成，Web 预览只做只读降级；
- 资产、来源 Take、帧区间、fps 和指纹必须可追溯。

### H3 模式覆盖

统一由配方能力适配，不让用户面对工作流文件名：

| 创作语言 | 执行模式 |
| --- | --- |
| 文字生成片段 | T2V |
| 首帧 / 首尾帧生成 | I2V / FL2V |
| 图片、视频、音频多参考 | R2V |
| 按源视频时间轴改段 | V2V |
| 源视频 + 人物图/声音替换 | RV2V |

逐段选择、缓存、失败重跑、原声/生成音频/静音继续沿用现有执行能力。

## 6.6 AI MV

### 产品链路

```text
选择歌曲/音乐资产
  → 探测时长、节拍、段落与可选歌词
  → 导入或生成多张视觉图
  → 把图片/角色/场景绑定到音乐区间
  → 生成各区间视频
  → 可选人物口型分支
  → 自动拼接、混音、字幕
  → 送往成片交付
```

### 页面布局

- 左侧：歌曲信息、歌词、素材池；
- 中央：预览器 + 音乐波形 + 节拍/段落轨 + 图片/视频轨；
- 右侧：当前区间提示词、角色、运动、口型、模型和生成设置；
- 顶部：分析音乐、批量生成、预演、送往成片。

### 首版边界

- 支持一首音乐绑定多张图；
- 支持按区间生成和替换，不重跑整首；
- 支持歌词字幕和基础节拍切换；
- 口型作为独立配方能力，未配置时明确显示不可用；
- 与普通 H3 导演台共享 Take、参考、队列和资产，不复制一套生成引擎。

## 6.7 成片交付

### 页面结构

```text
素材/问题列表 │ 监看器 + 多轨时间线                    │ 属性/质检/导出
```

### 真实后期链路

- V1 视频片段；
- T1 标题/黑场/图片卡；
- D1 对白、N1 旁白、S1 音效、A1 环境、M1 音乐；
- SUB 字幕；
- 入出点、硬切/交叉淡化、水平/垂直镜像、旋转、适配/裁切；
- 每轨音量、静音、淡入淡出、峰值保护；
- 缺片、损坏、时长、分辨率、帧率、音轨、连续性检查；
- 顺序预演使用真实裁切、变换、声音和字幕；
- FFmpeg 正式导出 MP4，同时支持 Premiere XML、SRT、镜头表和项目清单。

删除当前纵向卡片仪表盘。质量问题以时间线标记 + 右侧问题列表呈现，点击问题直接定位播放头。

## 6.8 3D 预演

- 全屏暗色 Stage，与 H3 监看器共用制作舞台令牌；
- 左侧窄工具架：实体、角色、机位、光源、导出；
- 右侧属性检查器跟随三主题，不使用白色大卡；
- 导演视角 / 机位视角、Gizmo、角色姿势、相机参数、灯光参数继续保留；
- 导出站位图时写入 `referenceRole: spatialLayout`，可直接送到片段参考；
- 3D 视口只在此工位全屏存在，不嵌入其他页面卡片。

---

## 7. 完整生产链与同步规则

### 7.1 主链

```text
新建项目
  → AI 导演共创或导入剧本
  → 保存/选择剧本版本
  → 提取角色与场景
  → 补齐角色和场景资产
  → AI 制图或 3D 预演生成分镜
  → H3 导演台逐段生成与选 Take
  → 22 帧微参考自动承接下一段
  → 音频/对白/旁白进入同一时间线
  → 成片质检、预演与正式导出
```

### 7.2 MV 支链

```text
音乐资产 → AI MV 分段 → 共享角色/图片 → H3 生成 → 成片交付
```

### 7.3 修改传播规则

| 上游变更 | 下游处理 |
| --- | --- |
| 剧本场景内容变化 | 标记对应分镜、提示词和连续性来源过期 |
| 角色外观变化 | 标记引用该角色的片段；不自动覆盖锁定提示词 |
| 分镜图变化 | 标记以它为首帧/参考的待生成任务 |
| 上游采用 Take 变化 | 紧邻下一段 22 帧参考过期并可一键重建 |
| 配方或参考槽变化 | 实际请求预览和预检结果实时更新 |
| 时间线裁切/变换变化 | 预演和导出读取同一份覆盖数据 |

过期不等于删除。旧快照仍可回放和复现，用户决定是否传播。

---

## 8. 数据模型重构

### 8.1 新的项目聚合

```ts
type StudioProject = {
  id: string;
  meta: ProjectMeta;
  activeScriptId?: string;
  scripts: ScriptDocument[];
  characters: CharacterEntity[];
  scenes: ScenePlan[];
  segments: SegmentPlan[];
  assets: ProjectAssetBinding[];
  promptRevisions: PromptRevision[];
  takes: MediaTake[];
  continuityBridges: ContinuityBridge[];
  sequence: TimelineSequence;
  mvProjects: MVProject[];
  previzScenes: PrevizScene[];
  jobs: JobRecord[];
  schemaVersion: 3;
};
```

### 8.2 需要新增的关键实体

| 实体 | 作用 |
| --- | --- |
| `ScriptDocument` | 正文、结构、版本、来源与状态 |
| `CharacterEntity` | 角色身份、外观组、声音、3D、版本 |
| `SegmentPlan` | 剧情片段与多个 Shot，不再把 Take 嵌套为唯一存储方式 |
| `PromptRevision` | 来源层、模型适配、用户覆盖、最终快照 |
| `MediaTake` | 图片/视频/音频统一版本与派生关系 |
| `ContinuityBridge` | 尾帧、末 22 帧、动作片段、文字状态和失效信息 |
| `TimelineSequence` | 预演和导出共享的多轨事实 |
| `MVProject` | 音乐分析、区间、视觉绑定、口型与生成结果 |
| `JobRecord` | 所有生成、提取、分析、渲染任务的状态和恢复信息 |

### 8.3 提示词不再是一段孤立字符串

提示词编译顺序固定为：

```text
项目创作简报
→ 当前剧本与场景事实
→ 本段实际出场角色快照
→ Shot 时间段与摄影语言
→ 图片/视频/音频参考编号
→ 连续性桥接状态
→ 项目 Skill 与模型适配器
→ 用户修订/最终锁定
→ 发送快照
```

UI 必须同时提供：导演语言、结构化来源、实际请求、与上次生成差异。最终锁定稿不会被角色同步或 AI 导演静默改写。

---

## 9. 后端与文件处理

### 9.1 项目目录

桌面端每个项目使用真实目录：

```text
项目名.momoproject/
  project.momo.json
  scripts/
  characters/
  assets/
    images/
    videos/
    audio/
    microrefs/
    proxies/
  renders/
  cache/
  templates.json
  manifest.json
```

AppData 只保存索引、窗口状态和最近项目，不再把完整项目仅锁在内部 JSON 中。

### 9.2 媒体服务

Rust/Tauri 侧统一提供：

- `probe_media`：时长、fps、分辨率、音轨和损坏检查；
- `extract_thumbnail`：分镜缩略图和代理图；
- `extract_micro_reference`：稳定区间末 22 帧视频；
- `extract_bridge_frame`：稳定尾帧；
- `trim_media`：入出点和动作参考；
- `render_sequence`：裁切、变换、转场、混音、字幕、H.264 MP4；
- `cancel_media_job`：安全取消，不覆盖已有成片；
- 内容指纹去重和原子写入。

### 9.3 生成适配层

```text
Studio Job
  ├─ ComfyUI Adapter
  ├─ Provider Image Adapter
  ├─ Provider Video Adapter
  ├─ LLM / Agent Adapter
  ├─ TTS / ASR Adapter
  └─ Local Media Adapter
```

界面只选择创作模式和能力，不直接绑定某个厂商页面。配方继续作为能力快照，预检负责确认真实素材容量、模型支持和费用风险。

### 9.4 任务中心

所有耗时操作进入统一任务中心：

- `planned → prechecking → queued → running → reviewing → done`；
- `cancelled / failed / blocked / stale` 为分支状态；
- 支持跨重启恢复尚未提交或可轮询的任务；
- 单段失败不阻断其他不相关片段；
- 远程任务提交后明确提示无法撤销计费；
- 自动质检可以运行，自动重生成必须再次经过用户授权。

---

## 10. 自动化程度

### 10.1 默认自动完成

- 剧本结构识别和角色/场景候选提取；
- 参考槽编号与配方容量匹配；
- 生成前预检；
- 上一段采用后提取尾帧和末 22 帧；
- Take 完成后生成缩略图、媒体探测和质量报告；
- 采用版本进入派生时间线；
- 对白生成字幕草稿；
- 项目自动保存和任务状态恢复。

### 10.2 必须由用户确认

- AI 对剧本、角色或锁定提示词的结构性修改；
- 远程付费生成与批量重生成；
- 替换已采用 Take；
- 跨片段传播角色/提示词更新；
- 删除素材、版本或项目；
- 正式导出覆盖同名文件。

### 10.3 明确不自动做

- 不因为质检失败就自行花钱重跑；
- 不跨过缺片把非相邻片段自动接力；
- 不把 3D 标注、箭头和空间辅助线生成到成片；
- 不用快速模式隐藏真实执行结果；
- 不在多个页面维持同一参数的不同副本。

---

## 11. 当前能力的保留、重用与退役

### 11.1 直接保留并接入新壳

- 三态剧本导入与 H3 成品提示词直录；
- Skill 绑定、精炼和快照；
- 双语资产册与参考槽顺序；
- ComfyUI 模板、前端/API 格式转换和往返编辑；
- Provider 与模型卡适配；
- `runBatch`、软取消、硬停止、显存清理；
- Take、采用版本、派生版本；
- 尾帧/末尾动作接力；
- FFmpeg 正式渲染、字幕、音频混合；
- 3D 场景和站位图；
- `.momoproject` 导入导出。

### 11.2 需要补完

- 真正的 22 帧微视频提取和投喂；
- 剧本库与角色库的一级实体和版本；
- AI 制图完整工位；
- AI MV 项目、音乐分析、图片区间与口型支链；
- 外部 Agent 受控动作网关；
- 镜像/旋转/适配等变换进入预演与 FFmpeg；
- 全局任务中心和模块间过期传播；
- 统一项目目录与媒体代理。

### 11.3 退役但迁移数据

- `DirectorWorkspaceKey = planning/directing/post/threed` 的四阶段导航；
- 全局 `workspaceMode = quick/pro`；
- `cockpitView = cockpit/detail`；
- ScriptPage 的整页大文本框壳；
- EditingPage 的纵向卡片仪表盘；
- 独立的旧 StoryboardPage / GenerationPage 入口；
- 同一配方在顶栏、检查器和详细视图中的重复控件。

退役是 UI 退役，不删除用户项目数据。v2 项目升级到 v3 后仍能回放所有已有 Take 和导出资产。

---

## 12. 代码结构建议

### 12.1 UI

```text
src/modules/studio/
  StudioShell.tsx
  studio.css
  nav/
  shared/
    DockPanel.tsx
    StageSurface.tsx
    ObjectBreadcrumb.tsx
    JobStrip.tsx
    VersionDiff.tsx
  director/
  scripts/
  characters/
  image/
  h3/
  mv/
  post/
  previz/
```

不建议继续向 `DirectorStudio.tsx`、`director.css` 和 `cockpit.css` 叠加。新壳建立后，按工位迁移能力，旧组件只作为短期实现参考。

### 12.2 Core

```text
src/core/studio/
  types.ts
  migration.ts
  projectStore.ts
  objectIndex.ts
  dependencyGraph.ts
  promptCompiler.ts
  jobOrchestrator.ts
  agentGateway.ts
  mvEngine.ts
  continuityBridge.ts
  timeline.ts
```

执行层可以复用现有 `directorQueue.ts`、`directorRefs.ts`、`directorRender.ts` 等，但 UI 不再直接知道其内部文件结构。

### 12.3 迁移顺序

`DirectorProject v2 → StudioProject v3` 映射：

- `script` → 首个 `ScriptDocument`；
- `characters` → `CharacterEntity[]`；
- `scenes[].segments[]` → 扁平对象索引 + 保留场景顺序；
- `segment.takes` → `MediaTake[]`；
- `globalSlots / slots` → 项目和片段资产绑定；
- `continuityCapsules` → `ContinuityBridge[]`；
- `postTimeline / audioTracks` → `TimelineSequence`；
- `threedEntities` → 默认 `PrevizScene`；
- `workspaceMode / cockpitView` 不迁移到产品状态，只保存在迁移日志中。

---

## 13. 分阶段实施

### R0：冻结 2.0 UI 扩展

- 不再往四工作区和旧详细视图增加入口；
- 为现有项目补完整迁移测试样本；
- 建立功能覆盖清单和截图基准。

### R1：Studio Shell 与主题系统

- 新一级导航、顶栏、停靠面板和 StageSurface；
- 三主题语义令牌；
- 新 H3 工位先只读接入现有数据；
- 3D 迁入统一外壳。

验收：四张现状截图对应页面不再出现卡片墙、白黑孤岛和双入口。

### R2：H3 核心工位

- 片段树、监看器、Take、检查器和音频条；
- 生成、采用、批量、停止全部接回现有队列；
- 实现 22 帧微参考闭环；
- 旧驾驶舱和详细视图从正式入口移除。

### R3：AI 导演、剧本库、角色库

- 对话/方案/大纲布局；
- 剧本与角色版本；
- 差异确认和依赖失效；
- 外部 Agent 提案协议第一版。

### R4：AI 制图与资产闭环

- 三种制图模式；
- 本地/远程引擎统一；
- 结果入角色、场景、片段和资产库；
- 生成历史与参数复用。

### R5：AI MV

- 音乐探测、波形、段落、图片区间；
- 分段生成与替换；
- 歌词字幕；
- 可选口型配方；
- 送往成片。

### R6：成片交付重构

- 删除卡片仪表盘，重建停靠式后期工位；
- 镜像、旋转、适配、转场、音频和字幕统一预演；
- 质量问题定位播放头；
- 正式 MP4 与交付包验收。

### R7：项目目录、迁移与清理

- v2→v3 迁移；
- 项目目录和代理媒体；
- 删除旧 UI 与无入口样式；
- 完成全链路性能和恢复测试。

---

## 14. 视频可见功能覆盖验收矩阵

| # | 功能 | 当前 2.0 | 3.0 完成条件 |
| --- | --- | --- | --- |
| 1 | 分镜预览 | 部分具备 | 所有片段都有明确预览顺位和空态 |
| 2 | 主导演台三栏 + 下方媒体条 | 形似但割裂 | 左中右与播放头同步，Stage 连续 |
| 3 | 提示词独立编辑/同步 | 部分具备 | 修订版本、影响范围、差异应用完整 |
| 4 | 角色独立编辑/同步 | 不完整 | 角色版本 + 受影响片段 + 选择性传播 |
| 5 | 22 帧微视频参考 | 仅预留字段 | 真提取、预览、绑定、失效、重建、投喂 |
| 6 | 自定义音频位置 | 有音轨但入口分散 | 分段波形可编辑且预演/导出一致 |
| 7 | 镜像等基础变换 | 不完整 | 时间线变换真实进入预演和 MP4 |
| 8 | 视频与音频叠加 | 已有后端基础 | 同一时间线所见即所得 |
| 9 | 整条分镜链预演 | 有但割裂 | H3 工位快速预演 + 成片工位正式预演 |
| 10 | LLM API AI 导演 | 抽屉版 | 一级工位、项目上下文、方案与差异确认 |
| 11 | 外部 Codex/Agent 协作 | 未产品化 | 受控提案协议可读写草案 |
| 12 | 草稿保存到剧本库 | 无正式库 | 新建/另存/合并三种动作走通 |
| 13 | 剧本二次修改 | 大文本框 | 正式版本、差异、回退与项目传播 |
| 14 | 图片集中保存 | 已有资产库 | AI 制图结果带来源直接入库并可反向定位 |
| 15 | 文生图/图生图/编辑图 | 分散在画布节点 | 独立 AI 制图工位完整覆盖 |
| 16 | 本地 ComfyUI | 已有 | 在 AI 制图/H3 中以统一引擎呈现 |
| 17 | 火山 Seedream API | 可通过 Provider 配置 | 制图工位可直接选择并预检 |
| 18 | 量化/显存档位 | 快速模式表达不稳定 | 模型预设真实能力显示，不做全局模式 |
| 19 | AI MV 音乐关联多图 | 仅快速模式名称 | 独立数据、时间线、生成、替换、导出 |
| 20 | MV 人物口型方向 | 无完整链路 | 配方可用时走通，缺失时明确不可用 |
| 21 | 完整长片播放与输出 | 已有基础 | 真实裁切/声音/字幕/变换进入正式 MP4 |

只有 21 项全部通过桌面端真实媒体验收，才可以对外称为“视频可见功能 100% 覆盖”。

---

## 15. 关键验收场景

### 场景 A：从故事到长视频

1. AI 导演生成故事与角色；
2. 保存为剧本库新版本；
3. 送入项目并提取角色/场景；
4. AI 制图补齐角色和场景图；
5. H3 导演台生成第 1 段并采用 Take；
6. 自动提取末 22 帧，下一段检查器显示真实来源；
7. 连续生成 5 段并预演；
8. 成片工位调整声音、字幕、镜像和转场；
9. 导出 MP4，成片与预演一致。

### 场景 B：角色更新但不破坏锁定提示词

1. 修改角色服装；
2. 所有引用片段出现“角色来源已更新”；
3. 未手改片段可批量同步；
4. 锁定最终稿只显示差异，不自动覆盖；
5. 旧 Take 仍保留原角色快照并可复现。

### 场景 C：AI MV

1. 导入歌曲；
2. 自动探测时长、波形和段落；
3. 绑定 6 张图片到不同区间；
4. 只生成所选区间并替换失败段；
5. 可选口型配方只作用于人物区间；
6. 歌词、音乐和视频一起送往成片并导出。

### 场景 D：三主题一致性

同一项目依次切换云白、深空蓝、深邃黑：

- 导航、面板、文字、边界和交互状态完整映射；
- 监看器、时间线和 3D 始终是连续暗色 Stage；
- 不出现蓝色写死光晕、白色突兀检查器或不可读灰字；
- 布局和控件尺寸不随主题改变。

---

## 16. 性能与可用性底线

- 50 个片段以上的树、Take 条和资产列表必须虚拟化；
- 大图继续使用 `Thumb`，播放器使用代理媒体优先；
- AI 制图、H3、3D 和后期模块按工位懒加载；
- 拖动播放头与波形保持 60fps，重计算移出主线程；
- 面板状态、选中对象、滚动位置和播放头跨工位保持；
- 所有图标按钮有中文 `title`、`aria-label`、可见焦点和快捷键替代；
- 错误必须说明原因、影响对象和恢复动作；
- 任务超过 300ms 显示阶段，超过 1s 显示进度或不可估计状态；
- 所有删除、覆盖、付费生成和大范围同步提供确认与撤销/版本恢复。

---

## 17. 实施决策

这次重构必须遵守三个硬决定：

1. **不再围绕旧四页改。** 新建 Studio Shell 和工位级路由，旧 UI 只作为迁移参考。
2. **不再保留两套入口。** 快速/专业、驾驶舱/详细视图、旧分镜/生成页全部退役；复杂度通过检查器渐进展开解决。
3. **先做 H3 核心链，再做外围模块。** 第一批必须完成统一外壳、三主题、主导演台和 22 帧微参考；否则继续加 AI MV 或剧本库只会再次产生割裂。

最终目标不是把 MOMO 变成参考软件的皮肤复刻，而是复刻它正确的产品组织方式，完整覆盖视频功能，同时保留 MOMO 在通用模型、ComfyUI、Skill、项目包、3D 和可追溯生成方面的优势。
