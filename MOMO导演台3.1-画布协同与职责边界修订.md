# MOMO 导演台 3.1：画布协同与职责边界修订

> ⚠️ **纠偏通知（2026-08-29）**  
> 本文正确识别了工位之间的底层重复，但将 AI 导演、剧本库、角色库和 AI 制图的独立业务职能收缩过度。  
> 后续以《[MOMO 导演台 3.2：智能职能深化与通用能力复用方案](./MOMO导演台3.2-智能职能深化与通用能力复用方案.md)》为准；本文仅保留为第一轮重复职责审计记录。

> 文档状态：3.0 架构纠偏基线  
> 规划日期：2026-08-29  
> 核心目标：保留参考软件的完整制片链，但不在导演台里重做一套 MOMO 画布  
> 决策原则：能连接就不复制，能引用就不搬运，能做投影视图就不新建数据源

---

## 0. 结论先行

用户的判断是正确的：导演台 3.0 已经从“画布上的制片编排器”逐步变成“MOMO 里的第二套 MOMO”。

当前的主要问题不是功能太多，而是同一职责同时出现了两个主管：

- 角色在画布的角色卡中可以完整分析、生成和向下游传递，导演台又新建了角色档案、服装、参考图和声音系统；
- 画布已经有成熟的生图节点、ComfyUI 节点、模型参数和流程编排，导演台又新建了 AI 制图引擎和生成历史；
- 画布已经有创作助手、Agent、语音、联网、附件和生成确认闸，导演台又新建了 AI 导演对话；
- 画布已经用资产库、节点运行器、错误中心和运行日志承载生成过程，导演台又引入了一套局部历史和任务中心；
- AI MV 中的音乐分析和区间编排有独特价值，但它的生图、生视频、素材导入、配方执行和导出又和画布重复。

正确的产品关系应该是：

```text
MOMO 画布 = 能力层（创作、生成、处理、流程）
MOMO 资产库 = 统一素材真相层
MOMO 创作助手 = 统一 AI 交互层
MOMO 导演台 = 制片编排与审片层
```

导演台只应拥有“项目顺序、片段上下文、连续性、批量调度、Take 审批、时间线和交付”。角色生成、图片生成、通用 AI 对话、资产管理、ComfyUI 管理、Skill 管理继续由 MOMO 的现有全局模块主管。

---

## 1. 这次审计看到的实际情况

### 1.1 3.0 当前不是“方案重复”，而是已经开始形成两套实现

3.0 已建立七个一级工位：

1. AI 导演
2. 剧本库
3. 角色库
4. AI 制图
5. H3 导演台
6. AI MV
7. 成片交付

其中 AI 导演、剧本库、角色库、AI 制图、AI MV 与其专用核心层合计已约 2400 行代码，还不包含 CSS、数据迁移和类型扩展。这说明冲突已经进入维护成本层：今后修模型选择、资产入库、任务取消、错误上报或提示词时，很可能要修两次。

### 1.2 冲突不只是页面像，而是四层双轨

| 冲突层 | 表现 | 后果 |
| --- | --- | --- |
| 入口冲突 | 用户既可在画布创作，又可在导演台创作同类内容 | 不知道应该去哪里做，也不知道哪边是最新版 |
| 数据冲突 | `CharCardData` 与 `DirectorCharacter`、节点结果与 `ImageStudioRecord`同时记录相似事实 | 修改、版本、删除和回滚可能不同步 |
| 执行冲突 | 节点 `runner` 与 `imageStudio` / `mvEngine` 各自调用生成服务 | 参数兼容、确认闸、错误上报、中断语义不一致 |
| 状态冲突 | 节点状态、标题栏运行日志、错误中心与 3.0 `JobCenter` 分开 | 任务在一处运行，在另一处可能看不到 |

---

## 2. 功能职责重叠矩阵

| 3.0 模块 | MOMO 已有主管 | 重叠程度 | 真正有价值的增量 | 3.1 决策 |
| --- | --- | --- | --- | --- |
| AI 导演 | 右侧「创作助手」：聊天 / Agent / 联网 / 附图 / 语音 / 模型选择 / 生成确认 | 极高 | 项目、场景、当前片段上下文；结构化提案回写 | 删除独立对话工位；给现有创作助手增加「导演上下文」模式 |
| 剧本库 | 提示词节点、分镜节点、导演台现有三态导入与拆分 | 中高 | 项目剧本修订、差异预览、重拆前影响分析 | 取消「项目内多文档资源库」；保留轻量「项目剧本与修订」，并支持绑定画布文本/分镜节点 |
| 角色库 | 角色库预设 + 成熟角色卡节点 + 资产库 | 极高 | 角色在当前项目的出场片段、连续性约束、来源过期提示 | 删除角色编辑与生成；改成「演员表 / 角色绑定」投影视图 |
| AI 制图 | 生图节点、ComfyUI 节点、图像编辑节点、资产库、节点历史 | 极高 | “所得结果绑定到当前片段”这一个上下文动作 | 删除独立制图工位和专用执行层；用「去画布制作 / 替换此参考」桥接 |
| H3 导演台 | 画布只有通用节点，没有片段顺序、上下文接力、Take 审片 | 低 | 整个模块都是核心增量 | 保留为导演台主工作区 |
| AI MV | 音频/生图/生视频/ComfyUI/配音节点已有，但没有音乐段落编排 | 中高 | 节拍分析、歌词时轴、视觉区间和音乐映射 | 保留为「MV 编排视图」，生成改由画布节点或共享 runner 执行，不再自建生成引擎 |
| 成片交付 | 画布能生成单素材，但没有完整项目时间线、质检、混音和交付 | 低 | 多轨时间线、质量检查、顺序预演、FFmpeg 交付 | 完整保留 |
| 3D 预演 | 画布无同类编辑器 | 低 | 站位、机位、灯光与空间锁 | 保留，作为项目工具而非必经流程 |
| 资产库 | 全局 AssetLibrary 已成熟 | 若重做则极高 | 项目/片段过滤 | 只打开现有资产库并预置筛选，不新建库 |
| 任务中心 | 节点状态、错误中心、运行日志已存在 | 中 | 跨片段批量进度和渲染阶段 | 暂不建独立全局中心；导演台只显示当前项目的批次进度，错误/请求仍进现有全局中心 |
| ComfyUI | 设置页、模板管理、Comfy 节点已存在 | 高 | 项目默认配方和片段配方绑定 | 仅保留绑定选择和「打开原管理器」 |
| Skill | 全局 SkillManager 已存在 | 高 | 项目和片段的 Skill 绑定与路由 | 只保留绑定卡和执行快照，不在导演台编辑 Skill |

---

## 3. 新的职责边界

### 3.1 画布主管“怎么做出一个素材”

以下能力只能有一个主实现，且主实现应在画布或全局模块：

- 角色设定的生成与物料产出；
- 文生图、图生图、图片编辑、生视频、音频生成；
- 模型选择、通用生成参数、供应商适配、ComfyUI 工作流；
- 节点依赖、串并行运行、结果回写、下游传递；
- 通用 AI 对话、Agent、联网、语音、生成确认；
- 资产导入、分组、预览、版本谱系、Eagle 同步；
- 通用 Skill、模板、设置和运行日志。

### 3.2 导演台主管“这些素材怎样组成一条片”

导演台的独有职责是：

- 项目剧本与片段顺序；
- 场景、片段、镜头和 Take 的层级；
- 当前片段的提示词、参考槽、配方和续接上下文；
- 22 帧/微视频参考、尾帧接力、空间连续性；
- 跨片段批量生成的排队、停止、失败隔离和进度；
- 多 Take 对比、采用、取消采用与派生处理；
- 连续性检查、缺片检查、质量探测；
- 时间线、字幕、混音、预演和正式交付；
- 与当前项目有关的 3D 站位和机位预演；
- MV 项目中的音乐分析、歌词轴和视觉区间编排。

### 3.3 导演台可以“管绑定”，但不能“抢编辑权”

例如角色：

- 画布角色卡主管角色的档案、提示词、三视图、表情、服装、立绘和设计卡；
- 导演台只管这个角色出现在哪些片段、当前项目绑定了哪张角色卡、哪些参考图在本项目生效，以及源角色卡更新后哪些片段需要复核。

这是“引用”，不是“复制”。

---

## 4. 3.1 信息架构：从七工位收缩为三主区 + 两工具

### 4.1 默认一级区域

| 区域 | 职责 | 说明 |
| --- | --- | --- |
| H3 制作 | 片段列表、监看、提示词、参考、配方、生成、Take | 默认首页与主工作区 |
| MV 编排 | 音乐分析、歌词、视觉区间、区间结果 | 仅在项目开启 MV 模式时显示；不自建生成层 |
| 成片交付 | 问题、监看、时间线、混音、字幕、导出 | 保留专业后期工作区 |

### 4.2 项目级工具

| 工具 | 进入方式 | 为什么不做一级工位 |
| --- | --- | --- |
| 项目结构 | 项目名旁按钮或 H3 左栏顶部 | 只承载剧本导入/修订、场景与片段、演员绑定，不需要长期占导航 |
| 3D 预演 | H3 片段工具或项目工具区 | 它是空间辅助器，不是每个项目都必经的一级流程 |

### 4.3 不再作为导演台一级导航的内容

- AI 导演：改为打开现有创作助手的导演上下文；
- 剧本库：改为项目结构中的轻量剧本修订；
- 角色库：改为项目结构中的演员绑定；
- AI 制图：改为去画布制作和绑定结果的上下文命令；
- 资产库、ComfyUI、Skill 管理：打开现有全局模块；
- 任务中心：不做导演台内的第二个全局任务系统。

---

## 5. 画布与导演台的正确衔接

### 5.1 衔接不应是“导出再导入”

两者共用同一个本地应用，所以应该用稳定 ID 和引用关系直接衔接：

```text
画布节点输出
   └─ 资产入库（唯一 AssetItem）
         └─ 导演项目绑定 assetId
               └─ 片段参考槽 / 角色 / Take / 时间线
```

导演台不应把 data URL、参数和生成历史再拷贝一份到项目中。

### 5.2 需要的五个轻量桥接器

#### A. `CharacterBinding`：角色绑定

建议项目角色只保留：

```ts
type DirectorCharacterBinding = {
  id: string;
  name: string;
  sourceBoardId?: string;
  sourceNodeId?: string;       // 绑定 charCard 节点
  sourceRevision?: string;     // 源数据指纹
  assetIds: string[];          // 本项目实际采用的参考
  continuityOverride?: string; // 仅容纳本项目例外
};
```

不再在 `DirectorCharacter` 里继续扩展身份、外貌、服装组、分类参考图、声音和 TTS 。这些属于角色卡或后续全局角色系统。

#### B. `NodeTaskBridge`：画布生成任务桥

导演台中点「制作参考图」时：

1. 记录返回上下文 `{ projectId, segmentId, slotSemantic }`；
2. 回到画布并选中已绑定的生图/ComfyUI 节点；
3. 若未绑定，则在导演节点旁创建一个标准生成支链，而不是打开另一个生图页；
4. 节点生成成功并收录资产后，提供「绑定到片段并返回导演台」；
5. 导演台回到原片段和原参考槽。

这里只新增跳转和回传机制，不新增生成引擎。

#### C. `AgentContextBridge`：创作助手上下文

创作助手新增一个上下文条，例如：

```text
正在协作：《雨夜站台》 / 场景 02 / 片段 05
[查看上下文] [退出导演协作]
```

它读取当前剧本、角色绑定、片段、连续性和参考槽，但仍使用原 `AgentPanel` 的模型、消息、附件、联网、语音和确认闸。AI 的结构化建议仍通过差异确认写回项目。

#### D. `StoryboardBinding`：剧本/分镜节点绑定

项目结构支持三种来源：

- 直接粘贴/导入剧本；
- 绑定一个画布提示词/文本节点；
- 绑定一个画布分镜节点并导入其镜头结构。

导入后保存源节点 ID 和内容指纹。源节点更新时只提示「来源已更新」并预览差异，不静默覆盖已做好的 Take、提示词和接力资产。

#### E. `ProjectAssetView`：项目资产投影

导演台所谓的“项目素材”必须是全局资产库的筛选结果，而非另一个库：

```text
filter = director.projectId == 当前项目
       OR assetId 被当前项目的 slot/take/character/timeline 引用
```

---

## 6. 关键用户流程

### 6.1 建角色：一次创作，多项目绑定

```text
导演台「演员绑定」
  → 选择已有角色卡
  → 若没有，点「去画布创建角色卡」
  → 在原角色卡上完成分析、提示词、三视图、表情、服装等
  → 选定本项目要用的素材
  → 绑定并回到原项目
  → 导演台显示出场片段和连续性状态
```

角色卡仍是角色主体，导演台是引用方。

### 6.2 做参考图：跳到能力层，带结果回来

```text
片段 05 / 角色参考槽「替换」
  → 打开关联的生图节点支链
  → 节点依旧使用画布的上游图、提示词、模型、ComfyUI 和生成参数
  → 结果依旧进全局资产库
  → 点「使用于片段 05」
  → 回到导演台，新 assetId 写入指定槽位
```

不新增任何模型调用代码。

### 6.3 AI 导演：一个助手，不同上下文

```text
导演台选中片段 05
  → 点「问创作助手」
  → 导演台暂时收起 / 助手侧栏打开
  → 助手自动带入项目和片段上下文
  → 输出剧本、镜头或提示词建议
  → 用户查看差异后采用
  → 回到原片段
```

原有的联网、附图、语音、多模型和生成确认全部保留。

### 6.4 AI MV：保留编排，回收生成职权

```text
导入音乐资产
  → MV 编排视图分析节拍/歌词/段落
  → 为区间绑定角色卡和图片资产
  → 为每个区间创建或引用标准视频生成节点
  → 通过共享 runner 执行
  → 资产结果回填区间 Take
  → 进入共用成片交付时间线
```

MV 不再自己调 `generateImage`、`runComfyTemplate` 或建立另一套生成历史。

---

## 7. 数据真相与迁移

### 7.1 唯一真相表

| 对象 | 唯一真相 | 导演台允许保存的内容 |
| --- | --- | --- |
| 角色创作 | `charCard` 节点及其资产 | 源节点 ID、当前项目采用的资产 ID、项目例外连续性 |
| 图/视/音资产 | `AssetItem` | 资产 ID 引用、槽位语义、Take 关系 |
| 生成配置 | 画布节点数据 + 模型/模板设置 | 运行时快照，用于 Take 可追溯，不作可编辑副本 |
| AI 会话 | `agentStore` | 项目上下文引用与已采用的提案快照 |
| Skill | `skillStore` | binding + 运行快照 |
| ComfyUI | `comfyStore` / 配方 | 项目/片段 recipeId |
| 剧本 | 项目当前 `script` 或已绑定源节点 | 项目修订快照、源 ID、内容指纹 |
| 项目制片结构 | `DirectorProject` | 场景、片段、参考槽、Take、连续性、时间线和交付 |

### 7.2 3.0 已有数据不能直接丢弃

收缩 UI 时采用可逆迁移：

- `project.scripts`：将当前 active 版转成项目剧本修订；其他文档导出为 Markdown 或保留在 legacy 区，不再从主 UI 继续编辑；
- `DirectorCharacter` 扩展字段：先生成不可变迁移快照，如能匹配角色卡则建立绑定，否则当作“未绑定的项目角色快照”；
- `project.imageStudio.history`：不删除已有资产；记录转成资产 provenance 或 legacy 查看记录，新版不再写入；
- `project.mvProjects`：保留音乐分析、区间和 Take，只替换其执行路由；
- `studioUi.station`：旧 `director/scripts/characters/image` 统一映射到 `h3`，`mv/post/previz` 按原区域恢复。

---

## 8. 建议的代码级取舍

### 8.1 完整保留

- `src/modules/studio/h3/H3Station.tsx`
- `src/modules/studio/post/PostStation.tsx`
- `src/modules/studio/previz/PrevizStation.tsx`
- `src/core/studio/microRef.ts`
- `src/core/studio/objectIndex.ts`
- `src/core/studio/skillRoute.ts`
- 现有 `directorQueue` / `directorRefs` / `directorPrompt` / `directorTimeline` / `directorRender` 主链
- 现有 AssetLibrary、AgentPanel、CharCardNode、ImageGenNode、ComfyNode、SkillManager

### 8.2 降级为轻量视图或桥接

- `CharacterLibraryStation` → `ProjectCastPanel`：只显示绑定、出场片段、过期状态；
- `ScriptLibraryStation` → `ProjectStructurePanel`：只保留当前剧本、修订和导入/绑定；
- `AIDirectorStation` 中的上下文编译与提案应用 → `AgentContextBridge`；
- `MVStation` → 只保留音乐分析、歌词、区间编排和结果检视；
- `JobCenter` → 项目批次进度投影，不作为第二个全局日志/错误系统。

### 8.3 停止继续扩展，完成迁移后删除

- `src/modules/studio/image/ImageStudioStation.tsx`
- `src/core/studio/imageStudio.ts`
- `ImageStudioRecord` 及 `project.imageStudio` 的新写入路径
- AI 导演的独立会话状态和直接 `chatOnce` 调用
- 角色工位对 identity / appearance / outfits / refViews / voice / TTS 的编辑权
- MV 中对生图/生视频服务的直调路径

---

## 9. 最小实施路线（防止再次过度开发）

### P0：先停止双轨扩张

1. 暂停为 AI 导演、剧本库、角色库、AI 制图添加新功能。
2. 明确唯一真相和迁移口径。
3. 将 3.0 文档中“一壳七工位”标记为已废弃架构。

### P1：收导航，不动核心生成链

1. `StudioShell` 只保留 H3、MV（按需）、成片、3D。
2. 增加项目结构弹层，承接剧本和演员绑定。
3. 资产、ComfyUI、Skill 仍打开原有全局页。
4. 任务条只显示当前批次，不新增任务管理功能。

### P2：做三个最小桥接

1. 角色卡绑定与「在画布打开」。
2. 参考槽的「去画布制作 → 绑定返回」。
3. 创作助手的导演上下文条与提案回写。

此阶段不做新节点类型，不做新生成服务，不做新资产库。

### P3：收回 MV 的执行职权

1. 保留音乐分析与区间数据。
2. 区间生成改用共享 runner / 画布节点。
3. 区间 Take 与 H3 Take 进入同一交付时间线。

### P4：清理 legacy UI，不清理用户结果

1. 执行数据迁移并保留只读兼容。
2. 删除无入口的重复 UI 和专用执行层。
3. 所有已生成资产、Take、时间线和项目修订必须可追溯。

---

## 10. 明确不做的事

这一节是防止项目再次膨胀的红线：

- 不在导演台再造一套可独立生成角色整套素材的角色系统；
- 不在导演台再造文生图/图生图/编辑图引擎；
- 不为 AI 导演再维护一份消息史、模型选择和附件系统；
- 不在项目里创建第二个资产库、第二个 Skill 管理器或第二个 ComfyUI 管理器；
- 不为导演台的普通生成再写一套 provider 兼容逻辑；
- 不用同步复制代替引用；
- 不为了形式上像参考软件，强行保留七个一级工位。

---

## 11. 功能覆盖不会因收缩工位而丢失

“参考软件功能 100% 覆盖”不等于“在导演台内部复刻它的每一个页面”。收缩后的覆盖路径是：

| 参考软件功能 | MOMO 3.1 实现位置 |
| --- | --- |
| AI 导演接 LLM / 外部 Agent | 现有创作助手 + 导演上下文 + 差异提案网关 |
| 剧本草稿、导入、修改、导入项目 | 项目结构 + 画布文本/分镜节点绑定 |
| 角色设定、参考图和影响片段 | 画布角色卡 + 导演台演员绑定/反向索引 |
| AI 制图的本地/远程引擎与参数 | 画布生图/ComfyUI 节点 + 片段绑定返回 |
| H3 分镜、22 帧微参考、参考槽、Take | H3 制作主工作区 |
| 音乐轨、多图映射、MV、口型支链 | MV 编排 + 画布音视频/ComfyUI 执行 |
| 时间线、音频、字幕、质检和正式导出 | 成片交付 |
| 站位、机位、灯光 | 3D 预演 |

功能没有被删掉，只是回到正确的主管模块。

---

## 12. 验收标准

### 12.1 边界验收

- 同一个角色不需要在画布和导演台分别维护两套档案；
- 同一次生图只走一套模型解析、确认、错误上报、结果入库和任务取消逻辑；
- AI 导演与创作助手不再保存两份独立会话和模型状态；
- 导演台的资产全部可在全局资产库找到，删除/替换有引用提示；
- H3、MV 和成片的错误都进现有错误中心，实际请求都进现有运行日志。

### 12.2 衔接验收

- 从导演台点「编辑角色」能定位到真实角色卡；
- 从导演台点「制作参考图」能定位/创建画布生成支链；
- 生成结果能一次操作绑定回原片段和原槽位；
- 来回画布时，导演台的项目、工作区、场景、片段和检查器状态不丢；
- 创作助手能看到当前项目/片段上下文，写回前有差异确认；
- 源角色卡或源剧本节点更新时，导演台只提示过期和差异，不静默破坏已有片段。

### 12.3 过度开发防线

任何导演台新功能开发前都必须回答三个问题：

1. 画布或全局模块是否已有同类能力？
2. 能否通过绑定、投影、预置过滤或上下文命令复用？
3. 新数据是必须的制片状态，还是已有数据的第二份副本？

只有“画布没有、桥接无法解决、且属于制片独有状态”时，才允许导演台新建实现。

---

## 13. 最终产品定义

MOMO 不需要两个并排的创作系统。

它需要的是一套画布能力，加一个能把这些能力组织成长链路的导演台：

```text
在画布上创作角色、图片、视频、音频与工作流
                         ↓ 引用，不复制
在导演台中组织剧本、片段、连续性、Take 与时间线
                         ↓
在成片交付中检查、混音、字幕、预演和导出
```

这个边界既能覆盖参考视频中的完整功能，也能保留 MOMO 画布的通用性和已经成熟的角色卡/生成流程，而不需要继续扩建第二套功能。
