# MOMO 导演台 2.0：专业驾驶舱改造方案

> **已废弃：** 本方案形成的“四工作区 + 快速/专业 + 旧详细视图”结构已被实际使用反馈证明会造成入口重复、视觉割裂和功能分散。后续改造统一以《MOMO导演台3.0-全链路制片工作站重构方案.md》为准；本文件仅保留为 2.0 实施历史，不再作为新增功能依据。

> 文档状态：产品设计与技术改造基线  
> 编写日期：2026-08-28  
> 适用项目：MOMO 智能画布  
> 关联文档：《MOMO导演台节点-产品与技术方案.md》  
> 竞品参考：杨墨川《MiniMax H3 导演台巨大优化升级》演示  
> 核心原则：吸收竞品的单屏导演体验，但不牺牲 MOMO 的模型无关、ComfyUI 可扩展、资产可追溯与画布互通能力

---

## 1. 改造结论

MOMO 导演台现有底层能力已经覆盖剧本拆分、分镜、H3 提示词、参考素材、ComfyUI、批量生成、Take 管理、连续性接力、高清放大、音频、交付导出和 3D 站位。当前主要问题不是功能数量不足，而是这些能力以“阶段页 + 设置卡片”的方式呈现，用户很难在同一视野中持续看到：

- 当前正在处理哪个片段；
- 当前采用的是哪个版本；
- 修改了什么内容；
- 最终会发送什么提示词；
- 上一段如何接入下一段；
- 视频、声音、字幕和标题在成片时间上的关系；
- 批量任务跑到哪里、失败后从哪里继续。

导演台 2.0 应从“多功能生产表单”改造成“内容始终居中的专业导演驾驶舱”。

改造后的产品结构采用两层体验：

1. **快速导演模式**：面向 H3、本地 ComfyUI 和常见远程视频模型，隐藏工作流技术细节，通过推荐配方、硬件档位和自动接力快速完成作品。
2. **专业生产模式**：保留 MOMO 当前完整的配方、Skill、素材槽、参数、Take、质量检查和交付能力，支持高级用户精确控制。

两种模式必须共享同一份 `DirectorProject`、同一套生成队列和同一套资产，不允许复制两套数据模型。

产品定位调整为：

> MOMO 导演台不是 Premiere 或 DaVinci 的替代品，但必须成为一套具备导演监看、镜头编排、轻量时间线、声音组织和可靠交付能力的 AI 视频制作驾驶舱。

---

## 2. 改造目标

### 2.1 产品目标

- 用户进入导演台后，默认看到作品，而不是参数卡片。
- 用户选择任意片段后，画面、提示词、参考素材、生成配方、版本和连续性状态同时更新。
- 用户无需切换页面即可完成“选片段 → 看画面 → 改内容 → 改提示词 → 调参考 → 生成 → 选 Take”。
- H3 用户无需理解 ComfyUI 节点、模板和语义槽，也能完成本地长视频生产。
- 专业用户仍可查看真实提示词、配方参数、Skill 快照、素材编号和工作流信息。
- 后期阶段可以完成硬切、入出点、声音混合、字幕、标题和标准 MP4 输出。
- 项目可以整体迁移、备份、恢复和重新链接素材。

### 2.2 使用体验目标

- 从进入导演台到看到第一个片段预览，不超过 3 次主要操作。
- 从修改当前片段到发起重新生成，不需要切换顶层页面。
- 从成片检查到导出 MP4，不需要借助画布视频拼接节点。
- 50～100 个片段的项目仍能保持顺滑滚动和选择反馈。
- 所有自动生成和计费操作都必须在执行前展示范围、模型、数量和费用风险。

### 2.3 非目标

- 不实现复杂关键帧动画、专业调色、蒙版跟踪、插件特效和多机位剪辑。
- 不在 MOMO 内复制完整 ComfyUI 节点编辑器。
- 不把 H3 的字段、时长、参考数量写死为导演项目的唯一标准。
- 不用全自动 Agent 替代用户的选片与最终确认。
- 不删除现有详细分镜、配方管理、成片检查和 3D 能力。

---

## 3. 竞品优点与 MOMO 吸收策略

| 竞品优点 | 用户感知价值 | MOMO 吸收方式 | 不照搬的部分 |
| --- | --- | --- | --- |
| 左侧镜头列表、中央监看器、右侧检查器 | 始终知道自己在处理哪个镜头 | 新增专业驾驶舱，合并分镜与生成的核心操作 | 不把全部功能挤入一个无层级页面 |
| 监看器下方持续显示镜头节奏和音频 | 作品的时间关系一眼可见 | 增加常驻 Sequence Dock 和轻量时间线 | 不首版实现专业 NLE 全功能 |
| 提示词与角色页面可独立同步 | 修改后的影响范围清楚 | 增加内容版本、同步状态、差异预览和“标记已修改” | 不允许静默覆盖用户锁定的最终稿 |
| 每段有微视频参考用于长视频连续生成 | 连续性操作可见、可理解 | 把现有尾帧/末尾视频接力升级为“上下文胶囊” | 不把固定 22 帧写死到所有模型 |
| AI 导演与剧本库是一等入口 | AI 能参与具体制作环节 | 在导演台内增加项目上下文助手 | 不允许 Agent 未确认直接批量扣费生成 |
| 本地模型参数被包装成硬件档位 | 普通用户无需理解工作流 | 增加 H3 快速模式、显存档位和推荐预设 | 不取消高级配方与模板编辑 |
| 图片、MV、视频模块之间内容直接流转 | 创作上下文不断裂 | 统一资产库、角色库、画布和导演项目引用 | 不复制多套孤立素材库 |
| 画面与声音叠加结果在主界面预览 | 更接近真正成片 | 增加真实音频混合、字幕/标题和 MP4 输出 | 不继续把 MediaRecorder 当正式交付引擎 |

---

## 4. 信息架构重组

### 4.1 顶层工作区

现有五页调整为四个工作区：

1. **策划**：剧本导入、AI 导演、角色与场景、项目规则、拆分与精读。
2. **导演**：分镜、监看、参考素材、提示词、生成、Take 和连续性接力。作为默认工作区。
3. **成片**：故事顺序、轻量时间线、声音、字幕、标题、质量检查、放大与交付。
4. **3D**：站位、机位、灯光、姿态、深度图和空间规划。

原“分镜”和“生成”不再作为两个彼此割裂的顶层页签，而是在“导演”工作区内形成同屏闭环。

### 4.2 全局层级

```text
MOMO 画布
└─ 导演台项目
   ├─ 策划工作区
   ├─ 导演工作区
   ├─ 成片工作区
   ├─ 3D 工作区
   ├─ 项目级创作助手
   ├─ 资产与角色库抽屉
   └─ 任务中心 / 报错中心
```

### 4.3 导航原则

- 顶层工作区只表示制作阶段，不承载具体参数。
- 当前项目、当前场景、当前片段和当前 Take 是全局选中态。
- 切换工作区后保持当前片段，不重新回到列表顶部。
- 资产库、角色库和创作助手以抽屉或右侧标签进入，不覆盖主监看器。
- 复杂设置进入二级弹层；高频操作常驻，低频操作按需展开。

---

## 5. 专业驾驶舱布局

### 5.1 桌面宽屏布局

```text
┌──────────────────────────────────────────────────────────────────────────┐
│ 项目名  工作区  快速/专业  配方  画幅  分辨率  自动保存  进度  导出      │
├──────┬────────────┬──────────────────────────────┬───────────────────────┤
│ 工作 │ 场景/片段树 │           中央监看器          │ 当前片段检查器          │
│ 区栏 │ 片段状态    │  当前 Take / 前后对比 / 擦洗  │ 内容/提示词/参考/生成    │
│      │ 缩略图      │                              │                       │
├──────┴────────────┴──────────────────────────────┴───────────────────────┤
│ Sequence Dock：故事顺序 / 镜头条 / 对白 / 音效 / 音乐 / 字幕 / 标题     │
├──────────────────────────────────────────────────────────────────────────┤
│ 任务状态：批量进度、当前节点、显存、错误、预计剩余时间、停止/取消       │
└──────────────────────────────────────────────────────────────────────────┘
```

建议尺寸：

- 工作区栏：72px。
- 场景/片段树：240～300px，可折叠。
- 右侧检查器：360～430px，可调整宽度。
- Sequence Dock：默认 180px，可在 120～360px 之间拖动。
- 中央监看器占剩余空间，不设置固定宽高。

### 5.2 中等宽度降级

- 小于 1280px 时，场景树可折叠为缩略图窄栏。
- 右侧检查器改为可切换抽屉，但当前标签状态保持。
- Sequence Dock 仍保留，不退化为完全不可见。
- 小于 980px 时只保证查看和轻量编辑，不保证 3D 与复杂时间线并排工作。

### 5.3 顶栏内容

顶栏只保留真正的全局参数：

- 项目名称与保存状态。
- 策划 / 导演 / 成片 / 3D。
- 快速模式 / 专业模式。
- 当前默认生成配方。
- 画幅、目标像素和目标时长。
- 项目总进度。
- 预演、导出和关闭。

显存清理、尾帧接力、批量范围等操作不再长期占据顶栏，进入任务条或生成检查器。

---

## 6. 场景与片段树改造

### 6.1 列表信息

每个片段行展示：

- 序号和简短标题。
- 计划时长。
- 当前采用版本缩略图。
- 状态：缺片、排队、生成中、待选片、已采用、已修改、失败。
- 连续性状态：无接力、已继承、接力过期、素材不完整。
- 评论/警告数量。

不在列表行显示长提示词、配方详情和完整错误文本。

### 6.2 交互

- 单击选择片段，更新中央监看器和右侧检查器。
- 双击播放该片段。
- Shift/Ctrl 多选，用于批量配方、批量生成和批量标记。
- 支持场景折叠、状态筛选、文本搜索。
- 支持拖动调整故事顺序；若调整会破坏连续性，释放前显示影响范围。
- 右键提供重新生成、采用最新、复制设置、标记锁定、打开资产位置等快捷操作。

### 6.3 虚拟化

- 片段超过 30 个后启用虚拟列表。
- 缩略图统一使用 `Thumb` / `VideoThumb`，不可直接加载原始 dataURL。
- 非当前片段的视频不挂载真实播放器。

---

## 7. 中央监看器

### 7.1 默认展示

- 当前采用 Take；没有采用版本时展示最新完成 Take。
- 没有视频时展示分镜图或参考图合成预览。
- 生成中展示上一版本，并覆盖当前进度，不用空白加载页替换内容。

### 7.2 监看功能

- 播放/暂停、逐帧、前后片段、循环当前片段。
- 当前时间、片段时长、全片时间位置。
- 画面适应、100%、全屏。
- 原始/放大后对比。
- Take A/B 对比。
- 静音、原声、项目混音三种监听模式。
- 安全框、字幕安全区、画幅遮罩。
- 当前片段存在首帧、尾帧、接力帧时可快速切换查看。

### 7.3 快捷键

| 快捷键 | 操作 |
| --- | --- |
| Space | 播放/暂停 |
| J / K / L | 倒放 / 暂停 / 正放 |
| ← / → | 上一帧 / 下一帧 |
| Shift + ← / → | 上一片段 / 下一片段 |
| I / O | 设置入点 / 出点 |
| 1～9 | 切换 Take |
| A | 采用当前 Take |
| R | 重新生成当前片段 |
| P | 打开最终提示词 |

快捷键必须加入全局快捷键设置，允许用户修改。

---

## 8. 当前片段检查器

右侧检查器采用四个一级标签：内容、提示词、参考、生成。

### 8.1 内容

展示和编辑：

- 片段标题、摘要、时长。
- 本段出场角色与道具。
- 对白、旁白和声音事件。
- 镜头列表：起止时间、景别、机位、动作、音频。
- `continuityIn` 与 `continuityOut`。
- 锁定状态和修改历史。

内容修改后不立即覆盖已采用 Take，而是将片段标记为“已修改，待重新生成”。

### 8.2 提示词

提供三个层级：

1. **导演描述**：面向普通用户的场景、动作、摄影机、声音描述。
2. **H3 结构**：六段式结构化展示与编辑。
3. **实际请求**：参考编号、风格、Skill、负向规则全部编译后的最终文本。

必须支持：

- 实际请求只读预览。
- 保存为最终稿。
- 恢复自动编译。
- 比较上一次生成与当前提示词差异。
- 显示哪些内容来自项目规则、Skill、角色连续性、参考素材和用户手工编辑。
- 复制最终提示词。
- 检测 `<Picture N>/<Video N>/<Audio N>` 越界或错序。

### 8.3 参考

参考素材不再只使用 32px 小格。检查器中使用 72～96px 预览卡：

- 首帧、尾帧。
- 角色外观。
- 场景与道具。
- 空间站位图。
- 动作/运镜视频。
- 音色和声音参考。
- 上一段桥接帧和动作片段。

每个素材卡明确显示：

- `Picture/Video/Audio N` 编号。
- 语义用途。
- 自动同步或手动绑定。
- 来源场景、片段或 Take。
- 是否参与本次生成。
- 配方容量不足时是否被裁剪。

拖动重排后立即重新计算编号，并在提示词标签发生变化时显示差异提示。

### 8.4 生成

快速模式只展示：

- 生成模型/配方。
- 画质档位。
- 时长。
- 参考连续性强度。
- 声音模式。
- 生成按钮。

专业模式增加：

- 模板和分支。
- 暴露参数。
- 固定或随机种子。
- 分辨率具体值。
- 参考容量分析。
- 本次真实输入摘要。
- 工作流指纹。
- 单段后处理。

发起生成前展示轻量确认卡：当前片段、模型、引擎、本地/远程、预计任务数、是否计费和预计显存档位。

---

## 9. H3 快速导演模式

### 9.1 目标

把当前“导入 ComfyUI 模板 → 建配方 → 暴露参数 → 选择引用语义 → 配置批量开关”的技术流程压缩成普通创作者可理解的预设。

### 9.2 内置工作模式

- 文生视频。
- 首帧视频。
- 首尾帧视频。
- 多参考图/视频/音频。
- 长视频连续生成。
- 角色替换/保持。
- AI MV。

这些模式映射到 `DirectorRecipe` 和 ComfyUI 模板能力，不新增第二套执行器。

### 9.3 硬件档位

| 档位 | 面向用户文案 | 行为 |
| --- | --- | --- |
| 节省显存 | 适合 8～12GB 显存 | 低精度、低并发、分段清显存 |
| 均衡 | 适合 16～24GB 显存 | 推荐精度、标准步数 |
| 高质量 | 适合 24GB 以上显存 | 高精度、高分辨率、更多步骤 |
| 自定义 | 专业用户 | 展示完整参数 |

实际档位必须来自模板声明或运行时能力分析，不能仅靠模型名称猜测。

### 9.4 一键预检

开始批量生成前自动检查：

- ComfyUI 是否在线。
- 模型和节点是否齐全。
- 参考素材是否超容量。
- 时长、分辨率和画幅是否合法。
- 提示词引用编号是否正确。
- 所需磁盘空间是否充足。
- 是否存在远程计费配方。

预检失败时只阻止受影响片段，其他片段允许继续。

---

## 10. 连续性上下文胶囊

### 10.1 概念

把现有尾帧接力升级为显式的 `ContinuityCapsule`：

```ts
type ContinuityCapsule = {
  sourceSegmentId: string;
  sourceTakeId: string;
  bridgeFrameAssetId?: string;
  motionClipAssetId?: string;
  microReferenceAssetId?: string;
  stateSummary: string;
  characterState?: string[];
  cameraState?: string;
  environmentState?: string;
  createdAt: number;
};
```

### 10.2 素材组成

- 稳定桥接帧：结尾前约 0.25 秒。
- 动作尾段：默认末尾 2 秒。
- 微视频参考：H3 快速模式可选择 16～32 帧，默认 22 帧；其他配方按能力启用。
- 状态摘要：人物姿势、朝向、服装、道具、机位、光线、环境和未完成动作。

### 10.3 UI 表达

- 片段头显示“继承自 06 · Take 2”。
- 时间线片段之间显示接力连接标记。
- 悬停可查看桥接帧、动作片段和状态摘要。
- 重新采用上游 Take 后，下游胶囊显示“已过期”，由用户确认重建。
- 不允许非相邻片段自动串接。
- 关闭连续性接力时保留素材，只停止投喂。

---

## 11. Sequence Dock 与轻量时间线

### 11.1 导演工作区的 Sequence Dock

导演工作区只提供故事级编排：

- 一行片段缩略图。
- 计划时长与当前 Take。
- 缺片、修改和连续性状态。
- 拖动排序。
- 点击定位片段。
- 当前播放头。
- 故事总时长和目标偏差。

### 11.2 成片工作区的轨道

首版正式时间线包含：

```text
V1  采用视频片段
T1  标题卡 / 黑场 / 图片卡
D1  对白
N1  旁白
S1  音效
A1  环境音
M1  音乐
SUB 字幕
```

### 11.3 首版编辑能力

- 片段拖动排序。
- 入点、出点。
- 硬切和短交叉淡化。
- 音量、静音、淡入、淡出。
- 对白/旁白与片段绑定。
- 字幕文本与时间调整。
- 标题卡、黑场和静帧卡。
- 画幅适配：包含、裁切、填充。
- 全片播放与区间循环。

### 11.4 暂不实现

- 任意关键帧动画。
- 复杂转场插件。
- 专业调色节点。
- 变速曲线。
- 复杂音频总线与效果器。

---

## 12. 正式后期与导出引擎

### 12.1 替换实验性 MediaRecorder

现有 `canvas.captureStream + MediaRecorder` 保留为浏览器预览降级，不再作为桌面端正式交付路径。

Tauri 桌面端增加 Rust 视频处理命令，建议采用受控 FFmpeg sidecar 或等价媒体管线：

- 输入探测：时长、分辨率、帧率、编码、音轨、损坏状态。
- 视频拼接与转码。
- 入出点裁切。
- 音频混合与响度控制。
- 字幕烧录。
- 标题卡和静帧合成。
- H.264/H.265 MP4 输出。
- 生成低分辨率代理预览。

### 12.2 输出预设

- 1080p 横屏。
- 1080p 竖屏。
- 2K/4K 高质量。
- 小红书/抖音/视频号。
- Premiere / DaVinci XML 交付。
- 素材包 + SRT + 镜头表。

### 12.3 音频落地

当前项目音频轨必须真正进入成片，不只是写入 XML：

- 原声保留、替换或混合。
- 对白、旁白、音效、环境音、音乐分轨。
- 支持轨道音量和淡入淡出。
- 默认进行峰值保护，避免混音爆音。
- TTS 生成结果支持多 Take 与采用版本。

---

## 13. 提示词系统改造

### 13.1 修正通用编译路径

当前非 H3、无最终覆盖的默认路径需要从“只编译第一个 Shot”改为“编译片段内所有 Shot”：

```text
0.0–3.2 秒：镜头 1
3.2–7.0 秒：镜头 2
7.0–12.0 秒：镜头 3
```

角色也必须按本段实际出场角色过滤，不应默认把项目全部角色写入每段提示词。

### 13.2 提示词来源图谱

最终提示词应能显示来源：

```text
项目风格 ─┐
角色连续性 ├─ 导演结构 ─ Skill 精炼 ─ 参考编号 ─ 负向规则 ─ 最终请求
片段镜头 ─┤
用户修改 ─┘
```

每次生成保存：

- 导演结构版本。
- H3 提示词版本。
- 最终请求版本。
- Skill 指纹。
- 配方与参数。
- 素材顺序。
- 连续性胶囊来源。

### 13.3 修改传播规则

| 修改内容 | 自动影响 | 默认行为 |
| --- | --- | --- |
| 项目风格 | 全部未锁定片段 | 标记已修改，不自动重生成 |
| 角色外观 | 使用该角色的片段 | 展示影响范围并可批量重编译 |
| 场景规则 | 当前场景 | 标记该场景片段 |
| 参考素材顺序 | 当前片段 | 立即重编号并校验提示词 |
| 最终提示词 | 当前片段 | 锁定自动编译 |
| 上游采用 Take | 紧邻下游 | 连续性胶囊标记过期 |

---

## 14. 项目级 AI 导演

### 14.1 上下文

导演台内助手默认读取：

- 项目目标与规则。
- 当前场景、片段和镜头。
- 当前提示词和参考素材摘要。
- 当前采用 Take 和最新 Take。
- 连续性问题、缺片和质量报告。
- 当前配方能力。

不默认读取全部原始媒体字节；需要视觉分析时只发送用户明确选择的缩略图或视频片段。

### 14.2 支持动作

- 改写当前片段摘要、对白或镜头。
- 调整镜头节奏。
- 生成或精炼 H3 提示词。
- 查找连续性冲突。
- 建议需要的参考素材。
- 生成批量执行计划。
- 解释失败原因并给出修复方案。
- 定位到受影响片段。

### 14.3 权限边界

- 修改文本前展示差异。
- 生成图片/视频前必须通过现有费用确认闸。
- 批量采用、删除 Take、重排故事顺序必须二次确认。
- Agent 不能静默修改用户锁定的最终提示词。

---

## 15. 自动化与任务中心

### 15.1 统一任务计划

批量生成前生成可检查的计划：

```text
范围：缺失片段 8 个
引擎：本地 ComfyUI 6 个，远程 API 2 个
配方：H3 REF2VA
预计显存：约 18GB
预计远程费用：按服务商计费，提交后不可撤销
连续性：7 个片段使用上段接力
后处理：每段生成后清理显存
```

### 15.2 状态机

```text
draft → modified → queued → running → review → approved
                         └→ failed
                         └→ cancelled
approved + content_changed → modified_with_approved_take
```

“已有采用版本但内容已修改”必须和普通“缺片”区分，避免用户误以为作品仍与剧本一致。

### 15.3 自动恢复

- 重启后识别被中断的本地任务。
- 保留已完成 Take，不重复消费。
- 支持继续缺失、继续失败、继续已修改。
- ComfyUI 任务不存在时标记中断，不无限轮询。
- 远程任务有任务 ID 时允许恢复轮询。

### 15.4 自动质量闭环

每个采用 Take 运行确定性探测：

- 文件可读性。
- 实际时长。
- 画幅和分辨率。
- 帧率。
- 是否有音轨。
- 黑帧/空帧比例。
- 音频峰值。

质量问题先形成报告；是否重新生成、放大或补帧由用户确认，不自动花费费用。

---

## 16. 文件与项目包改造

### 16.1 本地项目目录

新增可选的用户项目目录：

```text
项目名/
├─ project.momo.json
├─ scripts/
│  ├─ source.md
│  ├─ segments/
│  └─ prompts/
├─ assets/
│  ├─ characters/
│  ├─ scenes/
│  ├─ props/
│  └─ references/
├─ takes/
│  ├─ SEG-001/
│  └─ SEG-002/
├─ continuity/
├─ audio/
├─ subtitles/
├─ proxies/
├─ exports/
├─ reports/
└─ snapshots/
```

### 16.2 两种存储策略

- **托管模式**：继续使用 AppData 资产库，项目只保存资产 ID，适合普通用户。
- **项目目录模式**：素材复制或链接到用户目录，使用相对路径，适合长期项目与迁移。

不强制每次生成重复复制大型视频。只有用户选择“导出可移植项目包”时，才收集依赖素材。

### 16.3 可移植项目包

新增 `.momoproject`：

- DirectorProject 数据。
- 当前画布导演节点摘要。
- 使用中的项目素材。
- 提示词与 Skill 快照。
- 配方快照和所需模板清单。
- 时间线、字幕和音频轨。
- 不包含 API Key。
- ComfyUI 模型只记录名称和校验信息，不打包模型文件。

导入时提供：

- 缺失素材重链接。
- 缺失模板映射。
- 缺失模型报告。
- 相同资产按内容指纹去重。

### 16.4 版本与迁移

`DirectorProject.schemaVersion` 升级时必须增加明确迁移，不再只依赖缺省字段修补。

建议新增：

```ts
type DirectorProjectV2 = DirectorProject & {
  schemaVersion: 2;
  projectRoot?: string;
  workspaceMode?: "quick" | "pro";
  uiState?: DirectorUiState;
  postTimeline?: DirectorPostTimeline;
  promptRevisions?: PromptRevision[];
  continuityCapsules?: ContinuityCapsule[];
  qualityReports?: DirectorQualityReport[];
};
```

---

## 17. 视觉与内容规范

### 17.1 视觉方向

- 保留 MOMO 三主题令牌体系。
- 驾驶舱默认使用低干扰深色监看背景；云白主题下监看器和时间线仍保持中性深色。
- 强调色只用于选中、正在生成和关键操作。
- 状态色含义固定：完成、警告、失败、修改、运行中不得混用。
- 监看器是最大视觉区域，参数检查器不能抢占中心。

### 17.2 信息密度

- 高频操作直接显示：选择片段、播放、改提示词、参考、生成、采用。
- 低频参数折叠：种子、工作流指纹、隐藏节点、原始响应。
- 一行只表达一个主状态。
- 长错误进入报错中心，卡片只显示短摘要。
- 提示文字继续使用 `var(--text-3)` 小字号规范。

### 17.3 文案

技术概念提供创作语言映射：

| 技术名称 | 快速模式文案 |
| --- | --- |
| ComfyUI Template | 本地生成方案 |
| Variant | 生成模式 |
| firstFrame | 开场画面 |
| lastFrame | 结束画面 |
| referenceVideo | 动作参考 |
| referenceAudio | 声音参考 |
| tailFrameRelay | 连续性接力 |
| freeMemBetween | 每段释放显存 |
| workflow fingerprint | 方案版本 |

专业模式仍显示准确技术名称。

---

## 18. 性能要求

- 列表、时间线和 Take 墙必须虚拟化或按可见区挂载。
- 同一时间只允许中央监看器和悬停预览播放视频。
- 波形、视频代理和缩略图生成后缓存。
- 预览优先使用代理文件，导出读取原始文件。
- Zustand 选择器按片段粒度订阅，不能因生成进度更新让全部分镜卡重渲染。
- `DirectorProject` 大数组更新应逐步拆为规范化实体或提供局部 patch API。
- 生成进度与项目持久化分离节流，避免每个 ComfyUI 步数都落盘完整项目。
- 100 个片段、每段 5 个 Take 时，切换片段反馈目标小于 100ms。

---

## 19. 无障碍与可用性

- 所有图标按钮必须有中文 `aria-label` 和 `title`。
- 时间线与片段树支持键盘定位。
- 颜色状态同时提供图标或文字，不只靠颜色。
- 焦点顺序按场景树 → 监看器 → 检查器 → 时间线。
- 拖动排序同时提供“上移/下移”菜单作为无障碍替代入口。
- 弹层关闭后焦点回到触发按钮。
- 快捷模式不出现无法理解的空参数，缺少模板时给出明确配置入口。

---

## 20. 代码结构建议

### 20.1 UI 新增模块

```text
src/modules/director/
├─ DirectorStudio.tsx              # 新工作区壳与全局状态
├─ PlanningWorkspace.tsx           # 策划
├─ DirectorWorkspace.tsx           # 专业驾驶舱
├─ PostWorkspace.tsx               # 成片与轻量时间线
├─ ThreeDPage.tsx                  # 保留并适配新壳
├─ cockpit/
│  ├─ SceneSegmentTree.tsx
│  ├─ DirectorMonitor.tsx
│  ├─ SegmentInspector.tsx
│  ├─ ContentInspector.tsx
│  ├─ PromptInspector.tsx
│  ├─ ReferenceInspector.tsx
│  ├─ GenerationInspector.tsx
│  ├─ TakeSwitcher.tsx
│  └─ SequenceDock.tsx
├─ timeline/
│  ├─ PostTimeline.tsx
│  ├─ TimelineTrack.tsx
│  ├─ TimelineClip.tsx
│  ├─ AudioMixer.tsx
│  └─ SubtitleEditor.tsx
└─ agent/
   └─ DirectorAgentDock.tsx
```

### 20.2 Core 新增或重构

```text
src/core/
├─ directorContext.ts              # 当前项目/片段/Take 上下文
├─ directorContinuity.ts           # 上下文胶囊与失效传播
├─ directorTimeline.ts             # 时间线纯数据操作
├─ directorRender.ts               # 渲染计划与桌面端命令适配
├─ directorProjectIO.ts            # 项目目录与 .momoproject
├─ directorMigration.ts            # DirectorProject 迁移唯一入口
├─ directorQuality.ts              # 媒体探测与质量报告
└─ directorPrompt.ts               # 多 Shot 编译与来源图谱
```

### 20.3 现有文件调整

| 文件 | 调整内容 |
| --- | --- |
| `DirectorStudio.tsx` | 五页导航改为四工作区，承载全局选中态与可调整面板 |
| `StoryboardPage.tsx` | 拆出场景树、内容检查器、参考检查器 |
| `GenerationPage.tsx` | 拆出生成检查器、Take 切换器和任务条 |
| `EditingPage.tsx` | 升级为 PostWorkspace，接入真实时间线与质量报告 |
| `directorQueue.ts` | 继续作为统一执行器，接入上下文胶囊与渲染计划 |
| `directorPrompt.ts` | 编译全部 Shot，过滤本段角色，生成来源图谱 |
| `directorAnalysis.ts` | 将质量检查真正接入 UI 和媒体探测 |
| `directorExport.ts` | XML/SRT/清单保留，正式视频输出交给新渲染层 |
| `videoEdit.ts` | 保留浏览器降级；桌面端优先调用 Rust 媒体管线 |
| `directorStore.ts` | 增加 v1→v2 迁移与片段级更新 API |

### 20.4 Rust/Tauri

建议新增：

```text
src-tauri/src/
├─ media_probe.rs
├─ media_render.rs
├─ media_proxy.rs
└─ project_pack.rs
```

所有命令要求：

- 路径使用显式绝对路径和白名单项目根目录。
- 支持取消信号和进度事件。
- 输出先写临时文件，成功后原子移动到目标位置。
- 失败时保留可读中文错误，不覆盖旧成片。

---

## 21. 分阶段实施

### 阶段 P0：驾驶舱重组

目标：不改生成引擎，先获得竞品最重要的单屏导演体验。

- 新四工作区导航。
- 合并分镜与生成的主要操作。
- 场景/片段树。
- 中央监看器。
- 内容/提示词/参考/生成检查器。
- Take 快速切换与采用。
- Sequence Dock 故事条。
- 导演台内创作助手入口。
- 保留旧页面作为临时高级入口，防止功能回退。

完成标准：用户可在不离开“导演”工作区的情况下完成一个片段的全部编辑和生成操作。

### 阶段 P1：H3 快速模式与连续性可视化

- H3 内置生成模式。
- 硬件档位与预检。
- 上下文胶囊。
- 22 帧微视频参考预设。
- 接力来源、过期状态和重建操作。
- 提示词来源图谱和版本差异。
- 通用提示词多 Shot 编译修复。

完成标准：首次使用 H3 的用户不进入模板管理，也能完成连续片段生成。

### 阶段 P2：轻量后期与正式 MP4

- 桌面媒体探测。
- 视频轨、音频轨、字幕轨和标题轨。
- 入出点、硬切、交叉淡化。
- 音量和淡入淡出。
- 项目音频真实混入成片。
- 字幕烧录。
- H.264 MP4 输出。
- 质量检查接入。

完成标准：一个包含视频、对白、音乐和字幕的项目可以在 MOMO 内导出可发布 MP4。

### 阶段 P3：项目包与自动化闭环

- 项目目录模式。
- `.momoproject` 导入导出。
- 缺失素材重链接。
- 生成计划和预计资源。
- 重启恢复远程/本地任务。
- Agent 批量修改差异确认。
- 质量问题 → 修复建议 → 用户确认 → 批量后处理。

完成标准：项目复制到另一台已安装 MOMO 的机器后，可以重链接模型/模板并继续制作。

---

## 22. 验收用例

### 22.1 UI 与使用

1. 打开导演台后默认进入最近使用工作区和片段。
2. 选择片段后，监看器、检查器和时间线在 100ms 目标内同步更新。
3. 修改提示词、参考素材和配方后，片段显示“已修改”。
4. 重新生成产生新 Take，不覆盖旧 Take。
5. 采用 Take 后监看器和 Sequence Dock 立即更新。
6. 50 个片段滚动时不挂载 50 个视频播放器。

### 22.2 提示词

1. 多 Shot 片段的最终提示词包含全部时段。
2. 本段未出场角色不进入默认提示词。
3. 拖动参考素材后，编号与实际投喂顺序一致。
4. 保存最终稿后，项目风格与 Skill 不再重复拼接。
5. 修改角色外观时能列出受影响片段，但不静默覆盖锁定稿。

### 22.3 连续性

1. 第 2 段只能自动继承故事顺序中紧邻的第 1 段。
2. 重新采用第 1 段 Take 后，第 2 段胶囊标记过期。
3. 关闭接力后素材仍在，但不参与生成。
4. 配方无视频参考容量时自动降级为桥接帧和文字状态。

### 22.4 生成与恢复

1. 支持生成缺失、失败、已修改和勾选片段。
2. 取消批量跑完当前段后停止；立即停止会中断当前 ComfyUI 并清队列。
3. 重启后运行中任务显示中断，不伪装为未开始。
4. 已完成 Take 在恢复时不重复生成。
5. 远程计费任务开始前有明确确认。

### 22.5 成片与导出

1. 片段入出点影响预演和最终导出。
2. 对白、旁白、音效、环境音和音乐能真实混入成片。
3. 字幕既可导出 SRT，也可烧录到 MP4。
4. 输出 MP4 可被系统播放器、Premiere 和 DaVinci 正常读取。
5. 导出中止不会覆盖上一次成功成片。

### 22.6 项目包

1. `.momoproject` 不包含 API Key。
2. 导入时相同素材按指纹去重。
3. 缺失 ComfyUI 模型和模板能形成可读报告。
4. 项目素材路径支持重链接。
5. 旧 v1 导演项目无损迁移到 v2。

---

## 23. 产品指标

上线后重点观察：

- 首次进入导演台到生成第一段的中位时长。
- 单段修改到重新生成所需点击数。
- 用户在分镜与生成之间的页面切换次数。
- 批量生成成功率和恢复率。
- 提示词参考编号错误率。
- 已修改但未重新生成片段被误导出的次数。
- 成片导出成功率。
- 使用外部剪辑软件前，用户在 MOMO 内完成音频与字幕的比例。
- 快速模式和专业模式的使用分布。

建议目标：

- 单段修改闭环主要操作不超过 5 步。
- 参考编号错配为 0。
- 已修改片段误导出为 0。
- 桌面 MP4 导出成功率不低于 98%。
- 异常退出后已完成 Take 恢复率为 100%。

---

## 24. 风险与控制

| 风险 | 控制方案 |
| --- | --- |
| 单屏驾驶舱信息过密 | 快速/专业双模式，低频参数折叠 |
| 合并分镜和生成导致回归 | 先复用现有组件与 Store，旧页保留一个版本周期 |
| FFmpeg 体积与授权问题 | 明确构建方案、编码器和许可证，允许外部路径配置 |
| 大项目性能下降 | 虚拟列表、代理文件、细粒度订阅、进度节流 |
| Agent 修改范围过大 | 差异预览、锁定保护、费用确认 |
| 项目包体积巨大 | 托管/可移植两种模式，按依赖收集，不复制模型 |
| H3 快速模式侵蚀通用性 | 所有预设只映射现有 Recipe/Capability，不写死核心模型 |
| 时间线演变成无限范围 NLE | 首版严格限制轨道、转场和编辑能力 |

---

## 25. 最优先执行建议

如果只选择一轮改造，应先完成 P0，不要先继续添加零散功能。

第一轮推荐范围：

1. 新建“导演”工作区驾驶舱。
2. 左侧片段树复用 `StoryboardPage` 数据。
3. 中央监看器复用当前 Take 与顺序预演能力。
4. 右侧检查器复用现有片段内容、最终提示词、参考槽和配方组件。
5. 底部 Sequence Dock 复用 `deriveTimeline` 和片段状态。
6. 生成任务条复用 `runBatch`、`cancelBatch`、`stopBatchHard`。
7. 原“分镜”“生成”页面暂时保留为“详细视图”，等驾驶舱稳定后再删除重复入口。

这一轮不需要重写底层执行器，却能最大程度吸收竞品的专业感和操作效率，也是投入产出比最高的改造。

---

## 26. 最终产品判断

MOMO 不应把竞品的价值理解成“深色界面、三栏布局和很多按钮”。真正值得吸收的是：

- 作品始终在屏幕中央；
- 当前镜头始终有明确上下文；
- 提示词、角色、参考和生成围绕同一镜头工作；
- 镜头之间的时间与连续性关系可见；
- AI 是当前项目的导演助手，而不是另一个孤立聊天框；
- 本地模型的复杂度被包装成普通用户能够理解的选择。

MOMO 的差异化仍然必须保留：

- 模型无关；
- ComfyUI 工作流开放；
- 本地与远程混合；
- 画布、角色库、资产库和 Skill 互通；
- 真实请求、参数和素材顺序可追溯；
- 项目能够恢复、迁移和标准交付。

最终目标不是复制一个 MiniMax H3 专用导演台，而是让 MOMO 的通用生产能力拥有同样直观、专业、连续的导演体验。
