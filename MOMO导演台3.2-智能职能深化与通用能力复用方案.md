# MOMO 导演台 3.2：智能职能深化与通用能力复用方案

> 文档状态：导演台 3.0 智能化纠偏基线  
> 规划日期：2026-08-29  
> 纠偏说明：AI 导演与创作助手不是同一个产品职能；应保留独立 AI 导演，同时复用通用基础能力  
> 核心目标：不减掉导演台的独立性，不复制 MOMO 已有的模型、生成、资产和任务基础设施

---

## 0. 核心判断

上一版“取消独立 AI 导演、改成创作助手上下文”的决策不准确。

创作助手与 AI 导演虽然都使用对话模型，但它们解决的不是同一个问题：

```text
创作助手：我想做一张/一条什么内容？
             → 聊清方案
             → 确认提示词与规格
             → 快速出图/出片

AI 导演：这整个项目现在是什么状态？
          → 理解剧本、角色、片段、参考、Take 和质量问题
          → 诊断叙事、连续性和制作风险
          → 提出结构化变更与执行计划
          → 用户审核后交给各专业工位执行
```

因此：

- AI 导演应继续作为导演台的独立一级工位；
- 它不应被改成快捷出图助手；
- 它应复用通用 LLM 传输、模型选择、搜索、附件、错误处理和历史压缩等基础能力；
- 它必须拥有独立的项目会话、导演上下文、结构化提案、影响分析和审批权限；
- 它不直接生图或生视频，也不复制角色卡、AI 制图、H3 和 MV 的执行引擎。

---

## 1. 五维职能区分

### 1.1 创作助手（聊天模式）

| 维度 | 定义 |
| --- | --- |
| 用户目标 | 把一个模糊想法聊成可用提示词，快速生成单个结果 |
| 主要上下文 | 当前会话、用户附图、上一轮成图、已确认的风格/画幅 |
| 输出 | 自然语言建议、成品级提示词 |
| 权限 | 用户点击后在画布创建生图节点并运行 |
| 生命周期 | 跨画布的个人创作会话，不是导演项目档案 |

现有实现和这一定位基本一致：提示词围绕绘画/摄影/视频创作，用户可一键把消息送到画布生图。

### 1.2 创作 Agent（创作助手的 Agent 模式）

| 维度 | 定义 |
| --- | --- |
| 用户目标 | 让 AI 自动搜索、询问关键选择、组织提示词并交付图/视频 |
| 主要上下文 | 当前创作会话、附图、搜索结果、刚生成的媒体 |
| 输出 | `search / ask / image / video / reply` 动作 |
| 权限 | 经生成确认闸后直接生图/生视频，并在画布创建节点 |
| 生命周期 | 一次创作任务，最多 12 轮动作 |

它是“自动出成品的美术执行 Agent”，仍不是项目导演。

### 1.3 AI 导演

| 维度 | 正确定义 |
| --- | --- |
| 用户目标 | 完成一部项目的创意、叙事、分镜、连续性和生产决策 |
| 主要上下文 | 完整项目：剧本、角色、场景、片段、提示词、参考槽、Take、质检、配方与生产状态 |
| 输出 | 诊断、问题、导演意图、结构化修改提案、生产计划 |
| 权限 | 默认只读；所有写入项目的变更都先生成差异卡，经用户确认后应用 |
| 生命周期 | 与 DirectorProject 绑定，跨工位、跨会话保留 |

### 1.4 任务型 AI 工具

导演台还有多个不需要伪装成“AI 导演”的专用 AI 命令：

- 完整剧本拆分；
- 规则切段后的 AI 精读；
- 按 Skill 精炼 H3 提示词；
- 优秀提示词的结构化拆解；
- 3D 布景/机位建议。

这些功能的特征是“输入明确、输出契约明确、一键运行”。它们应保持独立专用函数，AI 导演可以建议运行它们或在用户确认后调用，但不应把各函数的提示词逻辑重写进对话 system prompt。

---

## 2. 当前 AI 导演的实际能力

### 2.1 已做对的部分

- 作为七工位之一独立存在，产品方向正确；
- 默认不直接发起计费生成，生成仍交给 H3 等专业工位；
- 已有项目 Skill 注入；
- 已有剧本、角色、片段和提示词提案的基础写回网关；
- 外部 Agent 不直接改数据，先进提案池，这一安全边界应保留；
- 锁定最终提示词不被 AI 静默覆盖，方向正确。

### 2.2 为什么现在还只像“项目聊天框”

#### 问题 A：项目上下文严重不完整

当前上下文只包含：

- 项目名、画幅、目标时长；
- 剧本开头 400 字；
- 角色名字；
- 当前片段的一句摘要。

它没有读取：

- 完整剧本或分层摘要；
- 角色的连续性、造型状态和参考素材；
- 场景与全部片段顺序；
- 当前片段的镜头、对白、提示词、参考槽和接力上下文；
- 已有 Take、采用状态、错误和生成快照；
- 连续性检查、缺片、质量报告和时长偏差；
- 配方支持的时长/画幅/参考容量和引擎状态；
- MV、音频轨和后期时间线的当前状态。

因此它无法做“整部片的导演决策”，只能写出泛化文案。

#### 问题 B：会话不属于项目，切换工位可能丢失

AI 导演的 `msgs` 是 `AIDirectorStation` 内部 `useState`。它不进 `DirectorProject`、不进独立持久化 store，也没有前情摘要。

这会导致：

- 从 AI 导演切到 H3 后再回来，会话容易重置；
- 项目无法保留关键导演决策的来源；
- 多个导演项目没有各自独立的持久会话；
- 长项目没有历史压缩，后续既可能超上下文，也可能失去早期决策。

#### 问题 C：输出是自由文本，不是导演动作

内置快捷动作只是五段固定指令：故事梗概、角色设定、场景表、分段方案、提示词草案。LLM 回复后仍是自由文本，系统不知道其中哪一段是场景、哪一段是角色或哪一条是变更。

结果是：

- 所有回复只有一个「存为剧本草稿」按钮；
- 角色设定也会被当成剧本文本保存；
- 导演无法一次提交包含多个对象的修改计划；
- 无法给每个变更单独采用/驳回；
- 无法记录变更依据和预期影响。

#### 问题 D：所谓“合并”实际是全文替换

当前「合并到当前剧本」只展示左右两段全文，点确认后直接写入 `script: mergeOpen.after`。

这不是合并，也没有真正计算：

- 新增/删除/修改了哪些场景；
- 哪些现有片段将失效；
- 哪些提示词、微参考、Take 和时间线需要复核；
- 是否只接受部分修改。

#### 问题 E：提案网关方向正确，但功能太浅

当前网关只支持：

- 剧本文本；
- 角色 `continuity` 一个字段；
- 片段摘要/对白/承接一小部分；
- 片段 `promptOverride`。

它尚不支持：

- 场景、片段顺序和时长变更；
- 镜头数组和具体镜头修订；
- 角色绑定、参考槽和造型状态；
- 配方或生成策略建议；
- 重生/精炼/质检等任务计划；
- 提案之间的依赖、冲突和部分采用；
- 提案的持久化和审计历史。

#### 问题 F：没有真正使用导演台已有的分析能力

项目里已经存在：

- `buildObjectIndex`：场景/片段/状态索引；
- `checkContinuity`：缺片、重复、跳跃、角色冲突、时长检查；
- `precheckBatch`：引擎、模板、时长、画幅、参考容量、计费接口检查；
- `previewSegmentPrompt`：实际发送提示词；
- `projectProgress` / `collectBatchTasks`：制作进度与待生成范围；
- `qualityReports`：已采用 Take 的质量探测结果。

但 AI 导演并没有读取这些结果，也不能把它们组织成导演诊断。这是“智能化没有连起来”的最核心问题。

#### 问题 G：对话基础能力反而弱于创作助手

AI 导演目前只用默认 chat 模型调一次 `chatOnce`，没有：

- 流式输出；
- 显式模型选择；
- 前情摘要与长会话管理；
- 联网调研；
- 图片附件和视觉理解；
- 生成中断与正式错误上报。

这些是“通用对话基础设施”，应从现有创作助手中抽取复用，但不共用创作助手的会话数据和出图动作。

---

## 3. AI 导演应该具备的完整职能

### 3.1 项目理解

AI 导演每轮不应盲目塞入整个项目，而是建立分层上下文：

#### 第 1 层：项目导演摘要（常驻）

- 项目目标、类型、观众、时长、画幅和风格原则；
- 故事梗概与当前版本的主要变化；
- 角色表与关键连续性锁；
- 项目当前阶段和主要阻塞。

#### 第 2 层：当前工位与选中对象（动态）

- 当前所在工位；
- 当前场景/片段/Take/MV 区间；
- 该对象的完整结构化数据；
- 相邻片段与承接状态；
- 当前提示词、槽位编号、配方能力和 Take 快照。

#### 第 3 层：按需查询（工具读取）

- 读取某场的全部片段；
- 读取某角色的所有出场和造型状态；
- 读取某个 Take 的提示词、配方、参考和质量报告；
- 运行连续性检查或批量预检；
- 读取资产元数据或少量关键帧。

这样才能在不爆上下文的前提下理解完整项目。

### 3.2 项目诊断

AI 导演应能回答的不是只有“请帮我写一个梗概”，而应包括：

- 剧本结构是否完整，开场、冲突、高潮和结尾是否成立；
- 片段是否过密、过松、超时或信息重复；
- 角色的动机、造型、道具和位置是否连续；
- 相邻片段在动作、镜头、空间、光线和声音上是否可接；
- 哪些片段缺参考、配方不兼容或提示词引用错误；
- 哪些 Take 需要重生、精炼、放大或仅调后期；
- 预计要跑多少本地/远程任务，哪些会产生费用。

确定性检查应优先调现有分析函数；LLM 负责把多个结果组织成可理解的导演判断，不凭空猜系统状态。

### 3.3 结构化提案

建议 AI 导演每次产出一个 `DirectorResponse`：

```ts
type DirectorResponse = {
  reply: string;
  findings?: Array<{
    severity: "info" | "warning" | "blocker";
    category: "story" | "continuity" | "prompt" | "reference" | "engine" | "quality" | "delivery";
    targetId?: string;
    evidence: string;
    suggestion: string;
  }>;
  proposals?: DirectorProposal[];
  plan?: DirectorPlanItem[];
  questions?: Array<{ id: string; text: string; options?: string[] }>;
};
```

提案不再只有 `before/after` 大文本，而应是对象级 patch：

```ts
type DirectorProposal = {
  id: string;
  targetType: "script" | "scene" | "segment" | "shot" | "characterState" | "prompt" | "slot" | "recipe" | "timeline";
  targetId: string;
  reason: string;
  evidence: string[];
  patch: unknown;
  impact: {
    invalidatedSegmentIds: string[];
    staleTakeIds: string[];
    rebuildMicroRefIds: string[];
    estimatedRemoteJobs?: number;
  };
  status: "pending" | "applied" | "rejected";
};
```

用户可以：

- 单独采用一条；
- 批量采用无冲突提案；
- 编辑 patch 后再采用；
- 驳回并保留原因；
- 查看提案会影响的片段、Take 和微参考；
- 应用后通过项目历史撤销。

### 3.4 生产计划而非直接生成

AI 导演可以产出：

```text
建议执行计划
1. 精读片段 03、04、07
2. 为 04 补充角色侧面参考
3. 精炼 03–07 的 H3 提示词
4. 先试跑 03、04，确认风格后再跑 05–07
5. 生成后执行连续性和质量检查
```

每一步的执行仍交给现有专用函数：

- 精读 → `analyzeSegmentsWithLLM`；
- 提示词精炼 → `refineSegmentPrompts`；
- 生成前检查 → `precheckBatch`；
- 片段生成 → `runBatch`；
- 质量探测 → `probeApprovedTakes`；
- 连续性 → `checkContinuity`；
- 放大 → `runBatchUpscale`。

AI 导演只是计划者和审批入口，不重写执行引擎。

### 3.5 多模态审片

只读文本的 AI 导演无法判断视觉连续性。但不需要一开始就开发复杂视频理解引擎。

最小实现是：

1. 对选中 Take 抽取首帧、中帧、尾帧；
2. 加上上一段尾帧和下一段首帧；
3. 仅在用户点「视觉审片」时传给具有 vision 能力的 chat 模型；
4. 输出角色外观、空间、光线、动作和轴线连续性问题；
5. 只生成问题与修改提案，不自动重生。

这比给 AI 导演加一个直接生视频工具更符合它的职责。

---

## 4. 导演台各工位的深层职责与通用复用

### 4.1 AI 导演：保留独立工位，重做智能内核

**保留：**

- 独立工位和导演会话；
- 项目事实、导演意图、问题与提案；
- 内置/外部 Agent 统一提案入口；
- 用户确认后的受控写回。

**与创作助手共享：**

- LLM 传输与模型能力判定；
- 流式输出；
- 历史摘要算法；
- 搜索服务；
- 图片附件和视觉输入格式；
- 中断、错误上报、模型选择 UI。

**不共享：**

- `agentStore.messages`；
- 创作助手的 `search/ask/image/video/reply` 动作协议；
- 出图/出片确认签名；
- 创作助手的绘画人设和快捷提示词。

### 4.2 剧本库：保留文档生命周期，不重做画布文本节点

剧本库的独特性是：

- 草稿/正式/归档；
- 不可变版本；
- 版本对比与回退；
- AI 导演提案的沉淀；
- 送入当前项目前的影响分析。

这些不等于画布的提示词/分镜节点，所以剧本库可以保留。但通用文本编辑、文件导入、拆分函数和差异组件应共享，不为每个页面再写一份。

需要修复的是：“送入项目”必须先生成场景/片段级差异和失效列表，不能先替换 `project.script` 再去提醒。

### 4.3 角色库：保留项目角色统筹，复用角色卡的素材生产

画布角色卡的核心是：

- 分析一个角色；
- 生成三视图、表情、服装、立绘和设定卡；
- 把选定结果向下游工作流输出。

导演台角色工位的核心应是：

- 项目演员表；
- 角色与场景/片段的出场关系；
- 角色在不同剧情阶段的造型状态；
- 本项目采用哪些角色卡素材；
- 角色源更新后哪些片段过期；
- 声音选角与对白轨绑定。

因此不是删除角色工位，而是将它从“第二个角色卡编辑器”改成“项目角色统筹”。

**应由角色卡主管的数据：**

- 基础身份；
- 外貌锚点；
- 基础服装设计；
- 正面/侧面/全身/表情/动作参考素材；
- 完整物料生成提示词。

**应由导演项目主管的数据：**

- 源角色卡 ID 与版本指纹；
- 本项目采用的参考组合；
- 剧情阶段造型（例如“第 3 场起淋雨、第 6 场起受伤”）；
- 出场片段、声音选角和当前项目的连续性例外。

### 4.4 AI 制图：保留项目化工作台，删除第二套执行层

AI 制图作为导演台工位仍有价值，因为它可以：

- 围绕当前项目/角色/场景/片段快速做视觉开发；
- 把结果立即绑定为角色、场景、首帧、尾帧或参考槽；
- 以项目视角筛选生成历史。

但它不应有自己的 `runImageStudio`、供应商适配、ComfyUI 调度和独立历史数据。

优化方式：

- UI 可继续保留文生图/图生图/编辑图工作区；
- 参数表单复用画布 `GenConfigPanel` 的模型家族与字段规则；
- 执行走共享的图像生成 runner/调用器；
- 结果历史是 AssetStore 的项目筛选投影，不再写 `project.imageStudio.history`；
- 结果携带项目、对象、槽位和来源参数的 provenance；
- 错误进全局错误中心，请求进全局运行日志。

这是“保留专用界面，共用通用能力”，而不是把用户赶回画布。

### 4.5 H3 导演台：保留完整主工位，AI 导演只做诊断与计划

H3 工位的核心职能是：

- 片段顺序、提示词、参考槽、配方与 Take；
- 22 帧微参考与尾帧接力；
- 单段/批量生成、停止、失败重跑、放大；
- 精读、Skill 精炼、采用 Take。

AI 导演可以说“建议先精读 03/04，然后用配方 A 试跑 03”，但不在对话工位里再做一套配方表单、参考槽编辑器或 Take 列表。

提案卡的「去执行」应定位到 H3 的真实片段和功能入口。

### 4.6 AI MV：保留音乐叙事编排，共享生成引擎

AI MV 的独特职能是：

- 节拍/能量/段落分析；
- 歌词时轴；
- 多图/角色与视觉区间的映射；
- 口型区间；
- 区间 Take 和成片编排。

应共享：

- 资产导入与素材真相；
- 配方/模型选择；
- 图像/视频生成执行；
- 任务取消、错误上报和运行日志；
- 最终成片渲染。

AI 导演在 MV 中的职责是“音乐叙事建议”，例如主歌信息密度、副歌视觉高潮、段落间颜色/运镜变化，不自己生成区间视频。

### 4.7 成片交付：保留执行与确定性质检，增加 AI 综合审片

成片工位继续主管：

- 缺片、分辨率、帧率、时长和媒体损坏等确定性检查；
- 时间线、混音、字幕、转场、预演和正式导出。

AI 导演可以读取检查结果与关键帧，给出叙事节奏、情绪连续、角色视觉连续和字幕/声音建议。确定性问题不交给 LLM 重算，避免花费更高且更不准。

### 4.8 3D 预演：保留专用 AI 布景动作，不搬进 AI 导演页

3D 布景中的 AI 调用有明确的结构化输出契约，与对话导演不是同一 UI 任务。AI 导演可以先给站位/机位意图，点「在 3D 中布置」后将意图作为输入交给现有 3D AI 动作。

---

## 5. 通用功能重复的正确处理

### 5.1 可以共享的基础能力

| 通用能力 | 当前情况 | 目标 |
| --- | --- | --- |
| LLM 调用 | AI 导演用 `chatOnce`，创作助手用 `chatStream` | 共用流式调用、中断、错误上报，保留不同 system policy |
| 模型能力 | 创作助手已检测 vision/search | AI 导演直接复用 `chatCaps` 与 `ModelPicker` |
| 历史压缩 | 创作助手已有最近窗口 + 摘要 | 抽取一个无 store 依赖的摘要工具，AI 导演使用自己的项目会话 |
| 搜索 | 创作助手已兼容内置/外部搜索 | AI 导演复用搜索服务，主要用于资料核查，默认不开 |
| 附件/视觉 | 创作助手已能传图 | 共享附件格式与资产解析，AI 导演保存项目级引用 |
| 提案差异 | 导演台已有初步 UI | 做成导演台共享 `ProposalCard`，剧本/角色/H3/MV 共用 |
| 错误与日志 | 多处 toast，专用任务又有 JobCenter | 运行错误统一进 `pushError`，网络请求统一进 runLog；JobCenter 只存语义任务进度 |
| 资产 | 各工位都直接导入/维护局部历史 | 物理素材只有 AssetStore；工位只维护引用、标签和投影 |
| 生成执行 | 画布 runner、ImageStudio、MV 有多条路径 | 模型适配、参数正规化、任务取消、入库为共享底层；不同工位保留自己的业务编排 |

### 5.2 绝对不应共享的业务状态

- 创作助手会话与 AI 导演会话；
- 创作助手的生成确认闸与 AI 导演的提案审批；
- 单张图的提示词方案与全片导演意图；
- 角色卡的角色设计数据与项目角色的剧情状态；
- 生图工位的临时表单与 H3 片段的参考槽/生成快照；
- AI 导演的提案/计划与实际执行任务。

共享底层不等于共享业务实体。

---

## 6. 建议的 AI 导演界面

### 6.1 不再是“左快捷词 + 中聊天 + 右大纲”

建议改为：

```text
┌─ 项目导演台───────────────────────────┐
│ 左：导演记忆/任务       中：对话与提案       右：项目诊断 │
│                            与结构化变更                    │
│ · 项目目标             · 对话消息            · 叙事问题    │
│ · 已确认导演决定       · 可编辑提案卡        · 连续性问题  │
│ · 待执行制作计划       · 依据/影响/差异       · 生产阻塞    │
│ · 历史决策             · 采用/驳回/去执行    · 交付问题    │
└───────────────────────────────────────┘
```

### 6.2 快捷入口应是“导演任务”

替换现在五个泛化生成按钮：

- 全项目体检；
- 剧本结构复盘；
- 当前场景导演；
- 当前片段连续性复核；
- 选中 Take 视觉审片；
- 生成前风险检查；
- 生产顺序与试跑计划；
- 成片节奏与交付复盘。

“故事梗概”和“角色设定”仍可以通过自然对话要求，但不应成为 AI 导演最显眼的五个主职能。

### 6.3 提案与对话必须区分

- 对话文本：用于说明、追问、论证；
- 问题卡：用于需要用户决策的分叉；
- 发现卡：有证据的问题，可定位对象；
- 提案卡：真实可写入的结构化 patch；
- 计划卡：一组要交给不同工位的任务；
- 执行结果卡：来自 H3/MV/质检的真实回流。

这样 AI 导演才不会成为另一个“聊完还得手动复制”的文本对话框。

---

## 7. 最小实施路线

### P0：修正产品边界，不先做大重构

1. 保留七工位整体概念，不删除 AI 导演、剧本库、角色库和 AI 制图。
2. 先冻结 `runImageStudio` 和 MV 中重复生成逻辑的继续扩展。
3. 明确每个工位的独特业务状态和可复用底层。

### P1：先把 AI 导演变成真正的项目智能

1. 新建按 `projectId` 持久化的导演会话，不复用 `agentStore.messages`。
2. 建立 `buildDirectorContext`，读取项目摘要、选中对象和现有分析结果。
3. 将输出从纯文本改为 `DirectorResponse`。
4. 将提案池改为可持久、对象级 patch，保留应用/驳回记录。
5. 将“合并剧本”改成真正的场景/片段差异与影响分析。

### P2：只抽取已经有二次需求的通用基础

1. 流式对话 + 中断。
2. 历史压缩。
3. 模型能力/联网/视觉输入。
4. 提案卡和对象差异组件。
5. 统一错误上报与运行日志。

不先建一个庞大的“通用 Agent 框架”；仅当创作助手和 AI 导演确实都需要同一个底层时才抽取。

### P3：收回工位重复执行逻辑

1. AI 制图保留 UI，改走共享生图执行层和 AssetStore 项目投影。
2. AI MV 保留编排，改走共享视频生成执行层。
3. 角色工位绑定角色卡源，不再重做角色物料生产。
4. 剧本库和 AI 导演共用提案差异/影响分析。

### P4：最后补多模态审片

1. 首/中/尾关键帧。
2. 相邻片段对比。
3. 叙事节奏与视觉连续性建议。
4. 对应的重生/后期提案。

不在 P1 阶段同时开发视频理解、自治调度和复杂多 Agent 系统。

---

## 8. 验收标准

### 8.1 创作助手与 AI 导演职能验收

- 创作助手仍然可以用最短路径“聊方案 → 确认 → 出图/出片”；
- AI 导演在没有对应项目时不出现，会话与 `projectId` 绑定；
- 切换 AI 导演/H3/成片后会话不丢；
- AI 导演可以准确描述当前项目的场景、片段、缺片、已采用 Take 和主要连续性问题；
- AI 导演不得虚构生成成功、引擎状态或质量检查结果；
- AI 导演的项目修改必须以可编辑提案卡呈现，不能从自由文本直接覆盖项目；
- AI 导演不具有 `image` / `video` 直接生成动作。

### 8.2 智能联动验收

- 点「全项目体检」时，AI 导演先调取确定性分析，再给综合判断；
- 提案可定位到真实场景/片段/镜头/角色/提示词；
- 提案显示会失效的 Take、微参考和时间线条目；
- 计划卡的「去执行」定位到原工位的真实功能，不在 AI 导演中复制表单；
- H3/MV/质检完成后，结果可回流到导演计划和诊断。

### 8.3 通用能力复用验收

- AI 导演和创作助手可使用不同对话模型，但走同一模型能力判定与 LLM 传输层；
- AI 制图与画布生图不再维护两份模型家族参数逻辑；
- AI MV 与 H3/画布视频生成不再维护两份 provider/ComfyUI 执行逻辑；
- 角色物料只由角色卡产出，导演角色工位管项目采用和剧情状态；
- 所有生成结果以 AssetStore 为物理真相，工位历史仅是查询投影。

---

## 9. 最终产品定义

导演台是一个独立制片产品，不是画布的简化页；AI 导演也不是创作助手换了一个名字。

正确的智能关系是：

```text
创作助手 = 单次创作加速器
AI 导演  = 项目级理解、诊断、提案和生产计划
专业工位 = 剧本、角色、制图、H3、MV、后期的真实执行器
共享基础 = 模型、Skill、资产、生成适配、任务、错误与日志
```

智能化的重点不是让 AI 导演获得越来越多的直接执行权，而是让它真正看懂项目，能基于真实状态做出导演判断，并把结构化决策安全地交给已有工位执行。
