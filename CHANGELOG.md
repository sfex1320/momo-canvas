# 更新日志

记录每个版本的功能变化与重要修复。未发布的改动记在「未发布」一节，打版时整段挪到新版本号下。

## 未发布

### 宫格切分 × 分镜组（复刻 LibTV 宫格动线，删除独立宫格节点）

- **删除独立「宫格」节点**：`grid` 节点类型、GridNode 组件、运行器与目录/快捷键条目全部移除（该节点 `nodeOutput` 本就没有 grid 分支，outPortType 声明的图片出口是空头的，「传入 N」角标也因此常与可见连线对不上）。旧画布上已有的宫格节点载入时自动转成图片节点（整图保留，来源写进名字），不白屏不丢图。
- **宫格切分会话（图上直接切）**：图片/生成节点「编辑 → 宫格切分」进入图上会话——整图叠加切割线，**线可拖动**调整每格大小（最小格宽钳制），预设 4/9/16/25 宫格 + **自定义宫格悬停选格器**（1-5 行 × 1-5 列任意组合），「重置均分」一键复原；点格按**点击顺序**勾选（角标显示分镜序号）。会话条上「全部拆出」按当前切割线把整图拆成独立图片节点（阅读序紧凑摆放，行高按真实比例）。
- **创建分镜组**：勾选宫格后一键把选中格按点击顺序包进一个**无框组**——切片**统一尺寸**（全部中心裁到切分形状的等分比例）、**严格网格吸附排布**（整行整列，不是随图乱排）、**无小标题**（纯图外观，悬停出工具条），每片与原图连一条**溯源连线**。整组可拖动，Ctrl+Z 一步回退。
- **分镜组工具条**（悬停组框显示，对标 LibTV 宫格分镜组）：**每行格数**下拉（按分镜顺序严格网格重排）、**拼接**（全部切片拼成一张长边 2K 的网格整图，生成新图片节点）、**序号**（切片左上角显示/隐藏分镜顺序角标）、**转普通组**（恢复组框与瀑布流自动排布）、**解组**。
- **AI 模板（剧情推进风格，动线对齐 LibTV「九宫格 ▾」）**：图片节点「编辑 → AI 模板…」打开**分类大面板**——分镜叙事（时间演进九宫格/剧情推演四宫格/25宫格连贯分镜/画面推演·3秒后/·5秒前）、质感调节（人像质感调节/电影级光影校正）、空间与机位（多机位九宫格/720°全景）、设定图（脸部三视图/角色三视图/角色设定图/场景设定图/产品设定图）。点模板即在下游**铺一个生成节点**（参考图自动连线、提示词模板预填），场景描述与画幅画质都在节点上自己调、自己点生成——不再在菜单里问画幅、也不自动跑；宫格类模板出图后走「宫格切分 → 创建分镜组」。九宫格抽卡原有路径不变。
- **修复「传入 N 却看不见连线」幽灵边**：`sanitizeEdges` 载入时剔除句柄非法的边（React Flow 对渲染不出来的边只留数据不留线——「传入」角标按边计数，于是出现角标有数、画布无线）；分镜切片的溯源连线列为清洗例外。「传入」组合预览弹窗支持**点击行定位来源节点**（选中 + 视图飞过去），来源再也藏不住。

### 生成链路稳定性 + 协议系统精简（取消自定义协议）

- **修复生成节点永远「生成中」**：`xfetch` 此前没有任何超时——一条挂起的连接（典型：65535 美国线路 sub-proxy-us 这类跨境代理不回包）会让整个 Promise 链永久悬挂，提交/轮询/下载全堵死，轮询循环里的截止时间也只在请求间隙才检查得到。现在默认 90 秒超时（超时抛中文「请求超时」错误，不会被误判为用户主动取消，且计入可重试的瞬时错误）；已付费的结果下载单独放宽（图片 5 分钟、视频 10 分钟）。
- **取消自定义协议功能**：设置里的「协议」页整页删除（协议助手 / JSON 编辑 / 真实测试与校准 / 协议自愈 / 报错中心一键修复协议全部移除；`protoCalibrate / protoSelfHeal / protoSpec / protoTabStore / ProtocolTab` 五个文件删除）。协议只来自内置预设：**65535 生图 · APIMart 生图 · APIMart 生视频**（Grsai 等历史在用协议保留）。
- **协议自动绑定与自愈式刷新**：baseUrl 命中预设的服务商（65535.space / apimart.ai / aishuch.com），对应用途槽位还走内置协议时启动即自动换绑预设协议；预设派生条目（id `preset-*`）每次加载强制刷新为当前预设内容——站点接口有变化改 `protoPresets.ts` 一处即可。历史导入的旧预设协议（旧 id）自动收敛到 `preset-*`，不会出现同名重复。
- **协议库清理与更名**：不再被任何服务商槽位引用的孤儿协议清除；协议名精简为「中转站 + 用途」（如「65535 异步生图（图生图+蒙版·修正版）」→「65535 生图」），模型配置下拉去掉「未校准 / ✓ 已校准」标注与 ★ 前缀，选项补齐角色图标。
- **提示词「自定义指令」改为编辑器语义**：此前用户的一句话指令被直接当 system 人设、原文当新需求，模型会自由发挥生成大量无关内容。现在框成「按修改指令编辑原文」——只改指令涉及的部分，未提及的措辞/结构/语言/长度原样保留，不扩写不解释。
- **修复提示词节点 Skill 凭空消失**：内置 Skill「提示词精修」缺 `prompt.text` 上下文——提示词节点工具弹窗在无同路参考图时只查该上下文，导致「有图的节点能看到、新建的无图节点上消失」。已补齐。
- **模型配置编辑面板操作置顶固定**：「测试 / 取消 / 保存服务商」移入标题栏（固定不随内容滚动，保存前自动收口未回车的模型名输入）；默认模型切换逻辑不受影响。
- 附带：报错中心「AI 分析」提示词去掉协议 JSON 修复输出；`http.ts`/`logStore.ts` 补 `.ts` 扩展名导入（纯逻辑快测链路）。

### Comfy 工作流无感同步 · 自我验证修复与测试加固

对照规格全链路走查（添加来源→首扫→勾选→保存→改名→写回→冲突→恢复→离线→桥通知）发现并修复 5 处问题，并补齐 Rust 侧自动化测试：

- **状态机修复**：文件内容与基线一致但 stat 变化（touch/离线恢复/损坏文件被撤销）时，`derive_failed` 不再被错误刷成 `synced`（派生未成功，等 ComfyUI 上线补）；`invalid` 恢复时清残留警告。
- **性能修复**：来源设置里改名称/忽略规则等不再重启 watcher + 全量重扫（此前每敲一个字全扫一次）；只有路径/递归/启用变化才重启监听。
- **防重复**：同一目录重复添加来源直接拦截（否则每个文件会长出两套记录与模板）。
- 桥探测与「在 ComfyUI 打开」统一走 `normalizeHost`（兼容无 scheme 的服务地址）；API-only 工作流的「双向」选项禁用（无布局不可写回）。
- **测试加固**：Rust 新增 6 项测试（`cargo test comfy --lib`）——桥扩展 JS 生成形状（插值/无 format 残留/JS 模板完整）、percent 解码、token 恒定时间比较、真实 tempdir 的「扫描(递归/平铺)→哈希→授权内原子写回（返回哈希=文件真实哈希、临时文件不残留）→越界拒绝→离线报错码」全链路、版本快照落盘往返、workflows 目录探测；生成的桥 JS 已通过 `node --check` 语法验证。

### Comfy 工作流无感同步插件 M3（同步桥）+ 同步中心收尾补全

- **一键安装 ComfyUI 同步桥**（规格 §4.3/M3，Rust `comfy_bridge.rs`）：同步中心顶栏「装同步桥」→ 选 ComfyUI 目录 → 自动生成 `custom_nodes/momo_sync_bridge/` 前端扩展（候选端口 39871-39875 与持久令牌嵌入 JS，MOMO 重启换端口扩展自愈），重启 ComfyUI 生效。
- **保存即时同步**：桥扩展捕获 ComfyUI 保存动作即时通知 MOMO，跳过防抖立即对账——本地与网络共享目录都不再依赖复扫间隔（NFR-002 <3s）。
- **在 ComfyUI 中打开指定工作流**：MOMO 把工作流放进待打开队列并打开 ComfyUI，桥扩展 2 秒内自动载入画布（未装桥时退化为打开 ComfyUI 首页并提示）。
- **同步中心收尾**：来源离线时工作流标记「来源离线」（恢复后自动收敛）；源删除的工作流支持「重新关联」（指定来源目录内新文件，校验路径后原地接续）与「保留本地副本」；来源设置补齐新增默认模式、忽略规则（逐行 glob）、允许写回授权；工作流表新增按模式过滤、上次同步时间（悬停含文件修改时间）、批量暂停；关联模板被删后可重新「关联现有模板」（旧模板迁移入口）。
- 桥为环回 HTTP 服务：只监听 127.0.0.1、恒定时间令牌比较、CORS 头、不读请求体；随同步引擎启停，应用退出清理。

### Comfy 工作流无感同步插件 M2（安全双向写回）+ 差异/依赖/孤儿参数补全

在 M1 之上完成规格 v1.0 的 M2 里程碑与 FR-009/014/016 补全：

- **双向写回（FR-010/§11.3）**：同步中心工作流行可将模式切为「双向」（确认后授权来源写回）。在 MOMO 模板里保存参数改动（模板编辑器的 Widget 值）即写回 ComfyUI 源文件——按 `widgetLayoutOf` 位序最小补丁 `widgets_values`，图结构永远以 ComfyUI 源为权威；写回经 Rust 同目录临时文件**原子替换**，写前 canonicalize 校验目标在授权来源目录内（防路径越界）。
- **自身写入抑制（§11.4）**：写回后记录「路径 + 写后真实内容哈希」令牌（TTL 5 分钟），文件监听事件匹配令牌即消费忽略，不把自己写的文件当外部修改再同步一遍。
- **字段级三方合并与冲突（FR-015）**：写回前若源文件也改了同一参数且值不同，绝不静默覆盖——存冲突记录（基线取版本快照，正文不落索引），同步中心冲突区按字段列出 ComfyUI 值 / MOMO 值，支持「使用 ComfyUI 版本 / 使用 MOMO 值 / 另存为新文件 / 稍后处理」。参数默认值同步规则同步升级：用户没改过的参数跟随源文件新值，改过的保留为待写回补丁（`baseParamValues` 作为双方共同基线）。
- **双向节点稳定 ID（§10.3）**：首次写回为每个节点注入 `properties.momoSyncNodeId`（保留原有全部属性）。
- **差异查看（FR-014）**：工作流行「MOMO ↔ 源文件」、版本历史「该版本 ↔ 当前」，分类列出节点增删/参数变化（object_info 在线时映射参数名）/连线增删/位置尺寸/分组/顶层扩展字段。
- **依赖检查（FR-016）**：派生时标记缺失自定义节点与疑似缺失的模型引用（仅警告，不阻塞同步与版本保存）。
- **孤儿参数提示（FR-009）**：工作流升级后画布实例里已失效的参数覆盖值在 Comfy 参数面板灰显列出（值保留、可单条清除），不再无声失效。
- **顺带修复**：模板编辑器保存同步模板时丢失 `workflowId` 关联的问题；`widgets_values` 位序规则提取为无依赖纯模块 `widgetLayout.ts`（frontendConvert re-export，读回/写回/转换三处同源）。单元测试扩至 74 项（新增补丁位序/冲突检测/稳定 ID/差异计算）。

### Comfy 工作流无感同步插件 M1（ComfyUI 主控单向同步）

对照《MOMO ComfyUI 工作流无感同步插件 开发规格 v1.0》完成 M1 里程碑：一次配置 ComfyUI 工作流目录，之后 ComfyUI 里新建/保存/改名工作流，MOMO 自动同步——不再需要手工导出 API JSON、重新导入、重新配置参数。

- **完整 UI Workflow 成为同步主数据**：原样入库 `<AppData>/comfy-sync/`（Rust 侧原子写入），布局/分组/子图/未知字段不再因 API Prompt 往返丢失；API Prompt 只作派生缓存（ComfyUI 在线时自动转换进画布模板，离线标「待派生」、上线自动补）。
- **全局同步服务**：Rust notify 目录监听（新依赖 `notify`）+ 事件 100ms 聚合 + 前端 750ms 防抖 + 本地 5 分钟/网络 60 秒周期复扫，替代旧「每模板 2 秒轮询单文件」的往返编辑会话（旧入口对未同步模板保留）。
- **身份与改名/移动识别**：graphId > 语义哈希 > 结构指纹一对一配对，文件改名/移动不产生重复模板；路径比较统一小写 + `/` 规范化。
- **版本历史与恢复**：每次内容变化自动留档（最近 20 个 + 7 天内 + 用户标记保留），同步中心可查看/恢复/固定版本；恢复只回滚 MOMO 本地，不写源文件。
- **实例参数保护**：画布节点的参数覆盖值与工作流定义彻底分离，模板升级只更新定义默认值、剔除失效参数定义，实例覆盖值原样保留；default 分支节点集跟随源文件更新，自定义分支只剔除失效节点。
- **安全默认**：默认 comfy_master（对源目录只读，绝不写回）；坏 JSON / 半写文件按 300/600/1200ms 重试后标 invalid，旧版本继续可用绝不覆盖；来源离线指数退避复扫，绝不误判删除；删除来源/源文件均保留 MOMO 数据。
- **同步中心 UI**：标题栏新入口（状态点：灰未配置/绿正常/蓝有新/黄警告/红失败）；来源管理（多来源、本机/移动盘/映射盘/UNC、自动探测 workflows 目录、子目录/自动跟踪/暂停）、工作流表（勾选入库、搜索、状态/来源过滤、立即同步、打开目录、解除关联、保留本地副本）、版本历史、同步事件记录。
- **设置开关** `settings.comfy.syncV2Enabled`（默认开）：关闭即完全回退旧手工导入/往返编辑路径，已同步数据不受影响；同步派生的模板在模板管理显示「同步」角标并提供同步中心入口。

### 导演台 · UI 规范审查与全流程漏洞修复

- **UI 规范合规**（对照 AGENTS.md UI 精致度规范）：核查无原生 `<select>`、无硬编码品牌色光晕、无裸 `.hint`；**24 处 PopSelect 选项全部补齐「图标 + 文字」**（新增 `studio/shared/selectIcons.tsx` 语义图标映射，全部复用手绘 SVG；状态筛选/引擎/配方/转场/旋转/画幅/张数/文档状态/参考图分类/生效范围等，关键下拉带 `triggerIcon` 当前项图标常显）；提案卡/Skill 绑定卡/历史卡的 20–22px 微型图标按钮统一提到 24px 热区。
- **全流程链路自查修复**：① 时长钳制——未配配方（远程默认模型）不再被 generic 兜底静默压到 12s，交预检提示；② 剧本「全量替换」后立即 `rebuildTimeline`，防持久 timeline 悬空引用旧 segmentId；③ H3 检查器的 ComfyUI 模板读取改订阅式（模板增删/改名即时刷新参考支持度与能力档案）；④ H3 监看控制条「生成」按钮在无片段时禁用并给指引；⑤ AI 导演右栏补回**外部 Agent 提案渲染卡**（3.2 重写时只留了计数——现在外部提案有差异预览/应用/驳回/定位，应用仍走受控应用器，锁定最终稿不受影响）。
- 提示性文案核查：空态文案与实际状态一致（冷启动引导 vs 筛选无结果分叉）、时长钳制说明依据、计费确认与取消语义、能力档案提示均带 title 悬停说明。

### 导演台 3.3 · P1–P4 补全（Skill 编排闭环 / 官方协议适配器 / LTX / 模型路由）

在 P0 纠偏之上完成 3.3 方案的全部剩余阶段。

**P1 · Skill 编排闭环**

- **阶段化硬规则落地**：生成栈**不再把任何 Skill instructions 原文拼进最终提示词**（§7）——指令只属于「写提示词的 LLM」阶段（精炼），视频模型只收阶段产物；未精炼段直发自动编译结果，想上模型方言走「Skill 精炼提示词」。Take 仍记录当轮生效的 Skill 栈快照（可追溯）。
- **validate 阶段**（`validateH3PromptForMode`）：精炼产物按模式合同校验——Ref2VA 六段式完整、I2V/FL2V 首帧对齐、L2V 尾帧演进；只报告不静默改写。
- **阶段产物快照**：片段记录 `refinedBy`（哪版 Skill、按什么模式合同、何时精炼），Take 链路可追溯。
- **H3 项目包导出**（`directorPackageExport.ts` + H3 批量菜单）：导出 h3-script-package 规范目录——完整剧本.md · 全部素材（MOMO_ASSET_CATALOG_V1 资产册 + 图片拷贝）· 分段资产库（每段提示词 + 参考图）· 分段剧本-中（`## H3-XX` + `<<<PROMPT_START>>>` 围栏，可直接三态直录回导）· 分段剧本-英（可选 LLM 翻译，保持围栏与原语言对白）；导出前跑包校验（围栏配对/空提示词/资产编号稳定/空占位目录检测），问题写入目录内校验报告，error 级阻止导出。

**P2 · 远程官方适配器**（`videoAdapters.ts`，协议下拉新增三项）

- **火山方舟 Seedance**（`ark`）：Ark v3 `contents/generations/tasks`——content[] 多模态（文本/参考图/尾帧 role/参考视频）、整数 4–15s、ratio/resolution/seed/原生音频、`return_last_frame`；`asset://` 可信素材与肖像授权错误给中文指引；DELETE 取消。
- **阿里云 DashScope Wan 2.7**（`dashscope`）：`X-DashScope-Async` 异步任务——img_url / first+last_frame / `first_clip` 延展 / `driving_audio` 音频驱动 / `prompt_extend` / 水印 / 种子 / 720P·1080P；`tasks/{id}/cancel` 取消。
- **Google Gemini/Veo**（`google`）：`predictLongRunning` operations 轮询——instances[].image（base64）、aspectRatio/negativePrompt/durationSeconds、各代响应结构宽容解析、Files API 取回视频；operation 取消。
- **本地中止 → 远程取消联动**（§9.5）：三家适配器在本地停止时尽力调用各自取消端点，不支持或失败时明确提示「已提交费用可能发生」。
- **导演队列素材贯通**（§2.5）：远程分支补传参考视频（`video`）、参考音频（`refAudio`）与原生音频开关（`audio`，按配方能力快照）——`VideoGenReq` 预留字段首次真正进链路；新增 `seed/negative` 字段供官方协议消费。
- **诚实标注**：适配器代码已实现但未经真实 Key 联调，能力档案标 `adapterReady`——配方下拉显示「官方适配器待联调」而非「已贯通」；联调后翻转标记即可。

**P3 · LTX**

- 内置「LTX 视频提示词方言」Skill（model-adapter / compile-video-prompt）：音画联合自然语言、多关键帧过渡、延展承接、V2V「same camera, same framing」、LoRA 触发词、能力边界交给模板档案——不复用 H3 六段式。导入真实 LTX 模板后由配方节点分析（P0 已修的判定）+ 家族档案自动接通，无猜测性参数面板。

**P4 · 模型路由建议**

- AI 导演上下文新增「可用配方与能力档案」层（真实状态：通道/模式/时长上限/音画/适配器联调状态）；新增第九个导演任务「模型路由建议」——按片段素材构成与时长需求建议通道（H3 各模式/LTX/Seedance/Wan/Veo），产出 plan 与费用影响，不自动切换模型、不发起计费任务。

### 导演台 3.3 · 双通道生成体系 P0（能力档案 / 模式补全 / Skill 职能分流 / 修配方误判）

按《MOMO导演台3.3-双通道生成体系与Skill编排补全方案》完成 P0 全部六项纠偏（只修错误不加功能）。

- **模型能力档案**（`studio/capabilityProfile.ts`）：内置 H3（2–15s/2K/全模态/微参考，Skill 文案里的 18s 是旧值）、LTX、Seedance（整数 4–15s）、Wan（2–15s/首尾帧/音频驱动/延展）、Gemini Omni、Veo、generic 七族档案；`profileForRecipe`（家族推荐→配方快照覆盖→内置兜底）与 `clampDuration`（时长按档案钳制并提示）成为时长/边界唯一来源；官方协议未贯通的远程家族标 `officialAdapter:false`，配方下拉显示「通用协议（官方适配待接入）」——填模型名不再被误解为官方 API 已贯通。
- **配方模式补全**：mode 枚举补 `l2v`（仅尾帧反推）/`audio2v`/`extend`/`edit`；模式文案表 `MODE_LABEL` 单一来源。
- **修复「添加 H3 配方」误判**：按模板真实节点判职能——T2VA（无媒体入口但确是视频）→ t2v；文生图/无任何媒体入口且非 T2VA → **image 配方**（此前「MiniMax H3文生图」会被误标成 fl2v 视频配方）；REF2VA/含视频入口 → r2v；L2VA → l2v。
- **H3 提示词合同按模式分流**（`h3ContractFor`）：**六段式只属于 Ref2VA**；T2VA/I2VA/FL2VA/L2VA 用三段式基础合同 + 各自对齐节（首帧延续/首尾帧精确到达/尾帧演进）；`refineSegmentPrompts` 按每段配方模式现场选合同（此前所有模式都被套六段式，3.3 §2.6）。
- **Skill 职能分流**（§2.2 互相污染修复，双向）：新增 `SkillPurpose`（script-plan / segment-authoring / compile-video-prompt / compile-image-prompt / project-package / validate，frontmatter 可声明 `purpose`，旧数据按名字/指令启发式推断）；**生成提示词栈排除拆分/项目包/校验类**（「剧本拆分」Skill 的规范不再被拼进最终提示词）；**拆分与精读链只吃规划类**（提示词方言 Skill 不再进拆分 system）；绑定卡显示职能徽标（如「剧本规划（只进拆分链路）」）。
- **时长钳制进检查器**：H3 片段时长输入按当前配方能力档案钳制（如 H3 ≤15s、Seedance 整数步进），越界 toast 说明依据。

### 导演台 3.2 · AI 导演智能化纠偏（项目级理解 / 诊断 / 结构化提案）

按《MOMO导演台3.2-智能职能深化与通用能力复用方案》完成 P1 全部 + P2/P3/P4 关键项：AI 导演从「项目聊天框」升级为真正的项目智能——**它不聊单张图，只做整部片的导演决策；自由文本永远不直接写项目**。

**AI 导演智能内核**（`core/studio/directorAgent.ts`）

- **分层项目上下文**（§3.1）：第 1 层项目导演摘要（目标/风格/角色表/进度/失败统计/剧本节选）常驻；第 2 层当前选中片段全量（镜头/对白/承接/槽位/配方/Take/质检/接力胶囊/相邻段）；第 3 层确定性分析结果——`checkContinuity`、`projectProgress`、`collectBatchTasks`、`qualityReports`、`precheckBatch` 全部注入，LLM 只组织不重算、禁止虚构系统状态（§3.2）。
- **结构化输出契约**（§3.3）：每轮要求 `DirectorResponse` JSON——reply + findings（证据级问题）+ proposals（对象级 patch 提案）+ plan（交给专业工位的执行计划）+ questions（决策卡）；解析失败自动降级为纯回复，降级结果没有任何写入口径。
- **会话持久化与压缩**（P1-1）：AI 导演会话存 `project.directorSession`（与创作助手完全隔离，§5.2），切工位/重启不丢；超阈值用 LLM 压缩前情摘要（失败退化截断），历史回灌只带 reply 不带卡片。
- **对象级提案应用器**：segment（摘要/时长/对白/承接）、shot（镜头修订）、prompt（提示词覆盖，**锁定最终稿永不被覆盖**）、characterState（角色档案 rev+1 触发下游过期提示）、scene、script（存剧本库新版本）；slot/recipe/timeline 为建议性提案只到对应工位执行。每条提案自动计算影响面（失效片段/过期 Take/需重建微参考，§8.2），应用/驳回记录持久审计。
- **八个导演任务**（§6.2）：全项目体检 / 剧本结构复盘 / 当前场景导演 / 片段连续性复核 / **选中 Take 视觉审片** / 生成前风险检查（预检结果注入）/ 生产顺序与试跑计划 / 成片节奏与交付复盘——替换原来的五个泛化生成按钮。
- **视觉审片最小实现**（P4）：抽取当前段采用 Take 首/中/尾帧 + 上一段尾帧，经 vision 模型判断角色外观/空间轴线/光线/动作连续性，产出 findings 与提案、不自动重生；模型无 vision 能力时明确报错指引。

**AI 导演工位重做**（§6 三栏）

- 左「导演记忆与任务」：八个导演任务 + 待执行计划（含「去 H3 定位」）+ 历史决策审计；中「对话与提案」：流式渲染 + 中断 + findings/proposals/plan/questions 分类卡片；右「项目诊断」：确定性检查的实时投影（叙事/连续性/引擎/质量/交付分组，点击定位片段）+ 待审提案池（外部 Agent 提案同池展示）。
- 提案卡：标题/理由/依据列表/patch 预览/影响面统计（N 片段·N Take·N 微参考）/采用/驳回/去执行——「采用」走对象级应用器，「去执行」跳 H3 真实片段。

**剧本库「送入项目」真差异**（P1-5，§4.2）

- 替换模式不再先写 `project.script` 再提醒：**先 dry-run 拆分预演**（三态识别：直录包/规则切段/LLM），弹出双栏差异卡（当前项目全部场景片段 vs 新草案），列明影响（旧片段移出、已采用 Take 资产保留、微参考胶囊失效）；提供「全量替换 / **追加为新场景（无损）** / 取消」三选。

**通用能力复用（P2/P3，§5）**

- AI 导演对话改走通用 `chatStream`（流式/中断/视觉输入/本地 GGUF 适配全复用，§8.3 不同工位不同模型同一传输层）；制图与 MV 的运行失败统一进报错中心（pushError，§5.1），任务进度仍归任务中心。

### 导演台 3.0 · MOMO AI 制片工作站（全链路重构）

按《MOMO导演台3.0-全链路制片工作站重构方案》完成产品架构重构：不再修补旧四页，导演台重构为**一壳七工位**的独立制片工作站（`src/modules/studio/`）。执行层（runBatch / 参考槽 / Take / 空间接力 / ComfyUI 模板 / FFmpeg 渲染 / 项目包）全部保留接入，只重做产品组织方式。

**新壳与视觉系统**

- 左侧一级导航改为专业工位（不编号、随时往返）：**AI 导演 / 剧本库 / 角色库 / AI 制图 / H3 导演台（主工位）/ AI MV / 成片交付**；底部共享工具：3D 预演 / 资产库 / 任务中心 / 引擎状态。
- 顶栏只保项目名、面包屑（项目/场景/片段）、保存与引擎指示灯、任务条、导出、关闭——画幅/时长/像素/配方/快速专业开关全部移入检查器与项目设置。
- **Chrome / Stage 分层**：新增 `--studio-*` 三主题语义令牌（云白/深空蓝/深邃黑同构映射）；监看器、时间线、波形、3D 视口为连续暗色 Stage，浅色主题下不再是白卡里的孤立黑块。平面停靠面板 + 1px 分隔线，退役 Bento 卡片墙；面板可拖动调宽并持久化。
- 退役：四阶段导航（planning/directing/post/threed）、全局快速/专业模式、驾驶舱/详细视图双入口、ScriptPage 大文本框、EditingPage 卡片仪表盘（旧文件已删除；数据与字段保留在存档中不再读取）。

**数据模型 v3**（`schemaVersion: 3`，迁移幂等）

- `StudioProject` 挂载新实体：`scripts`（剧本库文档+版本）、`mvProjects`（AI MV 项目）、`imageStudio`（制图历史）、`studioUi`（工位态）；`ContinuityCapsule` 扩展 22 帧微参考追溯字段（帧数/区间/fps/指纹/引擎）；`DirectorCharacter` 升级为角色实体（身份/外貌锚点/服装组/分类参考图/声音/rev 修订/锁定身份）；`PostClipOverride` 增加镜像/旋转。v2 项目自动升级（正文转首个剧本库文档）。

**H3 导演台主工位**

- 三栏：片段树（场景分组/缩略图/九态徽标/搜索/状态筛选/批量补缺）│ 暗色制作舞台（监看器顺位：采用 Take > 最新成功 > 分镜图 > 明确空态，生成中不闪空；Take 条含耗时与采用；**22 帧微参考条**；本段音频条）│ 检查器四稳定分组（导演描述/提示词·实际请求预览与差异/角色与参考/生成）。
- **22 帧微视频参考闭环**：采用上一段 Take 后自动提取末 22 帧微视频（Rust/FFmpeg `extract_micro_reference`，浏览器降级 Web 截取），按指纹复用不重复落盘；胶囊记录来源「段 XX / Take N / 末 22 帧」，上游重新采用自动标过期，工位内可预览/重取/重建；绑定下一段 Video 槽进真实投喂。
- Rust 媒体服务新增：`extract_bridge_frame`（稳定尾帧）、`extract_thumbnail`、`trim_media`；`media_render` 支持水平/垂直镜像与旋转，真实进入 MP4。

**AI 导演 / 剧本库 / 角色库**

- AI 导演升级一级工位：对话画布 + 项目上下文注入 + 五个快捷方案（梗概/角色/场景表/分段/提示词草案）；产出「存为剧本草稿」三动作（新建剧本/另存版本/合并到当前——合并先看双栏差异）。
- 剧本库：文档列表（草稿/正式/归档）+ 版本时间线 + 版本对比与回退（回退=旧版另存新版本）+ 文件导入 + 从项目另存 + 「送入项目」（替换重拆/追加拆分，先提示受影响片段与微参考胶囊）。
- 角色库：角色实体编辑（外貌锚点/服装组/五类分类参考图/声音/TTS 音色）+ 出场片段反向索引（「角色来源已更新」按修订时间判定）+ 锁定身份；修改角色不静默重写提示词，锁定最终稿永不覆盖。
- **外部 Agent 受控网关**（agentGateway）：propose_* 提案池 + 双栏差异确认后应用（剧本进版本库/角色进修订/片段与提示词受控写入），AI 写入全程可审计。

**AI 制图 / AI MV / 成片交付 / 3D**

- AI 制图工位：文生图/图生图/编辑图三标签；引擎统一「本地 ComfyUI 配方 ⇄ 远程 Provider」（Seedream 等以 Provider 配置）；画幅/张数/种子；远程计费确认闸；历史（60 条）参数复用；结果自动成组入库，一键绑片段/角色参考/场景参考。
- AI MV 工位：音乐导入 + **Web Audio 分析**（时长/BPM/onset 节拍/能量段落/包络波形）+ LRC 歌词 + 自动/手动视觉区间 + 区间绑定图与角色 + 口型区间标记（未配口型配方时明确提示）+ 逐区间生成与替换（失败不阻断）+ 「送往成片」走正式 ffmpeg 管线（区间拼接 + 音乐混音 + 歌词字幕烧录）。
- 成片交付工位：左轨问题列表（缺片/质量探测/连续性，点击定位片段）+ 监看 Stage + 多轨时间线（V1/T1/D1/N1/S1/A1/M1/SUB 复用）+ 混音器/字幕 + 片段属性（入出点/转场/**镜像/旋转**/原声音量——预演与导出同一份数据）+ 导出（预设/进度/取消/项目包）。
- 3D 预演迁入统一外壳成全屏工位（three.js 按需懒加载），视口与监看器共用 Stage 语义。
- **统一任务中心**：生成/微参考提取/渲染/音乐分析/制图全部进同一份可观察记录（顶栏任务条 + 浮层；planned→done/failed/cancelled 状态机；远程任务注明已提交计费不可撤销）。

**Skill 体系 · 双引擎路由与全工位覆盖（导演台 3.0 增量）**

- **引擎路由**（`skillRoute.ts` 唯一判定处）：Skill 绑定新增「生效范围」（全部引擎 / 仅本地 ComfyUI（H3/LTX）/ 仅外调远程（Kling/Veo/Claude/Gemini））与「模型/模板关键词」（如 `H3,LTX`，逗号分隔命中即生效）。同一项目混用两条路线时各吃各的 Skill 栈——H3 六段式不再被发给外调模型，外调规范也不污染本地工作流；H3 提示词编译（compileSegmentPrompt）按当前片段配方实时路由，Take 快照照常追溯。
- **新工位 Skill 扩展点**：`studio.director`（AI 导演对话规范注入 system）、`studio.image`（AI 制图提示词注入）、`studio.mv`（MV 区间提示词注入，按区间引擎路由）；contexts 合法表收敛为 `SKILL_CONTEXTS` 单一来源（导入校验/管理器标签/绑定卡筛选共用）。
- **项目级 Skill 绑定卡回归 3.0**（旧分镜页入口退役后）：H3 检查器新增「Skill 栈」分组——绑定/启停/生效范围/关键词/变量编辑一体；AI 导演、制图、MV 工位头部显示「Skill ×N」生效徽标，点击直达绑定卡。
- **内置「外调视频提示词规范」Skill**（model-adapter 阶段，默认可用、按需绑定）：Kling/Veo/Gemini 等第三方视频 API 的段落式提示词写法——单段叙事、通用运镜词汇、禁止 `<Picture N>`/六段式等本地工作流语法，与 H3 Skill 形成对位组合。

**3.0 补全（本地 ComfyUI 主线回归 + 冷启动引导）**

- **空项目冷启动引导**：H3 工位三处空态分叉——监看舞台显示「从故事开始你的影片」+ 直达 AI 导演/剧本库按钮；片段树与检查器区分「项目没有片段（带跳转）」与「筛选无结果/未选中」，不再误导。
- **剧本库「送入项目」三态识别**：正文先过 `detectScriptKind` 分流——成品提示词包直录（promptOverride + 锁定 + 全局风格锚定）/ 已分段脚本规则切段（原文留档供精读）/ 完整剧本 LLM 拆分；追加模式按名字去重并入角色。
- **H3 批量工具回归**（片段树头部「批量」菜单，本地 ComfyUI 主线）：重跑失败 / Skill 精炼提示词 / **AI 精读分段**（规则切段产物逐段提取结构）/ **双语资产册绑定**（资产册导入从旧分镜页迁入 core：Tauri 目录选择 + 浏览器 webkitdirectory 降级，指纹去重、按使用分段绑定、重导原位同步）/ 高清放大（模板下拉 + `upscaleParams` 覆盖）/ ComfyUI 设置与模板管理器直达。
- **ComfyUI 配置入口接进新壳**：导航底部「引擎状态」更名为「ComfyUI」，一键打开设置 → ComfyUI 页（服务地址/模板管理/显存清理）；检查器生成组加「管理模板」；`.modal-mask` 层级 200→470（高于导演台 450/资产库 460、低于 toast 500），从制片工作站打开设置与模板管理器不再被全屏层盖住。
- **项目风格与负向规则回归**：H3 检查器「生成」组直接编辑风格锚定（编译时自动前置）与负向规则（去字幕/水印/文字/背景音乐开关 + 自定义每行一条）。
- **长列表性能**：片段树行与 Take 卡 `content-visibility: auto`（视口外跳过渲染布局，50+ 片段滚动不掉帧，§16）。

### Eagle 桥 · 体检修复与无感化（第二轮）

- **修复增量扫描漏检（关键）**：实测 Eagle 改名/评分只前移 `lastModified`、文件替换只动 `modificationTime`——扫描游标改为两者取大，双向模式现在能真正检测到 Eagle 端的任何修改并自动回流新版本（原版本保留）。
- **修复库文件定位**：Eagle 4 实际布局是 `images/{id}.info/{原始文件名}`（此前按直存路径拼，字体等类型导入必败）。新增 `eagle_locate_item_file` 在 `.info` 目录挑主文件（排除 metadata/缩略图取最大者，旧布局兜底），缩略图端点同步改为 itemId 定位，前端不再经手路径。
- **修复插件「发送到画布」重复复制**：对已在库且无远端变化的素材，现在直接复用现有资产放画布，不再错误地生成重复版本。
- **修复推送死文件**：磁盘文件已丢失的资产推送前被拦截，给中文结论并出队，不再把死路径发给 Eagle 报原生 ENOENT。
- **简化设置为无感三步**：启用 → 选同步方式 → 选根文件夹；自动推送/自动拉取由同步方式派生，不再暴露独立开关。
- **流畅性**：增量扫描从全库分页（6 千项 7 个请求/轮）改为只查已绑定 id（1 个请求）；推送队列两路并发；浏览页限定根文件夹不再扫全库；Eagle 文件夹层级简化为「根/画布名/文件」两层。
- **补齐**：插件「在 MOMO 中定位」事件落地（打开资产库并预览目标资产）；Eagle 来源资产在预览层正确显示「来自 Eagle」；导入后浏览卡按钮即时变「已在库」。
- **验证**：新增 Rust 单测 4 项（库文件定位，含大小写兜底修复）；`scripts/eagle-e2e-sim.mjs` 对真实 Eagle 跑通 16 项多流程模拟（收录/存储/加工/回读/游标/幂等/非破坏删除），可重复执行（每轮生成唯一内容素材，规避 Eagle 内容指纹查重）。

### MOMO × Eagle 资产桥（新功能）

- **双向资产桥首版落地**：设置页新增「Eagle 资产桥」——连接检测（应用/素材库/版本）、Token 配置（DPAPI 加密落盘）、MOMO 根文件夹自动创建或挑选、手动/自动推送/双向三种同步模式；未启用时不发任何请求，浏览器预览安全降级。
- **Eagle Web API V2 适配层**（`eagleApi.ts`）：端点全部以本机 Eagle 4.0 Build 20260401 实测收口（item/get·query·add·update、folder/create·update、library/info、countAll、aiSearch*）；素材文件路径按 `{库}/images/{id}.{ext}` 推导（V2 raw 不带 filePath）。
- **同步引擎**（`eagleSyncEngine.ts`）：资产生成完成只异步入队不等 Eagle；同一资产重复同步幂等（已绑定仅同步元数据，绝不重复收录——实测 Eagle 端对同一文件重复 add 会生成新条目，幂等必须靠本地 itemId 记忆）；增量扫描只拉 `id+modificationTime` 轻量列表并带防回声窗口与指数退避；删除一律只解除绑定。
- **Rust 文件层**（`eagle_bridge.rs`）：大视频流式复制 + 流式 SHA-256 指纹直接落 AppData/assets，WebView 内存不随文件大小增长；`.part` 临时文件校验后原子改名。
- **本地桥服务**（`eagle_plugin_server.rs`）：127.0.0.1 随机端口 + token 恒定时间比较 + 白名单动作；Eagle 库内缩略图走环回流式端点（路径前缀与扩展名白名单双重校验）。
- **资产库集成**：卡片右上角 Eagle 同步状态角标，右键菜单「同步到 Eagle / 从 Eagle 导入新版 / 在 Eagle 中打开 / 解除绑定」（eagle://item 深链），多选栏批量同步；侧栏新增「Eagle 素材库」远程浏览页签（分页搜索、双击深链打开、按需导入后才能拖用，不自动占盘）。
- **Eagle 插件套件**（`integrations/eagle/`）：后台服务插件读桥描述自动发现 MOMO、把选中素材 itemId 发送到 MOMO（可选拷入画布）；Inspector 面板显示选中素材操作入口。插件只发 itemId，本地路径由 MOMO 向 Eagle API 复查。
- **设置结构 v3 → v4**：新增 `eagle` 配置节，老配置加载后自动补默认值并写回 v4。

### 资产库

- **修复组图无法拖出单张的问题**：此前点开组卡（如 H5 长图切片组）后的成员卡只是普通按钮，不支持任何拖拽。现在组展开面板的每一张都支持整卡拖拽——与主网格单卡同一条链路：Tauri 下走 OS 原生拖出（画布 / 右侧快捷栏 / 资源管理器 / 第三方软件通吃），浏览器预览退 HTML5 dataTransfer；点击仍进大图预览、右键仍是完整菜单。

- 便携版启动时自动检查桌面「MOMO 智能画布」快捷方式：缺失则创建，旧目标则覆盖为当前解压目录；移除跨版本的一次性标记，避免新版启动后桌面仍打开旧包。
- 便携包同步收录发行目录的运行 DLL（含 DirectML），与安装版保持一致；构建说明包含本次尚未发布的修复，避免同版本本地重打包遗漏变更记录。

- 修复创作助手顶部三类模型连续选择时异步偏好写入互相覆盖的问题；执行轨迹与生成确认卡现会显示服务商和具体模型，三栏补充「对话 / 绘画 / 视频」角色标签。
- 创作助手选用 MiniMax 官方对话模型时，联网动作改为复用当前模型服务商与密钥调用 MiniMax Coding Plan 搜索，不再误转到未配置的智谱搜索；失败时明确提示 Token Plan/积分权限要求。

### 创作助手·关键修复

- **修复 Anthropic 协议联网能力误判**：MiniMax Anthropic 支持普通工具调用，但官方当前将 Web Search 作为独立 Coding Plan/MCP 能力，`mcp_servers` 参数也不生效，并非 Anthropic 服务端 `web_search_20250305`。现不再给 MiniMax 注入错误的服务端工具，官方服务地址改走其 Coding Plan 搜索接口；兼容解析模型偶发输出的 `web_search` 动作与 MiniMax 工具标签，原始协议不再泄漏到聊天气泡。GLM Anthropic 的服务端搜索保持不变。
- **Agent 联网失败自动降级**：联网 tools 被服务端拒绝（端点不支持/中转站剥参数）时自动按无联网模式重发一次，任务不中断，本轮内不再反复白试。
- **search 动作硬拦截**：模型自带联网开启期间，search 动作（外部搜索接口）直接拒绝执行并劝返模型——提示词劝不住所有模型，必须程序硬拦，否则会去调未配置的外部接口反复失败卡死任务；外部搜索失败一次后本会话不再执行 search。
- **修复确认生成死循环**：此前用户点「确认生成」后系统要求模型「原样重发」动作，但确认签名含提示词全文、模型复现几乎必然有措辞差异，导致反复弹确认无法进入生成。现改为确认通过后**程序当场执行**该动作，不再回炉模型；「再改改」行为不变。
- **修复模型口头锁定方案却不生成**：部分模型会绕过程序确认卡，用普通回复说「方案已锁定 / 确认后自动生成」，第二次创作因此停在文字层。现由对话模型基于完整上下文做一次语义动作审计：讨论与继续修改仍可 reply/ask；要求制作、按修改方案重做或立即执行时必须返回真正的 image/video 动作，再进入程序确认闸与画布生成链路。识别不依赖固定口令，语音转写和自然表达同样适用。
- **自带联网真正生效**：对确实支持一次请求内返回搜索结果的模型，注入 tools 的同时在系统提示里告知「已启用内置联网、优先自行检索」；其余模型走 MOMO 内置搜索接口。联网开关默认开启（显式关过的用户不受影响）。
- **视觉判定按真实模型收紧**：MiniMax-M3 按官方能力支持图片/视频输入，M2.x 不再因使用 Anthropic 协议就被误判为视觉模型；kimi / moonshot 与其他明确视觉家族保持原判定。
- **持续对话强化**：Agent 模式接入前情摘要（与聊天模式共用，长任务不断片）；已交付内容回注提示词（前 80 字），「把刚才那张改成蓝色」这类后续修改不再失忆。
- **后续修改与首轮统一闭环**：每轮都按「按需搜索 → 理解/思考 → 完整提示词确认卡 → 生成或再想想」推进；视觉对话模型会重新看到最近成图，绘画模型也会收到最近成图作为图生图参考，不再只靠文字摘要猜上一张画面。
- 聊天模式联网搜索失败的提示降为普通通知（不再红色报错），失败自动直接回答的行为不变。

## v0.7.0（2026-08-26）

### 系统资源监控（画布仪表盘）

- **画布右上角仪表盘**：CPU / 内存 / GPU / 显存实时环形仪表，默认收起为一个低透明度小圆钮（不轮询、零开销），点击展开后 2s 轮询，点画布其他位置自动收回；沉浸模式不渲染，浏览器预览不挂载。
- **Rust 侧 `sysmon`**：CPU、内存走 sysinfo（new 时预热一次采样）；GPU 走 NVML 运行时加载 nvml.dll——非 N 卡 / 驱动异常全部字段降级显示「--」，绝不影响画布。

### 设置页全面重构

- **巨型弹窗拆分**：原 2500 余行的 SettingsDialog 拆成 10 个独立 tab 文件（`settings/tabs/`：模型 / 协议 / ComfyUI / 外观 / 快捷键 / 保存 / 搜索 / 声音 / 用量 / 关于），统一 `.set-page > .set-card` 骨架与 `settings.css` 样式层。
- **共享组件**：`shared.tsx` 提供 SecHelp 折叠说明等；提示文字统一 `.set-hint`。
- **模型配置导入导出**（cfgIO）：模型服务商配置一键导出 JSON / 导入恢复（Tauri 存盘对话框，浏览器预览退回下载）。

### 导演台·素材与接力

- **双语资产册绑定（推荐）**：分镜页可绑定项目根目录或「全部素材」，读取 `MOMO_ASSET_CATALOG_V1` 的《资产提示词.md》——每项 Markdown 图片 + 中文提示词 + English Prompt + 可选使用分段/参考顺序（支持 `01=2, 03=4` 分段映射）；图片按内容指纹只落库一次，重导按 catalogId 原位同步并刷新双语提示词与空间锁元数据；与「文件夹绑定（备用）」二选一。
- **配方参考能力实时判定**（directorRecipeSupport）：按模板真实节点判定五类参考入口（首帧/尾帧/参考图/视频/音频）的开放性；不支持的类别在分镜卡上保留素材但整体置灰、不参与生成与编号；图片容量不足时优先保留真实素材。
- **空间接力增强**（directorQueue）：尾帧接力槽持久化进下一段参考槽（分镜卡可见、可删可拖，auto:false 防同步对账误清）；桥接帧取结尾前约 0.25 秒的稳定帧防黑帧；已有同源接力直接复用不再重复截取；接力策略 opening / hard_cut 阻止跨场景误桥接；关闭开关时槽位和资产保留但不再投喂。
- **参考槽拖拽排序增强**：同语义多槽（如接力自动填入的独立槽）整体过一遍摘插，跨卡拖拽目标格前插入/尾部追加/单例区替换。

### 创作助手

- **画幅/清晰度兜底强化**：用户写像素尺寸（1920×1080、1080p）时自动折算最接近的常用比例 + 按像素量定清晰度档（1080p≈2K）；模型返回的非标准字段（「竖屏」「高清」等）一律先归一化再进兜底链，认不出丢弃走下一级。
- **生成确认闸收紧**：确认判定只认「确认/开始/生成/好的…」等肯定语（含否定词一律视为再改改）；系统提示词同步告知模型收到「用户已确认」反馈时原样重发动作。

## v0.6.0（2026-08-19）

- **剧本三态导入**：完整剧本 LLM 拆分 / 已分段分镜脚本结构化切分 / 成品分段提示词包直录（显式围栏 `<<<PROMPT_START>>>` 一对即一段，启发式后备 H3-XX 头、裸标题行「01-古刹闻客-11秒」等；风格/定调小节提取进规则集不作片段起点，`promptOverride` 标记 locked）。
- **ComfyUI 节点位置双向保留**：前端格式导入存 pos/size，API 格式由 `applyComfyLayout` 拓扑布局补齐；推送回 ComfyUI 优先用存储坐标，Ctrl+S 合并回来回写编辑后位置。
- **widgets_values 位序统一**：`widgetLayoutOf` 成为前端→API 与 API→前端两个方向唯一的取值位序来源（forceInput 不占位、control_after_generate 补 "fixed"）。
- **批量开关/确认卡共享组件**（BatchSwitches / AskCard）、参考槽悬停预览（图放大/视频音频即播、可移入浮层交互）、设置页协议区卡片化与占位符一键复制、3D 页主题令牌映射、combo 参数下拉矫正（数字枚举不再丢下拉）。
- 种子随机化：暴露参数留空即每轮随机，未暴露的 noise_seed/seed 也每轮随机。

## v0.5.0（2026-08-15）

### 导演台（全新项目级工作台）

- **导演台节点**：项目级复合节点上「进入导演台」，打开全屏五页签工作台（脚本 / 分镜 / 生成 / 剪辑 / 3D 站位）。
- **脚本页**：粘贴故事 → AI 两步拆分（剧情分析 + 时长装箱）成场景/片段/镜头；带「分段1 / 第1段 / ### 标题」等标记的外部剧本走确定性切分不花模型钱；优秀提示词可拆解成结构化配方复用。
- **分镜页**：场景折叠列表 + 片段卡（镜头景别/机位/动作/声音、编译后提示词预览、片段级提示词覆盖）。
- **生成页**：配方（引擎/模板/模型/模式）隔离「做什么」与「怎么调」；Take 多版本管理（采用/星标/备注/手动导入）；批量生成缺失/重试失败/生成所选/已修改，可取消。
- **剪辑页**：时间轴从场景 + 采用 Take 派生；串联预演；Premiere FCP7 XML（file:// URL 编码）/ SRT / 对白先行·画面先行双策略配音、分镜表、项目清单导出；连贯性检查（相似度 / 时长 / 缺口）。
- **3D 站位页**：场景平面示意（SVG 摆位 + 相机视角），生成分镜站位参考图。

### MOMO Skill（提示词技能系统）

- **SKILL.md 导入**：Claude 式 Skill 规范（frontmatter + 步骤正文），导入即结构化。
- **上下文绑定**：Skill 声明适用上下文（提示词优化 / 导演项目等），只在对应入口出现。
- **运行时注入**：生成时按项目级勾选拼装 Skill 栈进提示词，快照随 Take 存档可追溯。

### 本地 GGUF 模型（直跑 llama-server）

- **一键添加**：设置 → 「添加本地 GGUF」，选文件自动扫同目录 mmproj，点「添加并测试」即注册 + 启动 + 健康检查，模型直接出现在对话选择器。
- **受控子进程**：Rust 层 `std::process::Command` 逐项传参（无 shell 拼接），只绑 127.0.0.1，端口占用自动顺延，/health 就绪才算启动，180s 超时清理；应用退出停掉自己启动的进程。
- **视觉模型**：带 mmproj 自动识别为视觉模型（能力不靠模型名猜），对话可发图；mmproj 加载失败明确报错不静默降级。
- **引擎管理**：llama-server 路径一次性配置（探测顺序：程序目录 → AppData/tools → 设置指定 → PATH）；运行中模型列表可看状态/端口/日志、一键停止释放显存。
- **显存协调**：ComfyUI / 视频生成等大任务前检测本地模型占用，提示释放后继续（只停 MOMO 自己启动的）。
- **旧流程收编**：「导入 GGUF 到 Ollama」的 Modelfile 流程收进高级设置，Ollama 仍可作为独立服务商连接已安装模型。

### ComfyUI 子工作流分支

- 模板 v2 支持多分支（variants）：一个工作流文件按连通分量拆出可命名/着色的分支独立运行；语义槽绑定向导、能力标识；导入导出 v2 格式兼容 v1。

### 便携版体验

- **首次启动自动创建桌面快捷方式**（COM IShellLink 直写 .lnk，不走 shell；已存在即跳过，安装版不受影响）。

### 安全与其它

- **API Key 落盘加密**：DPAPI 绑定当前 Windows 用户加密存储，拷贝数据目录到他人电脑无法解密。
- 修复导演台全屏层背景透明（`--bg` token 拼写导致与画布文字重叠）等多处样式问题。
- 多轮外部 AI 交叉审查（DeepSeek / GPT / Open Code）报告的问题逐条核实修复：并发启动竞态、spawn 崩溃 180s 空等、重名模型误启动、日志缓冲无上限等。

### 创作助手（取代对话节点）

- **对话节点下线**：不再能新增（旧画布上的仍可运行），功能整体并入右侧「创作助手」侧栏。
- **聊天模式**：多模态对话（可附图/拖入图片）、流式回复 + 思考过程折叠、任意回复一键「在画布生图」。
- **Agent 模式**：JSON 动作协议循环（搜索 / 抉择提问 / 出图 / 出片 / 汇报），结果一键发到画布。
- **模型直选**：面板顶部一行选对话 / 绘画 / 视频模型，选择持久化（`agent-prefs.json`），重启保留。
- **上下文机制**：多轮讨论按已确定的设定延续；最近 10 条原样带，更早的每积 8 条自动压缩成「前情摘要」。
- **拖入与粘贴**：图片 → 参考图附件；视频 → 自动落画布建视频节点；文字 / 文本文件 → 填入输入框。
- **联网**：聊天与 Agent 都可开；模型自带联网（GLM / MiniMax / 混元等）优先走模型自己的搜索，失败自动降级到内置搜索接口。

### 语音

- 新增 **`asr`（语音识别）模型角色**：设置里可配 OpenAI 兼容 `/audio/transcriptions` 的任意模型，本地 FunASR 服务同样兼容。
- **语音输入**：说一句自动转文字填进输入框。
- **语音通话模式**：VAD 自动断句 → 识别 → 助手回复 → TTS 朗读 → 自动续听；朗读时开口即打断（barge-in）；浮层显示音量波形与当前阶段。

### 画布

- **Alt + 拖拽复制**：按住 Alt 拖节点复制一份跟手走，原节点不动；多选后整组复制且保留内部连线；一步撤销。
- **对齐吸附**：拖动时吸附到其他节点的边/中线并画参考线；`A` 键把所选节点强制对齐。
- **并行生成**：同参数并发 N 条（×1/×2/×3），部分失败保留成功结果，失败原因进报错中心。
- **面板参数记忆**：生成面板改过的比例/分辨率/数量/并行等自动记住（`gen-prefs.json`），新建同类节点直接继承。
- **ComfyUI 节点改版**：模板选择、参数调整、上游计数并入底部设置面板，节点上只留结果。
- 节点「上游传入」徽标移进悬浮工具条，弹窗改为按钮正下方弹出。

### 设置与快捷键

- 顶部新增**「默认模型」总览**：对话/绘画/视频/音频/语音识别五类各选一个，一处设定全局生效。
- 新增 **8 个标题栏功能快捷键**：创作助手 `Ctrl+Shift+A`、语音通话 `Ctrl+Shift+V`、角色库 `Ctrl+Shift+C`、报错中心 `Ctrl+Shift+E`、运行日志 `Ctrl+Shift+L`、主题切换 `Ctrl+Shift+T`、新建画布 `Ctrl+Shift+N`、设置 `Ctrl+,`。
- 快捷键设置页改为**两列 + 六组分类**；各分区的长说明收进标题右侧的「?」悬浮提示。
- 画布历史弹窗重做：小卡片、宽度随卡片数自适应（最多 6 列 × 2 行），超出分页，底部页码圆点 + 滚轮 / 拖拽翻页。

### 外观

- 深邃黑主题改为墨蓝岩黑底（参考 Liblib 深色），文字整体降白，长时间看不刺眼。
- 节点内文字统一减重减对比（600~700 → 500 字重 + 次级色），选中态保留强调。
- 20 处硬编码蓝色光晕改为跟随 `--accent` 混色，黑主题不再出现橙边配蓝光。

### 修复（本轮全局排查确认 35 项，节选）

- **生图比例/分辨率静默失效**：中转站以 OpenAI 兼容协议提供 nano-banana / gemini-image 时，`aspect`/`resolution` 无人消费，一律回落 1024×1024 出方图——面板选 9:16 也没用。已在服务层补齐比例→宽高折算，自定义协议补 `{{aspect}}/{{resolution}}/{{quality}}` 占位符。
- **Agent 不问画幅就出图**：模型常漏填动作字段，现按「已确认规格 > 动作字段 > 提示词措辞 > 用户原话」兜底，全没有就强制问一轮再生成。
- **对齐吸附形同虚设**：React Flow 松手时用内部纯指针坐标覆盖了吸附结果，节点原地弹回最多 6px。
- **切换画布丢结果**：任务运行中切走，结果写不回任何地方（已扣费），切回来还被误标「任务中断」。
- **持久化乱序覆盖**：慢的旧快照会盖掉新数据，丢的是整份 `boards.json`。
- **参考图计数漏组**：组节点上游被算成 0，导致创意度滑块消失、视频尾帧开关被锁死但请求里实际发了尾帧。
- **参数记忆被清空**：`genPrefStore` 从未在启动时加载，用户一改参数就把磁盘上的记忆整体覆盖。
- Agent 生成的视频用磁盘绝对路径当播放地址（打包后必定黑屏）；`collectUpstream` 共享 visited 让第二条路径读成空；组内角色卡导致 `@图N` 编号错位；`duplicateNode` 复制组得到空组；视频 `adaptive` 比例被当非法值回落 16:9；深邃黑缺便签配色；设置页 `.gp-lab` 类名不存在导致小标题裸奔。

### 自定义协议模块（多智能体审查确认 23 项，全部修复）

省钱与「别骗用户」两条主线：

- **重复扣费**：结果路径写错时任务其实早已生成成功（钱已扣），自愈却整条重跑再扣一次。改为先用修好的路径**重读同一份响应**，救得回来就直接收货；轮询超时 / 任务失败 / 5xx 这类「已扣费」错误加进跳过名单，不再触发付费重试；并行 ×3 或批量出图时同一协议只允许一份自愈重发，其余复用结果。
- **校准的假阳性**（最危险）：提交响应里的 `status_url`、Replicate 风格的 `urls.get`、网关回显的输入图都会被当成「结果」，于是异步协议被判成同步、`poll` 被静默删掉、`resultPath` 指向轮询地址，最后还盖上「已校准 ✓」。已给结果识别加三道闸（轮询地址键名黑名单、排除回显输入、http 串要"像产物"）。
- **轮询误判完成**：同一套误判还会把 `processing` 当成「完成时的实测状态」写进 `doneValue`，此后每次生成都在第一轮就误判完成。改为「结果 + 状态」双确认，且只有确实靠状态判完成时才允许改写 `doneValue`。
- **原生 TypeError 糊脸**：模型把 `submit.body` 写成对象（最常见的畸形）能一路存盘，最后报 `tpl.replace is not a function`。新增 `protoSpec.validateProto`，生成/保存/校准/自愈/一键修复五个入口共用，能自动纠的自动纠，纠不了的给中文说明。
- **占位符清单漂移**：协议助手与自愈的提示词只列了图片类占位符，AI 生成的视频/音频协议天然接不上面板的时长/分辨率/比例/音色。清单收进 `protoSpec.ts` 一处维护。
- **提示词转义**：视频/音频链路只转义了 `"` 和 `\n`，提示词里一个反斜杠就让请求体变成非法 JSON（400），还会白触发一次自愈。
- **结果落地**：过期/需鉴权的结果直链返回的 HTML 错误页会被当成图片收进资产库（节点显示成功、图永远加载不出）；相对路径 `/files/x.png` 则被静默丢弃并误报「resultPath 写错」。两头都补了。
- **其它**：`request_id` 抢在 `data.task_id` 前面被当成任务 ID；补出来的轮询配置写死 Bearer 鉴权（非 Bearer 站点必 401）；校准轮询一次网络抖动就作废整次付费测试；TTS 直接回二进制流的协议完全跑不通；测试蒙版是不透明 JPEG（等于没有可重绘区域）；粘贴 curl 示例会对用户自己的 API 端点发一次 GET、404/登录页照样当文档喂给模型；「已校准」章不随手工编辑失效；报错中心一键修复会把音频协议强改成图片协议；未知占位符静默变空串；轮询间隔不设上下限。

## v0.3.1 及更早

见 git 提交历史与 README 路线图。
