# 工作流盘点与恢复方案（2026-09-19）

本文是执行维护前的只读盘点。实际是否完成，以 AppData/recovery 下维护报告及维护后读回数据为准。未读取或记录 API Key、桥接 token。

## 问题定位

- MOMO 当前模板共 14 条，全部 H3；不存在当前在库的放大模板。
- 其中 5 条旧 H3 的源路径已不存在，索引标记 `source_deleted`，但保留模板是同步引擎保护用户数据的既有行为。删除 ComfyUI 源文件不会自动删除 MOMO 模板。
- 其余 9 条分属 H3 Native 三条和 H3 常用六条。14 个模板各自关联唯一 workflowId，没有同一 workflowId 重复关联，但用途重叠。
- SeedVR2 五条 ComfyUI 源文件仍在，全部 `untracked`；未被导入 MOMO。原放大模板存在于两份 MOMO 备份，不能据当前列表缺失断言模型或原始工作流被永久删除。
- 来源 `src_NKK71cZO`：`G:\ciomfyui AI\ComfyUI-aki-v3\ComfyUI\user\default\workflows`，开启递归及新增自动入库。同步索引 39 条：25 untracked、9 synced、5 source_deleted。

## 保留与归档清单

保留四条 H3，加恢复两条放大，活动模板由 14 条调整为 6 条。首尾帧八步有本机完整历史生成记录，四步 v1.2 组合当时仅静态验证，所以默认保留八步。

| 保留模板 ID | 名称 | 同步工作流 ID |
|---|---|---|
| KfERil-G | 01_多参考_768p八步 | wf_cIOnRqbmGr |
| FZ34UNWt | 04_多参考_544p四步 | wf_0P_prKldJ0 |
| oLma7DJS | 02_首尾帧_768p八步 | wf_oHz2kX8kZ4 |
| jiVodXYj | 06_首帧_768p四步_v1.2 | wf_6JHy643Vkp |

| 归档模板 ID | 名称 | 同步工作流 ID |
|---|---|---|
| X-1-YesL | 05_首尾帧_768p四步_v1.2 | wf_J8f__rFExm |
| s4yUYn0_ | 03_首帧_768p八步 | wf_uiNtFXs4UW |
| WKigTL6J | 03_FL2VA_文生图生首尾帧_无LoRA | wf_VrQ5JLP_c8 |
| 0Faa01hh | 02_Singularity_多参考_单LoRA四步 | wf_fgVCTHbRYY |
| Znsu5dDp | 01_Ref2VA_多参考_无LoRA | wf_zjhkNUJa-J |
| 0F0TBHNc | 02_H3_MultiReference | wf_PDJO0301r1 |
| yXAi0GI2 | YZ金鱼-MiniMax H3-多参考生视频-直连版-全特效 | wf_RgWdx1ohuT |
| YK7aS1Ho | 01_H3_Text_Image_FirstLast | wf_qRdSgNJI6c |
| sXBlLa_n | YZ金鱼-MiniMax H3-多参考生视频-直连版 | wf_qa2nTxEKWn |
| 12u4sMCT | YZ金鱼-MiniMax H3-多参考生视频-优化版 | wf_SgABjbhpM1 |

归档仅移出 MOMO 活动模板及同步索引，原模板完整存入本次维护备份。ComfyUI 源文件和 AppData/comfy-sync 下正文、历史版本全部保留。给对应十条相对路径增加精确忽略规则，并关闭来源的新增自动入库，防止下次扫描重新出现。其他 25 条候选保留。

## 原放大模板恢复

应用数据根目录：`%APPDATA%\site.jinpengi.momo`。

| 原 ID | 名称 | 恢复备份 |
|---|---|---|
| iIOMlftz | 多倍放大seedvr2 | comfy-templates.backup-9ref.json |
| LAlsB92Q | SeedVR2 TTS多倍 | comfy-templates.backup-before-h3-combat-lora-20260822-181804.json |

2026-09-19 从正在运行的 `http://127.0.0.1:8909/object_info` 读取 3233 类节点。两条模板所需 SeedVR2、TTP、KOOK、PlaySound 等类均已加载，必填输入及选项匹配。分块模板节点 `129` 的 `cache_model` 被旧备份错误保存成字符串 `sdpa`，当前字段为 BOOLEAN；恢复时只将该项改为 `false` 并记录。其余参数保留原值。此为静态兼容性核验，不等同本次完整 GPU 放大生成测试。

`seedvr2_ema_7b_fp16.safetensors`（15.348 GiB）和 `ema_vae_fp16.safetensors`（0.467 GiB）位于 v3 `ComfyUI/models/SEEDVR2`，确实存在。目录另有 3B Q4、7B sharp Q4/FP16/FP8/混合 FP8 等权重，恢复不下载新模型。

五条未导入源位于 `workflows/SeedVR2`：图片 TTP 批量无损 PNG、图片 TTP 超大图无损 PNG、图片基础放大、视频快速 3B、视频高质量 7B。本次优先恢复原 ID 两条，避免额外增加五条重复入口。

## 引用保护

当前 14 条 H3 在活动画布和活动导演台未发现 `templateId` 引用。维护脚本执行前会再次核验，发现新引用则停止。

`iIOMlftz` 被归档画布 `QrAK8kfa` 的两个节点引用；`LAlsB92Q` 被归档画布 `UeZ61dge` 的一个节点引用。恢复原 ID 能保留这些关联。

活动导演台历史配方仍引用另外四个缺失 ID：`swY3ffEq`、`Yi1RBOpG`、`H4pPRxrp`、`nOXgmB5S`，两份旧模板备份可以找到这些对象。这是维护前已有缺失，不是本次归档造成。此次只记录，不批量恢复旧 H3/Krea 模板，也不替换项目里的配方 ID 或历史 Take 快照。

## 本机资源与性能证据

- GPU：RTX 5090，显存总量 32607 MiB；盘点时空闲 25731 MiB，是瞬时值。
- 当前 H3 主模型：Ref2VA INT8、FL2VA INT8、Hybrid INT8，三份各约 19.53 GiB 位于 v3；Singularity v1.3 INT8 为 34,004,507,622 字节，实际位于 `G:\ComfyUI-aki-v2\ComfyUI\models\diffusion_models\minimax H3`，由 v3 `extra_model_paths.yaml` 引入，线上 UNETLoader 已列出。
- 编码器：qwen3vl_32b_minimax_h3_nvfp4_awq（14.61 GiB）；H3 视频 VAE FP16（4.85 GiB）、音频 VAE FP32（0.564 GiB）。
- 现有 LoRA：Ref2V 四步 v0.1；FL2V 四步 v1.0 768p；FL2V 四步 v1.2 768p；FL2V 八步 v1.0 544p；FL2V 八步 v1.0 768p；Ref2V 八步 v1.0 768p。每份约 1.822 GiB。
- 当前已加载 H3 Turbo、FirstBlockCache、FaceRefine、SolAttention、H3 latent upscaler 相关节点。安装和节点加载不能自动证明它们已用于某一模板。
- `H3_Upgrade_20260912/升级完成与使用说明.md` 的历史实测：01 多参考八步 125.33 秒、02 首尾帧八步 138.69 秒、04 多参考四步 74.14 秒、06 首帧四步 92.29 秒。均为 124 帧 / 24fps，04 为 960×544，其他为 1344×768。缓存、分辨率不同，不能做严格速度倍数比较。
- 9 月 7 日清理记录声称移动到 `H3_cleanup_backup_20260907`，但该历史记录指定目录在本次盘点时不存在。因此只能依赖实际仍在的 MOMO 修订与当前源文件，不能承诺那份旧目录可恢复。

## 维护执行与回退

脚本：`.codex/maintenance/精简工作流-2026-09-19.py`。

默认执行仅输出预演，不写用户数据。显式 `--apply` 才执行；脚本强制检查 `momo-canvas` 进程已关闭，避免当前 WebView、Zustand 和 Tauri LazyStore 缓存随后覆盖磁盘修改。无需停止 ComfyUI。

顺序为：校验确定的 14 条 ID / workflowId / 来源路径 / 活动引用 / 两份备份 / 放大模型存在 → 确认 MOMO 已关闭 → 备份两份活动文件及两份恢复来源 → 写维护报告及恢复说明 → 原子替换两份活动数据 → 读回结构验证。进程检查在写入前再执行一次。发生写入错误会尝试原样恢复两份活动文件。脚本是针对本次确定清单的有界维护工具，不可拿来批量清空未知模板。

回退：关闭 MOMO，将本次 `recovery/workflow-maintenance-20260919-*` 下的 `comfy-templates.json` 与 `comfy-sync.json` 成对复制回应用数据根目录。正常应用恢复交给既有 Stop hook，不手动启动 `pnpm tauri dev`。
