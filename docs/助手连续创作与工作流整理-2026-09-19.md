# 助手连续创作与工作流整理（2026-09-19）

## 助手与画布

- 取消对话/生图/编辑三个可见标签及 Codex 创作、文件任务两个重复顶栏按钮。一个输入框连续讨论、生成、编辑；模型仍从统一模型选择器选择，文件夹任务在更多菜单。
- 生图、编辑、普通回复仍为不同内部动作。编辑执行原图加本轮修改原文，不追加品牌词、旧整图提示词；语音与文字共用发送入口。
- 新生成节点保存 referenceImages 快照，取消新建“助手参考图”副本。执行、预检、参考缩略图、输入审阅共用相同顺序；人工新接上游图时优先使用新图。快照随画布现有 blob 持久化保存。
- 画布图片、当前生成结果、放大结果可直接拖入助手，也可右键加入；整组展开图片成员并去重。引用后恢复节点原位、父组及 extent，不触发自动连线。重复“发到画布”不会再复制已有结果。

## ComfyUI 诊断

- 本机 8909 的 object_info 有 3233 类。9 个当前 H3 工作流的黄色缺节点提示均为 MarkdownNote 误报。
- 依赖检查识别已知前端备注/虚拟节点，递归检查已使用子图；未知类型及真实缺模型仍报告。
- 对未变化文件也刷新诊断，重连/手动扫描重新取 object_info，避免旧警告一直残留。

## 已实际整理的本机数据

2026-09-19 14:21 执行维护脚本，写入并读回验证：模板由 14 条 H3 改为 4 条 H3 + 2 条原放大；同步索引由 39 条变为 29 条（4 条 H3 + 25 条非 H3 候选）。10 条冗余/旧 H3 移出活跃列表并设置忽略规则，关闭该来源自动认领新工作流，防止清理后复现。没有删除模型或源文件。

保留：01 多参考 768p 八步、02 首尾帧 768p 八步、04 多参考 544p 四步、06 首帧 768p 四步 v1.2。

恢复：iIOMlftz「多倍放大seedvr2」、LAlsB92Q「SeedVR2 TTS多倍」，保持旧 ID。后者节点 129 的 cache_model 从不合法字符串 sdpa 修复为 false；其余按备份恢复。源文件一直存在，当前模板列表缺失不能证明源文件被删除。

备份：`%APPDATA%/site.jinpengi.momo/recovery/workflow-maintenance-20260919-142119-694261/`。内有原模板、同步索引、恢复原始备份、维护报告及恢复说明。关闭 MOMO 后成对恢复 comfy-templates.json 与 comfy-sync.json 可回退。

社区调研见 [H3 社区核查](H3社区核查-2026-09-19.md)，本机证据见 [工作流盘点](工作流盘点-2026-09-19.md)。本轮未改变 H3 采样参数：本机多参考八步历史 6/3 已跑通，作者推荐 12/3 尚未在本机作同素材 A/B；不得声称本轮获得最优画质。

## 验证

- pnpm build 通过（含 TypeScript，Vite 保留已有分包体积/静态动态混用提示）。
- node scripts/test-agent-routing.mjs 通过：原文编辑、取消、缺图阻止、普通聊天、生成规划、同一会话连续生成→编辑→讨论；编辑只新增结果节点。
- Comfy 同步纯逻辑快测通过，包含备注、子图、真实缺失与警告刷新。
- scripts/qa/agent-references.html 在浏览器真实 runner/store 上 7 项通过：快照重试输入、界面顺序、换图优先、整组多选图片。
- 界面实测：标题控制单排、三个模式标签和重复顶栏按钮已消失；真实拖入本地图片后助手出现附件，原节点回原位；测试节点已清理。
- 数据维护已读回校验与幂等复查。应用由项目既有 Stop hook 恢复，不手动启动 tauri dev。
- 本轮未调用付费图片服务或执行完整 H3/SeedVR2 推理；不能把代码检查与依赖核验说成成片质量验证。
