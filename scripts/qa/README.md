# 流程回归

最新真实服务、原生导出、骨骼/语音、性能及生产能力的结果见 `docs/真实环境与生产能力验收-2026-09-07.md`。新增 `deep.html` 提供骨骼/刀线/语音及 5000 节点检查；`production_test.py` 运行受控本地生产验收。`live.html`、`elements.html` 需要专用本地代理，会实际调用已配置的付费模型，不属于普通无费用回归。

在现有开发服务运行时，打开 `http://[::1]:1430/scripts/qa/audit.html`，点击「运行流程回归」。不要在普通 localhost 预览来源运行；测试页主动检查独立来源。

此页导入真实的助手引擎、模型协议适配器、画布/资产 store、宫格与 3D 引擎，模拟聊天 SSE 和绘画响应。所有非模拟网络请求均阻断。测试不会初始化用户模型配置；假模型配置只在本页内存中，测试画布与主预览按来源隔离。测试图片为本地产生的颜色块和像素图，不使用用户素材。测试页不进入 Vite 正式入口产物。

覆盖连续两轮生图、自由输入确认、过期确认卡、停止恢复、图片参考顺序、异常动作字段、宫格逐像素重拼、PSD 原位合成、平面拆件的尺寸与颜色、内置 Skill、导演流程导引、3D 取景比例与异步删除。2026-09-06：16 项通过，0 项失败。

这不能替代付费服务商实测、Tauri 原生导出和真实模型输出质量验收。

2026-09-07：扩展为20项通过、0失败；新增“不用旧图”实际请求、毫米/像素单位往返、PNG DPI像素保持与块去重、三套场景模板完整性检查。

视觉回归入口：`http://[::1]:1430/scripts/qa/visual.html`。用真实 ImageNode / GroupNode 渲染自制九宫格，点击「检查元素工坊」检查手工圈选降级、平面稿参数和图片边界。此页阻断真实网络请求；源图为程序生成的测试图案。

2026-09-07 收尾：31 项通过、0 失败。额外覆盖尺寸画板与撤销、品牌隔离、AI 制图连续运行/历史/缺失素材、ComfyUI 实际提交正负提示词、取消终态、能力重试、关键帧和真实视频录制/合成。所有模型提交均为受控模拟。

新增界面验收页：`http://[::1]:1430/scripts/qa/finishVisual.html`，使用真实设计工具、画布、3D 和 AI 制图组件，包含测试文化墙素材与场景，不连接真实模型。

2026-09-07 追加：`followup.html` 验证平面总稿与 H3 中文写作（7 项）；`node scripts/qa/run-core-tests.mjs` 运行 12 个核心测试文件，新增 H3 合同 13 项。黑色主题、资产分段按钮与双语编辑也可在 followup 页面视检。
`planar-live.html` 需要以 `MOMO_QA_MODE=planar` 主动启动 live-server.mjs，最多提交一张图片和两次对话。`planar-replay.html` 依赖本机已有 `.Codex/planar-acceptance/report.json`，重放真实返回稿，不调用付费模型。详情见 `docs/平面总稿与双语写作改造-2026-09-07.md`。
